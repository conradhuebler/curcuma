# AI-Generated Changes Log

This file tracks significant improvements, refactorings, and new features generated by AI assistants (primarily Claude).

Format: One line per change, newest first.

- **Native xTB d-shell on ROCm GPU (X-I1 B6, Jun 28, 2026)**: the ROCm/HIP native-xTB path now handles d shells, mirroring the CUDA port. The d-shell `__device__` helpers (cartesian->spherical dtrafo + general `d_cartMoment1d` + d overlap/multipole/gradient elements) were copied verbatim from `cuda/xtb_gpu_integrals_device.cuh` into `rocm/xtb_hip_integrals.hiph`, and the same `la>=2||lb>=2` branch added to the three HIP kernels (`k_overlap_h0`/`k_multipole_ints`/`k_grad_h0_pulay` in `xtb_hip_context.hip`); `HipScfBackend::supportsDshell()` now returns true, so d systems run fully device-resident instead of the CPU-fallback warning. s/p path byte-unchanged (the `!dpair` branch). **Validated on an AMD Radeon 890M (gfx1150):** `-gpu rocm` energy bit-identical to CPU at 8 dp (H2S/PH3/SiH4/HCl, gfn1+gfn2; new `sqm_val_rocm_*` ctests 32/32 incl. s/p non-regression + the xfail set), and `-opt` on H2S (gfn2) tracks the CPU trajectory step-by-step in energy and gradient norm (gradient floor ~1e-6 from the default FP32 mixed-precision eigensolve; `-scf_threshold 1e-8` for tighter). Vulkan d still falls back to CPU (GLSL shaders pending). See [docs/SQM_DSHELL_WP.md](docs/SQM_DSHELL_WP.md). 🤖 machine-tested (ROCm).

- **Native xTB d-shell on CUDA GPU (X-I1 B6, Jun 27, 2026)**: the CUDA device integral (overlap/H0/GFN2-multipole), resident SCF and nuclear-gradient path now handles d shells. Per-spherical-AO-element `__device__` functions (`cuda/xtb_gpu_integrals_device.cuh`) port the cartesian(6)->spherical(5) dtrafo + general `d_cartMoment1d` (la,lb<=3) from `xtb_ao_utils.hpp`; the 4 integral/gradient kernels in `xtb_gpu_context.cu` branch on `la>=2||lb>=2` (d-touching), s/p path byte-unchanged. Gated by a new `GpuScfBackend::supportsDshell()` (CUDA overrides true; ROCm/Vulkan stay false -> validated CPU fallback). **Validated on a GTX 1660:** `-gpu cuda` energy bit-identical to CPU (H2S/PH3/SiH4/HCl, gfn1+gfn2), device gradient vs CPU ~1e-16 (`test_xtb_cuda_gradient`), `-opt` converges, s/p `gpu_gfn{1,2}_validation` 32/32 unchanged. ROCm/Vulkan device d kernels are a follow-up (no AMD/Vulkan hardware here to validate). See [docs/SQM_DSHELL_WP.md](docs/SQM_DSHELL_WP.md). 🤖 machine-tested (CUDA).

- **Native xTB d-shell integrals (X-I1, Jun 27, 2026)**: native GFN1/GFN2 now support d-shell basis functions (main-group S/P/Cl/Si/…). New `xtb_ao_utils.hpp` is the single source of truth for `as_cgto_shell`/`ao_to_type` (resolves the X-I5 4× duplication) and the cartesian(6)→spherical(5) `dtrafo` taken verbatim from tblite `integral/trafo.f90`, plus a general `cartMoment1d` (la,lb≤3) and the d overlap/multipole/gradient blocks. Per shell-pair, only d-touching pairs use the transform path; pure s/p pairs keep the existing scalar kernels, so all s/p molecules stay **bit-identical** (H2O/CH4/NH3/C6H6/HCN/caffeine dE=0 vs tblite). Validated: overlap max|ΔS|=1.5e-10 vs tblite; SCF energy ≤1e-8 Eh vs tblite (H2S/PH3/SiH4/HCl, gfn1+gfn2); analytic gradients FD-pass (`test_xtb_gradient`, GFN1 ~1e-6 / GFN2 ~1-4e-4 = s/p FD floor); `-opt` on H2S converges. The B0 d-shell guard (`xtb_native.cpp`) was removed; the 4 d molecules joined the `sqm_val_*` ctest suite at the 1e-8 gate. **GPU**: device integral/SCF/gradient kernels are still s/p-only, so `m_has_dshell` (set in `buildBasis`) routes d systems to the validated CPU path (atom-based D4/EEQ stay on device) — device d kernels (CUDA/ROCm/Vulkan) are a follow-up. **Transition metals enabled but NOT validated** (main-group d only). See [docs/SQM_DSHELL_WP.md](docs/SQM_DSHELL_WP.md). 🤖 machine-tested.
- **Tech-debt fail-loud + dispatch hardening + -sp energy-only (Jun 26, 2026)**: resolved the highest-priority TECHNICAL_DEBT.md correctness items so a failed native calculation can no longer be silently reported as E=0. **A1/X-I1-root** (`xtb_native.{h,cpp}`, `native_xtb_method.cpp`): added a hard-error flag to the `curcuma::xtb::XTB` engine, set at genuine solve breakdowns (eigensolver/density/multipole failure, singular overlap) distinct from benign max-iter non-convergence; the wrapper checks `m_xtb->hasError()` and surfaces it. **B0** (`xtb_native.cpp` InitialiseMolecule): the s/p-only native integral kernels now refuse d-shell elements (Na/Mg/Al..Ar + transition metals) with a clear message instead of producing a singular overlap → silent 0 Eh (full d-shell integrals are the open WP). **A2/F-Q4** (`eeq_solver.{h,cpp}`, `gfnff*.{h,cpp}`): the GFN-FF EEQ uniform-charge fallback now sets `lastSolveFailed()`; GFN-FF propagates it (`eeqSolveFailed()`) and the wrapper refuses the result. **A3/D-2/D-4**: `-sp` and the optimizer driver now check `EnergyCalculator::Error()` and abort with a non-zero exit (MD already did); deleted the dead `energycalculator_enums.h`. **A4/X-L4** (`xtb_fragment_scf.cpp`): `large_system_mode=dc|sparse` hard-error when a gradient is requested instead of returning a zero gradient an optimiser mistakes for converged. **A6/D-1** (`energycalculator.{h,cpp}`): method sub-scopes are re-merged before the single `createMethod`, so a JSON-constructed EnergyCalculator no longer builds the method twice. **X-S2**: removed the dead strict-threshold `checkConvergence` in `xtb_scf.cpp`. **X-G3** (`xtb_scf.cpp`/`xtb_gradient.cpp`/`xtb_native.h`): the gradient's energy-weighted density `W` now uses the per-MO occupations that built the density (`Wavefunction::focc`) — `W = Σ_i f_i·ε_i·C_iC_iᵀ` — so at the default `electronic_temperature = 300 K` the Pulay term matches the fractional density for small-gap systems (bit-identical for gapped molecules where f_i ∈ {2,0}). **X-I3** (`xtb_h0.cpp`/`xtb_native.{cpp,h}`): memoised `computeCoordinationNumbers()` (called up to 3×/Calculation), invalidated on every geometry change — bit-identical. **X-S6** was found stale (the post-SCF potential is only rebuilt for `m_F` on the device path, and the gradient rebuild excludes dispersion — no double D4 eval on the CPU path), so no change. **-sp is now energy-only by default** with an opt-in `-gradient` flag (`main.cpp`), avoiding an unused gradient and letting energy-only large-system modes serve a single point. **2 pre-existing ctests fixed**: `scf_extrapolation_caffeine` (generated the missing `caffeine.opt.xyz` interpolation endpoint + gitignore exception); `gfnff_numgrad_fixed_charges` retired — its premise (analytical gradient = frozen-charge approximation) is obsolete since the Feb-2026 Coulomb Term-1b/dynamic-charge work made the analytical the TOTAL derivative (validated by `gfnff_numgrad_builtin`, full EEQ), so comparing it to the fixed-charge PARTIAL derivative is invalid (H2O total -1.2e-2 vs partial +2.9e-2 Eh/Bohr). Verified: H2S native gfn2 errors with exit 1 (was 0 Eh exit 0); all gfn1/gfn2 s/p energies (H2O/CH4/NH3) bit-identical; `large_system_modes` test green incl. new -gradient hard-error checks; native-GFN gradient/cpscf/d4/validation + gfnff validation suites green. The 2 pre-existing ctest failures (`gfnff_numgrad_fixed_charges`, `scf_extrapolation_caffeine`=missing input file) are unrelated (confirmed by stash baseline + identical diffs).

- **GFN-FF ROCm catch-up: WP-A + WP-B + many-fragment EEQ (Jun 26, 2026)**: ported the two recent CUDA GFN-FF GPU work packages to ROCm/HIP and closed the many-fragment EEQ gap. **WP-B** (`-gfnff.eeq_mixed_precision`, opt-in default OFF): FP32-factor + FP64-refine EEQ solve via `mixedFactorHip`/`mixedSolveRefineHip` + cast kernels in `rocm/eeq_solver_hip.hiph` (rocSOLVER `spotrf`/`spotrs` + rocBLAS `dsymm`), gated to nfrag<=1 in `eeqBuildFactorSolve`, auto-fallback to FP64 dpotrf/LU when the FP32 factor is not SPD. **WP-A** (`-gfnff.gpu_disp_pairs_on_device`, opt-in default OFF): on-device two-pass D4 dispersion pair build (`k_disp_pairs_count`/`k_disp_pairs_build` + `FFWorkspaceHip::generateDispersionPairListOnGPU` in `gfnff_rocm.hip`), plus a ROCm-only rebuild of the per-atom CSR adjacency for `k_dispersion_gather` (no CUDA precedent); `setSkipHostDispPairs` + the device build wired in `gfnff_hip_method.cpp`. **Many-fragment EEQ** (`-gfnff.eeq_rocm_cpu_fragment_threshold`, default 16): routes nfrag>=threshold to the exact CPU PCG+block-Jacobi (after `finalizeCNForCPU`) instead of the intractable dense N×N device Cholesky, restoring CPU-consistent charges for solvent boxes. Validated Radeon 890M/gfx1150: caffeine + 231-atom `complex` energy and caffeine 8-step opt bit-identical to the FP64/host ROCm path for WP-A/WP-B; water8_cluster (threshold 4) opt trajectory matches the pure-CPU `-gpu none` reference (the device path is the divergent one). `ctest cli_gfnff_gpu_02_rocm_singlepoint` + 17 gfnff validation tests green. CPU levers 3/5 already shared (no port). Device-resident HIP PCG/block-Jacobi remains the open follow-up. The pre-existing `gfnff_numgrad_fixed_charges` CPU failure is unrelated (confirmed by baseline).

- **GFN-FF Lever 3: dispersion C6 half-contraction + dc6dcn cutoff (Jun 25, 2026)**: cut the per-pair D4 C6 inner loop from 7×7=49 FMA to 7 via a per-atom half-contraction keyed by partner element (`g_i[Zj][b]=Σ_a w_i[a]·c6ref`), reused in BOTH `getChargeWeightedC6` and `computeDC6DCN` (Opt B, behind PARAM `disp_half_contraction`, default on; reassociates the FP sum ~1e-16). `computeDC6DCN` also now iterates the in-cutoff dispersion pair list instead of building the full dense N×N matrix with no cutoff — the beyond-cutoff/diagonal cells were never read by the gradient consumer (Opt A, exactly bit-identical). Measured mixed_3007 SP ~4.93→4.79 s @4thr (~3%); Opt-A bit-identical and Opt-B 6-dp-identical (energy+grad) over a 10-step mixed_1001 opt; CPU dEdcn == GPU dEdcn on 1410-atom polymer. `-disp_half_contraction false` reproduces pre-B byte-for-byte. `docs/GFNFF_PERFORMANCE_LEVERS.md` lever 3 marked done.

- **Precision/threading WP doc + fp32-threshold help fix (Jun 22, 2026)**: added `docs/SQM_PRECISION_THREADING_WP.md` capturing the perf session — BLAS thread-starvation fix (done), per-card `scf_fp32_threshold` tuning, GFN-FF EEQ mixed-precision iterative refinement, device-FP64-ratio auto-default, tensor-core/multi-GPU ideas, plus the polymer CPU-scaling/GPU benchmark and the FP64 hardware matrix. Also fixed the backwards `scf_fp32_threshold` PARAM help (`xtbinterface.h`): SMALLER threshold = more FP32 iters = fewer FP64 polish iters (was stated inverted), which would have misdirected per-card tuning.

- **Native xTB SCF eigensolve threaded on OpenBLAS (Jun 22, 2026)**: `MklThreadScope` (the per-thread scope that bumps BLAS threads only around the GFN1/GFN2 generalized eigensolve, budget = `effectiveIntraThreads`) was MKL-only; on the OpenMP-threaded OpenBLAS build `MKL_Set_Num_Threads_Local` is a no-op and curcuma's global OMP=1 pin starved the eigensolve. Extended it to also set the per-thread `omp_set_num_threads` (nesting-safe). complex/231 `-threads 8` vs 1: gfn1 2.69x, gfn2 2.41x, energy bit-identical; native gfn ctests pass. Audit: opt-path eigensolves don't benefit as-is — rf_solver/lbfgs use Lanczos for large systems (full dsyevd only nvar<50), hessian/ancopt use Eigen's own non-BLAS solver.

- **GFN-FF HB-CN data race fixed (Jun 22, 2026)**: `FFWorkspace::computeHBCoordinationNumbers` ran inside partition 0's `executeGFNFF` and wrote the SHARED `m_bonds[].hb_cn_H` + `m_hb_grad_entries`, but there was NO barrier before partitions 1..N ran `calcBonds()` — they read a half-written `hb_cn_H` (wrong bond exponent) and raced on the `m_hb_grad_entries` push_back. Result: run-to-run non-deterministic BOND energy (~0.2 Eh drift on the 6200-atom/1400-fragment mixture; only the Bond term drifted, all others bit-identical) and thread-count-dependent totals. Fixed by computing HB-CN once on the main thread in `calculate()` before the parallel dispatch (`ff_workspace.cpp`); partitions now only read it. Energy now deterministic and thread-count-independent.

- **EEQ Schur solve: thread the LAPACK (Jun 22, 2026)**: system OpenBLAS is the OpenMP build, so curcuma's global OMP/MKL=1 pin (CxxThreadPool, main.cpp) starved the GFN-FF EEQ dpotrf/dpotrs to 1 thread; added `src/core/blas_threads.h` `ScopedBlasThreads` RAII (driven by the EnergyCalculator/`num_threads` budget) around the solve — factorize ~4.3x, Z2 back-substitution ~4.6x at 8 threads (mixture2 6200 atoms/1400 frag: solve ~3.4s->~0.75s), energy bit-identical, 23/23 gfnff+eeq tests pass. Also: `schurComplementCZ` exploits the fragment-membership constraint matrix (O(N*nfrag) vs dense O(nfrag^2*N)). Open: native-QM `xtb_scf` dsyevd + opt Hessian eigensolves have the same starvation (not yet wired); `-threads>1` energy drift is a separate pre-existing non-determinism.

## June 2026

- **EEQ WP7-D — GPU block-Jacobi PCG + contact-aware CPU dispatch (June 21)**: closed two many-fragment (solvent/host-guest) EEQ weaknesses. **CUDA**: ported the CPU per-fragment block-Jacobi preconditioner (`EEQSolver::buildBlockJacobi`) to the GPU-PCG, replacing the diagonal Jacobi (`d_pcg_M_inv=1/A[i,i]`) that converged slowly with many fragments. New `buildBlockJacobiFactors()` (`eeq_solver_gpu.cu`) builds the per-fragment Coulomb blocks (reusing WP6 `k_eeq_build_fragment_matrices`/`d_A_blocks`), factorizes (`cusolverDnDpotrf`) + inverts (`cusolverDnDpotri`) + symmetrizes (`k_eeq_symmetrize_blocks`) each block; new `k_eeq_block_jacobi_apply` (one block/fragment, symmetric GEMV with explicit inverse, gather/scatter via `frag_atom_map`) applies `z=blockdiag(A_ff⁻¹)·r` each PCG iteration. Non-SPD or N_f>2048 → diagonal fallback; invalidated on topology change and in the batched path. Cuts PCG iters ~30-100→~2-5, **exact**: GPU PCG+block-Jacobi == GPU SchurCholesky bit-identical (≤1e-8, 27-water cluster `-opt` step-by-step). **CPU**: the batched auto-select (drops cross-fragment Coulomb) was density-only; now `m_contact_min_dist` (min inter-fragment distance from the packed distances + fraglist) routes in-contact systems (`< eeq_batched_min_distance`) to the exact solver. New PARAM `eeq_contact_prefer_exact` (default true; false = old behavior). Verified: in-contact 27-water → default Auto picks exact (−8.77203498 == cholesky == PCG+BJ); batched differs 3.58 mEh. FMM matvec + ROCm mirror left as future work. Pre-existing failures (numgrad_fixed_charges, gpu_vs_cpu h2/polymer — all single-fragment) confirmed unrelated via stash-rebuild. See [docs/GPU_WP7_EEQ_LARGE_SYSTEMS.md](docs/GPU_WP7_EEQ_LARGE_SYSTEMS.md). 🤖 AI-generated, ⚙️ machine-tested.
- **SimpleMD temperature ramps + live setpoint + thermal regions (June 23)**: `setTargetTemperature()` changes the thermostat target `m_T0` live between steps (qurcuma slider); new `temp_ramp`/`temp_schedule` PARAMs drive a multi-stage schedule (`target:mode:value;...`, mode `steps`=ramp over N steps, `reach`=advance once `|<T>-target|<value` K); new `temp_regions` JSON array thermostats atom subsets (`FragString2Indicies` selection) to their own target/ramp (Berendsen/CSVR/Anderson per region + default complement; Nosé-Hoover falls back to the global chain). Ramp state refactored into `RampState`; `ApplyThermostat()` replaces the two `ThermostatFunction()` call sites and is byte-identical to the legacy path when no regions are defined. Verified: schedule advances at exact steps, both modes run, `<T>` tracks the setpoint, two-region run drives `<T>` to the DOF-weighted average, `-export_run`/`-import_config` round-trip. See [docs/TEMPERATURE_RAMP.md](docs/TEMPERATURE_RAMP.md). 🤖 AI-generated, ⚙️ machine-tested.
- **GFN-FF ROCm performance — per-atom gather + GPU-wedge hardening (June 21)**: profiling the phase-2 kernels (polymer/1410, gradient mode) showed the pairwise Coulomb (~993k pairs, no cutoff) and the deferred dispersion gradient (~943k pairs) dominated the MD step — ~6M+ FP64 `atomicAdd` into grad/dEdcn on RDNA. Rewrote both as **per-atom GATHER** kernels (`k_coulomb_gather`/`k_dispersion_gather`, per-atom CSR adjacency; dynamic dc6dcn read via a pair-index + is_i flag so no per-step rebuild; plus a dense shared-mem-tiled `k_coulomb_dense` using the per-atom EEQ alpha), accumulating per atom with 3-4 atomicAdd/atom — bit-identical to the pair kernels. **polymer/1410: phase-2 122→15 ms, energy+gradient eval ~150→~26 ms, MD slower-than-CPU → ~2.3× faster** (30 fs: 10.1 s CPU-powersave vs 4.4 s ROCm, back-to-back); energy/`-opt` match CPU. Insight: FP64 atomics aren't the RDNA problem per se (the gfn2 ROCm path uses them fine on ≤231 atoms where rocSOLVER/rocBLAS dominate) — the issue was *contention* at ~1M pairs, which the gather removes; GFN-FF's O(N²) FP64-erf Coulomb has no BLAS analog. Also added **GPU-wedge hardening**: `FFWorkspaceHip` installs a SIGINT/SIGTERM/SIGABRT handler that `hipDeviceReset()`s, so an interrupted run (Ctrl-C/`kill`/`timeout`) cleans up the GPU instead of wedging the RDNA compute queue (only `kill -9` can't be caught). See [docs/SQM_ROCM.md](docs/SQM_ROCM.md). 🤖 AI-generated, ⚙️ machine-tested.
- **GFN-FF on ROCm/HIP (`-gpu rocm`, `USE_ROCM_GFNFF`, June 21)**: ported the CUDA GFN-FF GPU pipeline to AMD ROCm. The full energy + nuclear-gradient kernel stack (bonds/angles/dihedrals/inversions/repulsion/Coulomb/dispersion/HB/XB/ATM/BATM + CN + CN-chain-rule, 3 streams + hipGraph) is a single-TU hipify of `cuda/gfnff_*.cu` under `ff_methods/rocm/gfnff_rocm.hip`, with distinct class names `FFWorkspaceHip`/`EEQSolverHip` (zero change to the CUDA path; both backends coexist). Compiled by `hipcc -c` into one `EXTERNAL_OBJECT` linked with g++ (libgomp-safe, same pattern as the xTB ROCm block). **Stolperstellen fixed**: HIP `__shfl_down_sync` needs a 64-bit mask (warp reduction, wave32 on RDNA); `D4_MAX_REF` macro-vs-member include-order; `FMT_HEADER_ONLY` for the device TU; `CxxThreadPool.hpp` wrapper. **Device EEQ** = rocSOLVER `dpotrf`/`dpotrs` (+ `dgetrf`/`dgetrs` LU fallback) → z1/Z2, with the host wrapper's exact CPU Schur complement applying the charge constraint (nfrag=1 and >1); the device-resident GPU-Schur/PCG/batched variants are intentionally not ported. New host wrapper `qm_methods/gfnff_hip_method.cpp`; factory routes `-gpu rocm` → `GFNFFHipComputationalMethod`. **Validated** on Radeon 890M/gfx1150: single-point energy AND gradient norm match CPU ≤1e-7 (water/CH4/caffeine/231-atom complex), `-opt` tracks CPU; `ctest cli_gfnff_gpu_02_rocm_singlepoint`. Built `USE_D4=OFF`, but GFN-FF runs **true D4** anyway: `generateGFNFFDispersionPairs` always uses the self-contained `D4ParameterGenerator` (Casimir-Polder C6, compiled unconditionally), which has no `USE_D4` gate and never touches the external `dftd4interface`/`curcuma_d4`/LAPACKE (that flag drives only the standalone `-d4` method) — verified the D4 reference/Casimir-Polder path executes; matches CPU (same generator). (An earlier "free-atom fallback" note here was wrong.) See [docs/SQM_ROCM.md](docs/SQM_ROCM.md). 🤖 AI-generated, ⚙️ machine-tested.
- **GFN-FF Task #11: literal nhb1/nhb2 count diff vs Fortran analyzer (June 20)**: added a `-verbosity 3` "HB split: nhb1 = .., nhb2 = .." diagnostic and compared it to the Fortran analyzer's `nhb1=`/`nhb2=` lines. Result: **nhb2 exact everywhere**; nhb1 exact for acetic_acid_dimer (42) and triose (1495), off by only −2 (caffeine 352 vs 354) and −16 (complex 8827 vs 8843) — a 0.1–0.2 % near-threshold tail with **zero energy impact** (ΔE ≤ 1e-7). Root cause: both codes apply the same A–B strength pre-filter (curcuma mirrors Fortran `gfnff_ini.f90:833` `basicity*acidity < 1e-6`), but curcuma's basicity/acidity values differ minutely from Fortran's `hbonds()` `hbpi`/`hbpj` for a few pairs sitting exactly at the 1e-6 cut, so they fall on opposite sides. Disabling the filter is NOT parity (Fortran has it; unfiltered count is ~5× larger). No energy-relevant fix needed — closing the gap would mean bit-matching the HB acidity/basicity params, zero payoff. See [docs/GPU_GFNNF_DISCREPANCIES.md](docs/GPU_GFNNF_DISCREPANCIES.md). 🤖 AI-generated, ⚙️ machine-tested.
- **GFN-FF Task #10/#11: performance/accuracy tuning knobs for the GPU CN pair list + HB list (June 20)**: exposed 8 new `gfnff` PARAMs (single-line form — the `param_parser` silently drops multi-line PARAMs whose help text contains `)`, which is why pre-existing `eeq_refactor_*`/`solvent` never registered). **Task #10 (GPU CN-derivative pair list)**: `gpu_cn_pair_regen` (default true) now drops the build-once latch (`m_cn_pairs_generated`) when the topology-displacement check fires, so the list no longer goes stale during `-opt`/MD; `gpu_cn_pair_regen_every` (force regen every N grad steps) + `gpu_cn_pair_cutoff_factor` (default 2.5, was hardcoded in `ff_workspace_gpu.cu` + the CPU fallback) tune the perf/accuracy trade. **Task #11 (HB list)**: `hb_accuracy` (default 0.1) drives the Fortran thresholds `hbthr1=200-log10(acc)*50`, `hbthr2=400-log10(acc)*50` (`gfnff_param.f90:553`); `hb_thr1_bohr2`/`hb_thr2_bohr2` direct overrides; `hb_update_rmsd_bohr` (default 0.3 = Fortran `gfnff_ini2.f90:717` rebuild trigger) + `hb_update_force_every` for continuous-MD near-threshold re-classification. **Defaults are bit-identical to before** (`log10(0.1)==-1.0` exactly → 250/450; regen-on is energy-neutral for `-sp`). **Validated**: `gfnff_val_*` 18/18; `gfnff_gpu_vs_cpu_*` 19/20 (the 1 failure is a pre-existing missing `h2.xyz`); per-frame GPU-vs-CPU gradient mean 2.5e-9/max 7.1e-9 over a 42-frame MD trajectory with regen ON; HB knob functional (`hb_thr1_bohr2=20` drops acetic-dimer HB count 48→16, energy -2.47120432→-2.46441994); GPU regen on/off/cutoff self-consistent for `-opt`; the CPU-vs-GPU `-opt` divergence and `gfnff_numgrad_fixed_charges` failure are pre-existing (reproduced with my changes git-stashed). **Follow-up (Task #11 element-wise HB-list check)**: curcuma's static HB list matches Fortran `xtb-gfnff` to ≤4e-7 Eh on acetic_acid_dimer/caffeine/triose/complex AND across a water-dimer O-O scan through the hbthr1 boundary (native==Fortran ~1e-8 even at the 8.3→8.5 Å nhb 4→0 crossing — near-threshold pairs are energetically negligible), and `hb_accuracy` shifts the cutoff exactly per `sqrt(200-log10(acc)*50)` Bohr; the MD "nhb 39 vs 48" divergence is trajectory chaos, not a list-construction bug. See [docs/GPU_GFNNF_DISCREPANCIES.md](docs/GPU_GFNNF_DISCREPANCIES.md). 🤖 AI-generated, ⚙️ machine-tested.
- **Vulkan: D4 EEQ q-response ∂q/∂x on device → fully device-resident GFN2 D4 gradient (June 18)**: the last Vulkan D4-gradient host hold-out — the EEQ charge-response `Σ_A dE_D4/dq_A · ∂q_A/∂R` — now runs on the iGPU, so the **entire Vulkan GFN2 D4 gradient (in-SCF dEdq + 2-body + ATM + q-response) is device-resident, on par with ROCm**. The piece Vulkan lacked vs ROCm/CUDA was a device dense solve (no rocSOLVER/cuSOLVER): supplied as a hand-written **single-workgroup FP64 Gaussian elimination WITHOUT pivoting** (`d4eeq_solve.comp`) — the screened-Coulomb hardness block + charge-constraint border is non-singular without row swaps, so the no-pivot factor agrees with the partial-pivoted host/ROCm LU to rounding. The other three SPIR-V kernels (`d4eeq_cn` raw CN, `d4eeq_build` augmented (N+1) matrix, `d4eeq_resp` ∂q/∂x gather — one workgroup-thread per atom, no FP64 atomics) port the CUDA/HIP math verbatim. **GLSL-fp64 has no `erf`**, so a Numerical-Recipes `derf` (erfcc, ~1.2e-7) was added to `vk_integrals.glsl` — ample, the q-response is a small gradient term. The O(N) log-compression / RHS / u-weight stay host-side on the mapped buffers (as ROCm does on the device). New context API `eeqCharges`/`eeqChargeResponseGradient` + the `supportsDeviceEeq()→true` wrapper override reuse the existing CUDA-shared `GpuScfBackend` seam + host integration (`xtb_native.cpp` unchanged); `scf_guess=eeq` also routes through the device. **Validated (AMD 890M/RADV): GFN2 `-sp` E+gradnorm match CPU on H2O/HCN/NH3 (7 dp); device EEQ charges = host model exactly (O=−0.643500); per-iter SCF trace bit-identical from iter 1 with final E identical, iter-0 differs ±1e-10 — the `derf` signature, which confirms the device path runs rather than silently falling back to host; GFN2 `-opt` (HCN) trajectory bit-identical to CPU; GFN1 + CPU byte-unchanged; `ctest -L vulkan` 22/22.** New ctest `cli_gpu_gradient_02_vulkan_gfn2_qresponse`. Completes the Vulkan D4-gradient staircase ([docs/SQM_GPU_ROADMAP.md](docs/SQM_GPU_ROADMAP.md) X-AP4); only CUDA now trails (2-body + ATM still host). 🤖 AI-generated, ⚙️ machine-tested.
- **ROCm: D4 EEQ q-response ∂q/∂x on device → fully device-resident GFN2 D4 gradient (June 17)**: the last D4-gradient host hold-out — the EEQ charge-response `Σ_A dE_D4/dq_A · ∂q_A/∂R` — now runs on the AMD GPU, so the **entire ROCm GFN2 D4 gradient (in-SCF dEdq + 2-body + ATM + q-response) is device-resident**. Verbatim HIP port of the 6 CUDA `k_d4eeq_*` kernels (CN / augmented (N+1) matrix build / RHS / adjoint-RHS / CN-response weight / pairwise response gather — one thread per atom, no atomics) + the (N+1) LU via **rocSOLVER `dgetrf`/`dgetrs`** (the factor is kept and reused for the adjoint solve `M·z = [dEdq;0]`, mirroring cuSOLVER). `supportsDeviceEeq()`→true also routes the **scf_guess=eeq initial guess** through the device. The host integration (`xtb_native.cpp`) and the `GpuScfBackend::eeqCharges/eeqChargeResponse` interface already existed (CUDA-shared) — only the ROCm backend + the `.hip` were added; no shared file changed (Vulkan untouched, still host q-response). **Validated (Radeon 890M/gfx1150): GFN2 `-sp` energy 8-dp identical AND same SCF iteration count (11/19 — the device EEQ guess matches the host); GFN2 `-opt` (triose) FP64 trajectory BIT-IDENTICAL to CPU in every column (the full device D4 gradient reproduces the host bit-for-bit); device EEQ path confirmed active (trace); GFN1 + CPU byte-unchanged; `ctest -L native_xtb|gfn2_validation|gfn1_validation` 34/34.** Completes the ROCm D4-gradient staircase ([docs/SQM_GPU_ROADMAP.md](docs/SQM_GPU_ROADMAP.md) X-AP4). Vulkan keeps the host q-response (no device dense solver). **Inversion to note: the 2-body + ATM D4-gradient kernels (`k_d4_grad`/`k_d4_atm`) are net-new — they do NOT exist in CUDA, so CUDA now TRAILS ROCm/Vulkan for the D4 nuclear gradient** (CUDA: only dedq + q-response on device, 2-body + ATM still host). Catch-up = port the two kernels into `cuda/xtb_gpu_context.cu` + override `dispersionGradient`/`dispersionATM` in `xtb_gpu_method.cpp` (roadmap X-AP4). 🤖 AI-generated, ⚙️ machine-tested.
- **ROCm + Vulkan: D4 ATM 3-body gradient on device (June 17)**: the post-SCF D4 ATM (Axilrod-Teller-Muto 3-body: energy + nuclear gradient + dE/dCN) now runs on the GPU in a per-atom gather over pairs {x,y} (the triple {a,x,y}), joining the 2-body. New kernels `k_d4_atm` (HIP) / `d4_atm.comp` (FP64 SPIR-V, 8 SSBO). **Key trick: the triple-edge force is antisymmetric** (`dG_ba = -dG_ab`; the dftd4 `dang` is symmetric in its edge endpoints, verified), so atom `a` accumulates only ITS member's force from each triple → **no FP64 atomics**, each triple's energy counted once per member (host: E = ⅓·Σ e_atom). Verbatim port of `D4Evaluator::computeATM` (dftd4 atm.f90). The host does the cheap O(N²·49) q=0 reference C6/∂C6∂CN contraction (`buildAtmC6Flat`, new) + uploads the two nat² matrices; the device does the O(N³) triple loop, reusing the resident geometry + √r4r2 from beginDispersion. **GLSL fp64 has no `pow`/`log`, so the non-integer damping power `(r0/r1)^(alp/3)` is computed as `cbrt(b^alp)` via Newton** (alp integer = 16; 3 iters from a float seed → ~1e-15) — self-contained, no `dlog`. New seam `GpuScfBackend::dispersionATM`; host `calcDispersionEnergy` gates it on the 2-body having gone device. **Validated (Radeon 890M / RADV + gfx1150): GFN2 `-opt` (triose) Vulkan-FP64 trajectory BIT-IDENTICAL to CPU in every column (incl. Energy-Change + gradient norm) — the cbrt-Newton power is exact enough that the full device D4 (2-body + ATM) reproduces the host to below display; ROCm matches to 6 dp (FP32-SCF delta only). GFN2 `-sp` energy 8-dp identical (device ATM energy reassociates ~1e-12). Device ATM path confirmed active (trace) on both. GFN1 + CPU byte-unchanged; Vulkan `ctest -L gpu` 22/22, ROCm `-L native_xtb|gfn2_validation` 22/22.** Not a speed-up (once/geometry) — architectural residency. **Only the EEQ q-response ∂q/∂x remains host on every backend** (the last D4-gradient hold-out; needs a device dense solve — easy on ROCm via rocSOLVER, the hard piece on Vulkan). Extends [docs/SQM_GPU_ROADMAP.md](docs/SQM_GPU_ROADMAP.md) X-AP4. 🤖 AI-generated, ⚙️ machine-tested.
- **ROCm + Vulkan: 2-body GFN2 D4 nuclear gradient on device (June 17)**: the post-SCF 2-body D4 (energy + nuclear gradient + dE/dCN + dE/dq) now runs on the GPU in one per-atom gather — a superset of the in-SCF dEdq kernel, reusing the geometry-fixed reference data from beginDispersion. New kernels `k_d4_grad` (HIP) / `d4_grad.comp` (FP64 SPIR-V, 13 SSBO): direct radial gradient `dE_dr = -C6·ddisp_dr2·(R_i-R_j)` (ddisp_dr2 = s6·(-6r⁴t6²) + s8·r4r2·(-8r⁶t8²), the C6·ζc6 rescale cancels in the per-reference path), `dEdcn_i = -dc6dcn_i·disp_sum` (via the new `dWc`=∂W/∂CN flatten), `dEdq_i = -dc6dq_i·disp_sum`, per-atom energy (host: E = ½·Σ). Per-atom GATHER → no FP64 atomics (the pair force is antisymmetric, the energy is per-atom). New seam `GpuScfBackend::dispersionGradient`; host `calcDispersionEnergy` calls it behind `supportsDeviceDispersion()` (the 2-body energy/grad/dEdcn/dEdq), then **ATM (3-body) + the CN-distribution + the q-response (∂q/∂x) stay on the host** and sum on top — exactly as for the host D4Evaluator. Returns false → host fallback (CUDA keeps the host D4 gradient). buildRefWFlat gained a 3-output overload (W/dWq/dWc); the device uses the same converged charges (m_wfn.q_at == getZetaCharges) + CN (m_cn_values) the host path uses. **Validated (Radeon 890M / RADV + gfx1150): GFN2 `-opt` (triose) trajectory with Vulkan FP64 is BIT-IDENTICAL to CPU in every column incl. the sensitive Energy-Change (-23.315699) and gradient norm (0.144188, …) at every step; ROCm matches to 6 dp (its ~9e-9 Energy-Change delta is the pre-existing FP32 mixed-precision SCF, not the D4 math — the gradient norm still matches 6 dp). Device gradient path confirmed genuinely active (per-eval invocation trace, not a host fallback) on both backends. GFN2 `-sp` energy still 8-dp identical (device 2-body D4 energy reassociates ~1e-12, below display). GFN1 + CPU byte-unchanged; Vulkan `ctest -L gpu` 22/22, ROCm `-L native_xtb|gfn2_validation` 22/22.** **Honest: not a speed-up** (the gradient runs once per geometry, dwarfed by the SCF). It is architectural residency — the prerequisite for a fully device-resident GFN2 `-opt`/`-md`. **Still host on every backend (incl. CUDA): the D4 ATM 3-body gradient + the EEQ q-response ∂q/∂x** (the next staircase steps; ATM is a portable O(N³) gather, the q-response needs a device dense solve — easy on ROCm via rocSOLVER, the one genuinely-harder piece on Vulkan). Extends [docs/SQM_GPU_ROADMAP.md](docs/SQM_GPU_ROADMAP.md) X-AP4. 🤖 AI-generated, ⚙️ machine-tested.
- **ROCm + Vulkan: in-SCF GFN2 D4 dE_D4/dq on device (June 17)**: `supportsDeviceDispersion()`→true for both the HIP and Vulkan backends, so `XTB::addDispersionPotential`'s per-iteration D4 atom-potential (`computeD4PotentialDedq`) now runs the existing `xtb_native.cpp` device-dispersion path instead of the host O(N²) evaluator — closing the largest GFN2 cross-backend gap vs CUDA (which alone had it). ROCm: `__global__ k_d4_dedq` + `beginDispersion`/`dispersionDedq` in `rocm/xtb_hip_context.hip` (verbatim port of CUDA `k_d4_dedq`; per-atom GATHER, no atomics; element C6 block uploaded once/process, Z/nref/√r4r2/xyz per geometry, W/dWq per iteration). Vulkan: new FP64 SPIR-V `d4_dedq.comp` (9 SSBO + `uint nat` push; element/ref constants in-shader) + `begDispersion`/`dispDedq` in `xtb_vulkan_context.cpp` (host-mapped buffers, one chunked submit/iteration). Host plumbing was already wired (`xtb_native.cpp` `use_device_disp`), so only the three backend overrides + kernel were added; GFN1 (no in-SCF D4) and the CPU path are byte-unchanged. **Validated on a Radeon 890M (RADV + gfx1150): GFN2 `-sp` energy bit-identical to CPU over `sqm_reference` incl. the 231-atom `complex` (ROCm −329.52707823, Vulkan −329.52707823 = CPU, 8 dp); GFN2 `-opt` (NH3) converges to the same minimum; device path confirmed genuinely active (per-iteration invocation trace, not a silent host fallback); Vulkan `ctest -L gpu` 22/22.** **Honest: NOT a measured per-iteration speed-up at the tested sizes** — the threaded host D4 evaluator is only ~3.2 ms/it on `complex`/231, and the device round-trip (W/dWq up + kernel + dEdq down, sync/submit-bound on the iGPU) costs ~4.0 ms/it on both backends, so per-iter D4 is a wash-to-slight-regression here; the win grows with N (O(N²) host cost) and lands fully only once fused into a device-resident potential/loop (the CUDA Stage-5/6 path, still unported to ROCm/Vulkan). The 1 pre-existing failing test `gfnff_numgrad_fixed_charges` is unrelated (fails identically in the CPU-only `release/` build). Part of [docs/SQM_GPU_ROADMAP.md](docs/SQM_GPU_ROADMAP.md) device-dispersion. 🤖 AI-generated, ⚙️ machine-tested.
- **Vulkan eigensolver EIG-3: full-residency GPU divide-and-conquer tridiagonal solve (opt-in, June 17)**: implements the last open item of [docs/SQM_VULKAN_EIGENSOLVER_WP.md](docs/SQM_VULKAN_EIGENSOLVER_WP.md) — the Cuppen divide-and-conquer eigensolve (the host `tridiagDC`/`rank1Eigen` of `native_eigensolver.cpp`) ported entirely onto the GPU. Two new SPIR-V shaders: `dc_leaf_jacobi` (base-case n≤32 leaf eigensolve by cyclic Jacobi, one workgroup/leaf, algebraic rotation — GLSL has no FP64 trig — replacing the host `tql2` leaf) and `dc_merge_rank1` (the **whole** `rank1Eigen` for one merge node on one workgroup: lane-0 sequential bookkeeping = z-normalize/sign-fold/sort, deflation Givens scan, slot assignment; all-lanes parallel math = secular root bisection (shifted dlaed4), Löwner/Gu–Eisenstat weights, eigenvector columns, Givens-undo, perm scatter). `dcRecursive` dispatches GPU leaves + GPU merges; the blockdiag(V1,V2)·Wm back-transform reuses the EIG-2B `gemm_g`. `CURCUMA_VK_DC_HOST=1` keeps host deflation/tql2 (GPU secular only) for reference. **Correctness validated:** standalone `CURCUMA_VK_TEST_TRIDIAGDC=1` over all spectral classes (well-separated/clustered/exact-multiplicity/near-diagonal, incl. the dupdiag deflation path) to n=558 — residual ≤1.03e-12, ortho ≤8.4e-15; SCF **bit-identical** to host tql2 on 7 mol × {GFN1,GFN2} + 231-atom `complex` (16/16, 8 dp); NH3 GFN2 `-opt` identical trajectory; new `ctest -L gpu_vulkan_dc` 20/20. **Honest performance (the WP's predicted risk, now measured on the 890M/RADV iGPU): `complex` GFN2 Phase-2 ~382 ms vs ~29 ms host tql2 (~13× SLOWER)** — the host-deflation hybrid (`CURCUMA_VK_DC_HOST=1`) is ~130 ms (~4.5×). The single-workgroup sort/deflation bookkeeping + the bottom-up merge tree's sequential per-node fence latency dominate; the iGPU's FP64 can't overcome the submit overhead vs a zero-submit host tql2. Ships **opt-in only** (`CURCUMA_VK_TRIDIAG_SOLVE=dc`, default host tql2 unchanged), confirming the WP's "diminishing returns" call by measurement. Completes EIG-3. 🤖 AI-generated, ⚙️ machine-tested.
- **Vulkan eigensolver EIG-2B: WY-blocked GEMM back-transform (June 16)**: the Householder eigenvector back-transform (C = Q·Z = H_0·…·H_{n-3}·Z) now groups the n−3 per-reflector passes into panels of b=32 reflectors applied via the compact-WY form Q_panel = I − V·T·Vᵀ as three rectangular FP64 GEMMs (Y = Vᵀ·Z, Y2 = T·Y, Z −= V·Y2), cutting Z memory passes from ~n to ~3·(n/b) and reusing V tiles across all columns. Three new SPIR-V shaders: `tri_vfull` (scatter the compact panel reflectors to a full n×b layout, β=0 columns zeroed so degenerate reflectors drop out of T and V·T·Vᵀ), `wy_buildt` (build the b×b WY factor T from Gram(Vfull)+β by the LAPACK forward/columnwise recurrence in one workgroup), and `gemm_g` (a general rectangular M×N×K tiled GEMM with transA + subtract-accumulate — the existing `gemm.comp` is square-only). All panels record into one chunked submit (no host readback between them). Default ON; `CURCUMA_VK_BACKXF=reflector` reverts to the EIG-2A per-reflector path. **Fresh `complex`/231 GFN2 `-sp` (Radeon 890M/RADV): back-transform 27.7 → ~20 ms/iter (~1.4×)**, energies bit-identical to EIG-2A/CPU (gfn1+gfn2 over the sqm_reference set incl. the 231-atom `complex`, all dE = 0; standalone eigensolver vs Eigen resid 3.4e-14 / ortho 8.2e-15 to n=558; gfn2 `-opt` (NH3) identical; `ctest -L gpu` 2/2). Honest: the b=32 tall-skinny GEMMs are still occupancy-limited on the iGPU and tql2 noise swamps the *total*, so it is a real but modest isolated back-transform win. Completes [docs/SQM_VULKAN_EIGENSOLVER_WP.md](docs/SQM_VULKAN_EIGENSOLVER_WP.md) EIG-2B. 🤖 AI-generated, ⚙️ machine-tested.
- **ROCm R-AP3: GFN2 nuclear gradient incl. multipole-integral Pulay on device (June 16)**: `-gpu rocm -method gfn2` now computes the nuclear gradient on the AMD GPU (it joins the GFN1 repulsion/Pulay/Coulomb kernels), so the GFN2 device gradient is no longer a host fallback. New `__device__` helpers `d_mpg_dGdA`/`d_primitive_multipole_grad`/`d_cgto_multipole_grad_transformed` (verbatim port of the CUDA `.cuh` A-derivatives) in `rocm/xtb_hip_integrals.hiph`; `k_grad_h0_pulay` extended with the dp/qp-integral derivative term contracted against the converged `v_dp`/`v_qp` (column-major 3×nat / 6×nat, uploaded once per gradient to `dVdp`/`dVqp`); `XtbHipContext::gradient` + `HipScfBackend` thread `v_dp`/`v_qp`, and the GFN2 dispatch in `xtb_hip_method.cpp` runs the device gradient instead of returning false. Only the multipole SD/DD/SQ **interaction** gradient (§5) + dispersion + CN chain-rule stay on the host. Validated on a Radeon 890M (gfx1150): with `-scf_mixed_precision false` the gfn2 `-sp` gradient norm is bit-identical to CPU over the `sqm_reference` set incl. the 231-atom `complex` (reldiff = 0); the ROCm-routed finite-difference check (`test_xtb_gradient`) is byte-identical to the CPU path per molecule (proving device analytic == FD-validated CPU AP5 gradient); gfn2 `-opt` (triose) tracks the CPU step-by-step then diverges in the LBFGS tail exactly like the known-good GFN1 device gradient (rocSOLVER vs Eigen eigenvectors ~1e-13, amplified by LBFGS — not an R-AP3 error); `ctest -L native_xtb` 10/10. At the default FP32-mixed path the gradient differs ~1e-7 (the X-AP3 lever; use `-scf_threshold 1e-8` or `-scf_mixed_precision false`). Completes [docs/SQM_GPU_ROADMAP.md](docs/SQM_GPU_ROADMAP.md) R-AP3. 🤖 AI-generated, ⚙️ machine-tested.
- **Vulkan eigensolver EIG-4: FP32 mixed-precision tridiag eigensolve (June 16)**: opt-in `-scf_mixed_precision true` now uses an FP32 Householder eigensolve far from SCF convergence, reverting to the FP64 `solveSymTridiag` once `max|dq| < scf_fp32_threshold` (so the converged energy stays FP64). New `solveSymTridiag32`: casts Ã→FP32, runs FP32 `tri_house_f32`/`tri_matvec_f32`/`tri_kw_f32`/`tri_rank2_f32` (tridiagonalization) + `tri_applyl_f32` (back-transform) on FP32 mirror buffers (`tA32`…`tZ32`, same ploJ/ploT4 layouts), keeps the `tql2` in FP64 on the cast-to-double tridiagonal (host, cheap), casts eps/vectors back. **Honest, fresh same-clock (`complex`/231 GFN2 `-sp`, shared box): only the tridiagonalization benefits — 76→51 ms/it (1.5×, it is arithmetic/bandwidth-bound) — while the back-transform (28→26 ms) and host tql2 do NOT (latency/host-bound, the same lesson as the X-AP3 FP32 Jacobi). Net SCF ~3700→~3560 ms, total ~4.2→~4.0 s ≈ 5%, consistent across runs, energy bit-identical** to ROCm/CPU (10/10 sqm GFN1+GFN2). Kept **opt-in** (not defaulted, matching X-AP3): the gain is modest and the FP32 fixpoint can shift the gradient ~1e-7 at the loose default `scf_threshold` (use `-scf_threshold 1e-8` for tight gradients). Default FP64 path byte-unchanged (standalone n=558 residual 3.2e-14; `ctest -L gpu` green). Completes [docs/SQM_VULKAN_EIGENSOLVER_WP.md](docs/SQM_VULKAN_EIGENSOLVER_WP.md) EIG-4. 🤖 AI-generated, ⚙️ machine-tested.
- **Vulkan eigensolver EIG-1 + EIG-2A: fully-GPU tridiagonalization + workgroup back-transform (June 16)**: executes the first two items of [docs/SQM_VULKAN_EIGENSOLVER_WP.md](docs/SQM_VULKAN_EIGENSOLVER_WP.md) on the V-PERF-1 Householder eigensolver. **EIG-1** moves the whole per-column reduction onto the GPU — new `tri_house` (one workgroup: shared-memory ‖x‖² reduction → builds the Householder vector + β + diag/off into a combined `tScal` buffer `[diag|off|β]`) and `tri_kw` (reduces pᵀv + builds `w = β·praw − K·v`) kernels fold in the former host ‖x‖/pᵀv reductions, so `tri_house→tri_matvec→tri_kw→tri_rank2` for all columns record into chunked command buffers with the host out of the loop. The tridiagonalization host↔GPU fence syncs drop from **2·(n−2) = 1112 to 3** for the 558-AO `complex` (structural, clock-independent). **EIG-2A** rewrites `tri_applyl` (reflector back-transform) from one-thread-per-Z-column (serial length-m reduction) to **one workgroup (256 lanes) per column** with a shared-memory tree reduction of `vᵀZ` + parallel rank-1 update (β read from `tScal[2n+k]`); its submits likewise batch to 3. New `ploT4` (dsl4+12B push) for `tri_kw`. Standalone vs Eigen unchanged (n=558 residual 3.2e-14, ortho 8.7e-15, `CURCUMA_VK_TEST_TRIDIAG=1`); energies bit-identical to ROCm across the 12-mol sqm set, GFN1+GFN2 (20/20); `ctest -L gpu` green. Per-phase hook `CURCUMA_VK_EIG_PROFILE=1`. **Fresh same-clock (`complex` GFN2 `-sp`, shared machine so ±noise): tridiag eigensolve 161 ms/it (total 4.35 s) vs the cyclic Jacobi 3016 ms/it (61 s) = 18.7× and vs ROCm 66 ms/it (1.67 s) = 2.4×.** Remaining WP items: EIG-3 (tql2 29 ms host), EIG-4 (FP32 mixed). 🤖 AI-generated, ⚙️ machine-tested.
- **Vulkan GPU nuclear gradient: one-workgroup-per-atom — V-PERF-2 / ex-V-AP6 (June 15)**: `grad_pulay` (the H0-Pulay + on-site CN + GFN2 multipole-integral Pulay term) was the dominant cost once the eigensolver was fixed — `-sp` gradient on 231-atom `complex` was **18 s (GFN1) / 28 s (GFN2)** vs ROCm's 38 ms. Cause: with no FP64 atomics on RADV, the V-AP1/V-AP4 kernel used **one thread per atom** (a per-atom GATHER over all foreign AOs), so the chunked dispatch ran **1 active lane** doing the full O(nao·nprim²) inner sum serially (~78 ms/atom). Fixed by restructuring to **one workgroup (64 lanes) per atom**: the lanes split the inner `nu` (foreign-AO) loop, accumulate per-lane partials, then a shared-memory tree reduction writes each atom's force/`dEdcn` once (no atomics) — full GPU occupancy. The per-atom cost dropped ~64×, so the timeout chunk budget grew (`chunk·nao` 700→32000) and the dispatch is now `end-base` workgroups (not `/64`). Measured on a Radeon 890M, `complex` `-sp` gradient: **GFN1 18075→98 ms (185×), GFN2 28209→81 ms (347×)** — now beats the host gradient (642 ms) and within ~2× of ROCm. The GFN2 device gradient is therefore back ON by default (the V-PERF-1 host-gradient gate is replaced by an escape hatch `CURCUMA_VK_GFN2_CPUGRAD=1`). Correctness: `cli_gpu_gradient`/`cli_gpu_multipole` green; GFN1+GFN2 `-opt` (NH3, triose/66) converge to the same minimum as ROCm; the reduction reorders the sum so the gradient matches CPU to ~1e-13 (not bit-identical, same tolerance as the eigensolver). Net `complex` GFN2 `-sp` 9.4→8.7 s (the eigensolve 7 s now dominates); GFN1 25.8→7.8 s. 🤖 AI-generated, ⚙️ machine-tested.
- **Vulkan GPU eigensolver: Householder tridiagonalization replaces the cyclic Jacobi + device-lost crash fix — V-PERF-1 / ex-V-AP5 (June 15)**: the real reason `-gpu vulkan -method gfn2` was ~8× slower than ROCm on the 231-atom `complex` was a **silent CPU fallback**: the resident `begin()` failed with `VK_ERROR_DEVICE_LOST` because `jacobiDevice` recorded the whole sweep schedule (~44k dispatches for nao=558) into **one** command buffer / one submit, tripping the GPU command-timeout; `submit()` returned false → the SCF quietly ran on the CPU (reference BLAS, no MKL → 19.8 s) instead of crashing visibly. Worked ≤66 atoms, failed ≥231. **Two fixes:** (1) **chunked Jacobi submits** (`jacobiRoundsPerSubmit`, ~6e7 invocation budget/submit, calibrated against the known-good n=200 single-submit solve) so no single submit exceeds the timeout — the resident A/V/cs buffers persist across submits and each `submit()` fully drains the queue, so chunking at round boundaries is exact. (2) **a new GPU eigensolver** that replaces the O(sweeps·n³) two-sided Jacobi with a single O(n³) **Householder tridiagonalization**: GPU `tri_matvec` (p = A_sub·v) + `tri_rank2` (A_sub −= vwᵀ+wvᵀ) per column (host builds the length-n Householder vector + reductions directly on the mapped coherent buffers), host EISPACK `tql2` solves the n×n tridiagonal, then GPU `tri_applyl` applies the stored reflectors to the eigenvectors (Z −= βv·vᵀZ, batched). Default ON; `CURCUMA_VK_EIGEN=jacobi` reverts. Standalone vs the validated Jacobi (random symmetric, n≤558): max|Δλ| 3.6e-12, residual ‖Av−λv‖ 4e-14, orthonormality 3.6e-15 (`CURCUMA_VK_TEST_TRIDIAG=1`). Also **tiled the FP64 GEMM** (shared-memory, k-order preserved → bit-identical). Measured on a Radeon 890M, `complex` GFN2 `-sp`: **eigensolve 2629→367 ms/iter (7.2×)**, SCF 50.8→7.9 s, Löwdin setup 2.75→0.57 s; GFN1 SCF likewise. Energies bit-identical to ROCm/CPU across the sqm set (9 mol × gfn1/gfn2 = 18/18). To remove the `-sp` gradient regression the device-lost fix exposed (the V-AP4 GPU GFN2 gradient is ~28 s on this iGPU vs 0.6 s host vs 0.04 s ROCm), the **device GFN2 gradient is now opt-in** (`CURCUMA_VK_GFN2_GPUGRAD=1`, default OFF → validated host gradient, matching ROCm which keeps the GFN2 gradient on the host). Net `complex` GFN2 `-sp`: 82 s (correct GPU Jacobi) → **9.4 s** (8.7×); vs ROCm 2.0 s the residual gap is the eigensolve (367 vs 70 ms/iter — rocSOLVER `ssygvd`/`dsygvd` + FP32 mixed) + the host integral build. See [docs/SQM_VULKAN.md](docs/SQM_VULKAN.md) / [docs/SQM_GPU_ROADMAP.md](docs/SQM_GPU_ROADMAP.md). 🤖 AI-generated, ⚙️ machine-tested.
- **Vulkan on-device GFN2 nuclear gradient + chunked-dispatch timeout fix — V-AP4 (June 15)**: `-gpu vulkan -method gfn2` now computes the **GFN2 multipole-integral Pulay term** on the device, so the device `gradient()` returns true for GFN2 (was: false → full host fallback) and `-opt`/`-md` run the rep + Coulomb + H0-Pulay + **multipole-integral Pulay** + on-site CN on the GPU (host still adds §5 direct dp/qp interaction + CN chain-rule + D4). New SPIR-V helpers in `grad_pulay.comp` (`primitive_multipole_grad`/`cgto_multipole_grad_transformed` — the d/dA of the origin-at-ket traceless dipole/quadrupole integrals, a GLSL port of the CUDA `d_cgto_multipole_grad_transformed`); the per-atom GATHER computes both threads of each pair identically so the ±share cancels (no FP64 atomics). `v_dp`(3·nat)/`v_qp`(6·nat) flow in via grad_pulay bindings 24/25. **Correctness bug found during validation + fixed**: the GFN2 per-atom cost is O(nao²·nprim²), so a one-shot grad_pulay dispatch over a medium molecule exceeds the GPU command-timeout (~2 s) and is killed mid-flight — yet the fence still signals `VK_SUCCESS`, so the gradient comes back **silently truncated** (rep+Coulomb only, the Pulay/CN writes lost), giving a wrong gradient (66-atom triose: norm 0.30 vs 0.17, Σforce 0.17 ≠ 0) and a spurious Newton-3 violation; GFN1 was intermittently hit too (nao=200, right at the limit, flaky run-to-run, worse under the slower `-verbosity 2` path). Diagnosed by an early-vs-end sentinel write (kernel starts, never reaches the final store) + dispatch timing (triose GFN2 = 2030 ms, caffeine = 411 ms). Fix in `XtbVulkanContext::gradient`: split grad_pulay into atom **chunks** via new `base`/`end` push constants (`A = base + gl_GlobalInvocationID.x; if (A >= end) return;`), `chunk = clamp(700/nao, 1, nat)` so the worst-case all-heavy chunk stays ≈0.2 s with margin even when the GPU is throttled; chunks are disjoint (each atom written once) so the result is bit-identical. Validated on a Radeon 890M (RADV): gfn1+gfn2 `-sp` gradient norm bit-identical to CPU over the full 12-molecule `sqm_reference` set incl. the 231-atom `complex`, at **both** `-verbosity 0` and `2` (the throttled condition; triose GFN2 stable 6/6); per-atom gradient ≤1.4e-13 vs CPU; triose GFN2 `-opt` step-by-step identical to CPU (energy + gradient norm). `cli_gpu_gradient`/`cli_gpu_multipole` ctests green. **Honest: NOT a speed-up** — the device GFN2 gradient is ~10× the CPU on this iGPU (triose 4.1 vs 0.4 s/opt-step); residency/correctness milestone, not performance. See [docs/SQM_VULKAN.md](docs/SQM_VULKAN.md) / [docs/SQM_GPU_ROADMAP.md](docs/SQM_GPU_ROADMAP.md) V-AP4. 🤖 AI-generated, ⚙️ machine-tested.
- **FP32 mixed-precision eigensolve on ROCm + Vulkan — X-AP3 (June 15)**: the `-scf_mixed_precision` flag (FP32 eigensolve far from convergence, reverting to FP64 once `max|dq| < scf_fp32_threshold`; convergence never accepted on an FP32 step, so the converged energy is FP64) is now honored by the ROCm and Vulkan resident solves — both adapters previously did `(void)fp32` while only CUDA honored it. **ROCm = real win, defaulted ON for `-gpu rocm`** (matches CUDA): `residentSolve`/`solveMultipole` call `rocsolver_ssygvd` on FP32 copies of F/S (`k_d2f`/`k_f2d` element-wise casts; the resident overlap `dS0` stays FP64), eigenvectors/eigenvalues cast back to FP64. Measured on a Radeon 890M, `complex` (231 atoms): GFN1 1844→1281 ms (**1.44×**), GFN2 1924→1510 ms (**1.27×**) over the resident FP64 path (itself ~6-8× the CPU since rocSOLVER is mature); energies bit-identical to CPU across the sqm set (6 mol × 2 methods verified). Caveat: at the loose default `scf_threshold` (1e-5) the FP32 path reaches a slightly different fixed point, so the gradient norm differs ~1e-7 from CPU (`-scf_threshold 1e-8` restores bit-identical gradients; `-scf_mixed_precision false` disables). **Vulkan = no win, opt-in only (NOT defaulted on)**: FP32 variants of the two-sided Jacobi shaders (`angles_f32`/`col_f32`/`row_f32`/`vec_f32`) + FP32 work buffers (`rAtil32`/`rCtil32`/`rCs32`) are implemented and correct (energy matches CPU), but measured net-neutral to net-negative — the hand-written cyclic Jacobi is dispatch/barrier/bandwidth-bound, not FP64-arithmetic-bound, so FP32 is ~equal per iteration (`complex` GFN2 828 vs 844 ms/iter) and the perturbed early iterations can cost an extra SCF cycle (GFN1 14→15), making it slower. So `xtb_vulkan_method.cpp` deliberately does NOT call `setMixedPrecision(true)`; the FP32 path stays a documented opt-in (`-scf_mixed_precision true`). The real Vulkan lever is a faster eigensolve *algorithm*, not its precision. Vulkan gpu_gradient/gpu_multipole ctests green (default FP64 path unchanged). See [docs/SQM_ROCM.md](docs/SQM_ROCM.md) / [docs/SQM_VULKAN.md](docs/SQM_VULKAN.md) / [docs/SQM_GPU_ROADMAP.md](docs/SQM_GPU_ROADMAP.md) X-AP3. 🤖 AI-generated, ⚙️ machine-tested.
- **GFN2 device-resident multipole SCF — Vulkan V-AP3 + ROCm R-AP2, Stage 2b (June 15)**: `-gpu vulkan`/`-gpu rocm -method gfn2` now run the **device-resident** multipole SCF (`supportsMultipole()→true`) instead of falling back to the Stage-1 host eigensolver — the Fock build (incl. the anisotropic `F −= ½·Σ_k dp_int[k]·v_dp + qp_int[k]·v_qp` term), eigensolve, density, populations AND the atomic dp/qp moments all run on the device; only `v_dp`(3·nat)/`v_qp`(6·nat) up + eps/pop/`dp_at`/`qp_at` down per iteration (no nao² eigensolver transfer). Vulkan: `fock_multipole` (one thread per AO pair) + `multipole_moments` (per-atom GATHER, no FP64 atomics) SPIR-V kernels reusing the GFN1 resident Löwdin+Jacobi solve; ROCm: `k_add_fock_multipole` + `k_multipole_moments` (verbatim CUDA ports — HIP has `atomicAdd(double)`) + rocSOLVER `dsygvd`. The SCF loop was already method-agnostic (drove the CUDA resident path), so no loop change; dp_int/qp_int build moved into `computeIntegrals` (per geometry). **Gradient bug found+fixed**: enabling the resident path set `use_gpu_resident=true`, routing GFN2 through `calculateGradientGpu` → the GFN1-isotropic-only device `gradient()` (missing the multipole-integral Pulay) → wrong gradient (norm 5–60× off, energy still bit-identical since it is quadratic in dp_at). Fix: the device `gradient()` returns false for GFN2 (non-empty v_dp/v_qp) → falls back to the full host `calculateGradient` (V-AP4/R-AP3 device GFN2 gradient still pending). Validated on AMD 890M (RADV + gfx1150): gfn2 `-sp` energy bit-identical (8 dp, 12-mol incl. 231-atom `complex`) + `-sp` gradient norm matches CPU + `-opt` (NH3) step-by-step identical, both backends; GFN1 unaffected; ctests green. **Honest: NOT a speed-up on this iGPU** — the FP64 Jacobi eigensolve dominates (GFN1, already resident, is also ~1.3× the CPU on `complex`; the removed transfers are cheap shared-memory copies, not PCIe). Residency is the correct architecture + the prerequisite for the device GFN2 gradient; the iGPU speed lever is an **FP32 mixed-precision eigensolve** (`-scf_mixed_precision` is already plumbed and honored by CUDA; the Vulkan/ROCm `fp32` path is the missing piece — roadmap X-AP3). See [docs/SQM_VULKAN.md](docs/SQM_VULKAN.md) / [docs/SQM_ROCM.md](docs/SQM_ROCM.md) / [docs/SQM_GPU_ROADMAP.md](docs/SQM_GPU_ROADMAP.md). 🤖 AI-generated, ⚙️ machine-tested.
- **GFN2 multipole integrals on device — Vulkan V-AP2 + ROCm R-AP1, Stage 3m (June 15)**: `-gpu vulkan`/`-gpu rocm -method gfn2` now build the AO dipole(3)/quadrupole(6) integrals (`dp_int`/`qp_int`) **on the device** and download them, so the host GFN2 SCF skips its O(nao²) `setupMultipole` integral loop. Vulkan: new `multipole_ints` SPIR-V kernel (port of CUDA `cgto_multipole` + `k_multipole_ints` — global-origin dipole/quadrupole then the per-column origin shift + traceless transform with the resident overlap) + `gDpInt`/`gQpInt` buffers + `XtbVulkanContext::{beginMultipoleComputed,downloadMultipole}`. ROCm: `d_moment1d`/`d_type_to_cart`/`d_primitive_multipole`/`d_cgto_multipole` ported verbatim from the CUDA `.cuh` into `xtb_hip_integrals.hiph` (the `__device__` math compiles unchanged under hipcc) + the `k_multipole_ints` kernel + `XtbHipContext::{beginMultipoleComputed,downloadMultipoleInts}`. Shared host plumbing: new `GpuScfBackend::downloadMultipoleInts` hook + `setupMultipole(bool integrals_on_device)` skips the CPU integral build; wired in `xtb_native.cpp` only for `!supportsMultipole()` backends (CUDA's resident multipole loop builds AND consumes them on-device, so it is excluded and keeps the full host build). No new basis fields (Stage 3/4 already upload ao2sh/ao2at/iao_sh/ang_sh/prim_*). Since Vulkan/ROCm GFN2 run the host SCF (Stage-1 eigensolver), the device integrals feed the host Fock — **bit-identical GFN2 energy proves them**. Validated: gfn2 `-sp` energy bit-identical to CPU (8 dp) over the full 12-molecule `sqm_reference` set incl. the 231-atom `complex` on BOTH backends; gfn2 `-opt` (NH3) identical trajectory CPU/Vulkan/ROCm (integrals rebuilt each geometry, feeding the host Fock AND the host multipole gradient); GFN1 unaffected. New ctest `cli_gpu_multipole_01_vulkan_gfn2_multipole`. GFN2 resident multipole SCF (Stage 2b) + multipole gradient stay on the host/pending. See [docs/SQM_VULKAN.md](docs/SQM_VULKAN.md) / [docs/SQM_ROCM.md](docs/SQM_ROCM.md) / [docs/SQM_GPU_ROADMAP.md](docs/SQM_GPU_ROADMAP.md). 🤖 AI-generated, ⚙️ machine-tested.
- **Vulkan on-device GFN1 nuclear gradient, Stage 4 / V-AP1 (June 14)**: `-gpu vulkan -method gfn1` now computes the nuclear gradient on the device, so GFN1 `-opt`/`-md` are **fully device-resident** (integral build + SCF + gradient on the GPU; only the dispersion gradient + CN chain-rule on the host) — Vulkan now matches ROCm/CUDA on the whole GFN1 stack. **No FP64 atomics on Vulkan** (unlike CUDA/ROCm `atomicAdd(double)`), so the four scatter kernels are restructured as three **per-atom GATHER** SPIR-V shaders: `grad_rep` (repulsion, section 1), `grad_coulomb` (isotropic Coulomb, section 3), `grad_pulay` (on-site CN section 2a + H0/Pulay off-site section 2b with the inline Obara-Saika `cgto_overlap_grad`). One thread owns atom A's gradient slot and sums over all partners — exact because the pair forces are antisymmetric (`f_ij = −f_ji`) and every H0-Pulay scalar term is pair-symmetric, with the overlap derivative taken as `dS/dR_A` (A's shell first) and `dxij = x_A − x_foreign` carrying the sign (~2× the overlap-grad evals, acceptable on an iGPU). The energy-weighted density W = C·diag(2ε)·Cᵀ is built on device via the existing `scale_cols`+`gemm` from the resident rC (Jacobi order, `rd[ridx[k]] = 2ε[k]`). New `XtbVulkanContext::gradient` + gradient buffers/descriptor sets in `xtb_vulkan_context.cpp`; `VulkanScfBackend::{supportsGradient→true, gradient(...)}` over the resident density/MO coefficients (`pc_resident`, no P/C upload), passing ao2at/ao2sh/rep_alpha/rep_zeff through `XtbVulkanBasisData`. GFN2 keeps its gradient on the CPU (its SCF isn't device-resident on Vulkan, so `use_gpu_resident` is false → the device gradient is only ever taken for GFN1). Validated on a Radeon 890M (RADV): gfn1 `-sp` energy + gradient norm bit-identical to CPU over the full 12-molecule `sqm_reference` set incl. the 231-atom `complex`; `-opt` caffeine 35 steps step-by-step identical in energy AND gradient norm (a divergence-sensitive check). Added ctest `cli_gpu_gradient_01_vulkan_gfn1_gradient` (labels `gpu_gradient;gpu;vulkan;cli`, self-skips without a device). See [docs/SQM_VULKAN.md](docs/SQM_VULKAN.md) / [docs/SQM_GPU_ROADMAP.md](docs/SQM_GPU_ROADMAP.md) V-AP1. 🤖 AI-generated, ⚙️ machine-tested.
- **GPU backend roadmap doc (June 14)**: added [docs/SQM_GPU_ROADMAP.md](docs/SQM_GPU_ROADMAP.md) — a backend-vs-backend gap matrix (CUDA complete; ROCm trails on the GFN2 anisotropic stack + GFN-FF; Vulkan trails ROCm by one stage, no on-device gradient yet) and 13 proposed work packages for the remaining ROCm/Vulkan work: Vulkan GFN1 gradient (V-AP1), GFN2 multipole integrals/resident-SCF/gradient for both backends (V-AP2..4 / R-AP1..3, porting the CUDA `cgto_multipole`/`k_multipole_ints`/`solveMultipole`/`multipoleMoments`/`d_cgto_multipole_grad_transformed`), the optional Stage-5/6 fully-resident GFN2 loop (correctness-not-speed per CUDA), GFN-FF GPU, device in-SCF solvation (X-AP1), and validation-harness parity (X-AP2). Each AP names its CUDA port source, the files/kernels to touch, the bit-identical-energy + step-by-step-gradient-norm validation, and dependencies/ordering. Linked from SQM_VULKAN.md, SQM_ROCM.md and the top-level CLAUDE.md. Plan only — nothing implemented. 🤖 AI-authored plan.
- **Vulkan on-device integral build, Stage 3 (June 14)**: `-gpu vulkan` now builds CN, overlap S, bare Hamiltonian H0, the Löwdin X=S⁻¹ᐟ² and Coulomb gamma **on the GPU** via SPIR-V compute kernels (`cn`/`self_energy`/`overlap_h0`/`gamma` + the existing Jacobi/scale/gemm Löwdin chain), so the host skips its per-geometry integral build (no nao² upload). `beginBasis` uploads the flattened basis + element tables (covrad/pauling/atomic_rad) once into `XtbVulkanContext`; `beginComputed` runs the kernels per geometry, writing S/H0 into the resident buffers the SCF consumes; `downloadOverlap`/`H0`/`Gamma` feed the host bookkeeping and L=chol(S) is derived host-side (Eigen LLT — no GPU triangular solve). Wired through the existing `XTB::exportGpuBasis` → `beginBasis`/`beginComputed` seam (`XtbVulkanBasisData` POD bridge + `GpuScfBackend` hooks); active for both GFN1 (then resident SCF) and GFN2 (then Stage-1 eigensolver), s/p subset (GFN2 multipole integrals still CPU). Added a `-verbosity≥2` confirmation in the shared AP4 download path ("integrals built on GPU device ... host build skipped") and corrected the stale Vulkan init banner (it claimed "GFN1 SCF ... integrals on CPU (Stage 2)" even for `-method gfn2`; now method-aware, reports Stage 3). Validated on a Radeon 890M (RADV): SCF log confirms the device path for both methods; gfn1+gfn2 single-point over the full 12-molecule `sqm_reference` set incl. the 231-atom `complex` — all 24 bit-identical to CPU at 8 dp (|dE|=0); gfn1/gfn2 H2O opt bit-identical (-5.768775 / -5.070544 Eh). Only the nuclear gradient stays on the CPU (Stage 4 pending). See [docs/SQM_VULKAN.md](docs/SQM_VULKAN.md). 🤖 AI-generated, ⚙️ machine-tested.
- **ROCm on-device nuclear gradient, Stage 4 (June 14)**: `-gpu rocm -method gfn1` now computes the nuclear gradient on the device too, so GFN1 `-opt`/`-md` are **fully device-resident** (integral build + SCF + gradient on the GPU; only the dispersion gradient + CN chain-rule on the host). HIP `__global__` kernels `k_grad_repulsion` (section 1), `k_grad_cn_onsite` (2a, dEdcn diagonal), `k_grad_h0_pulay` (2b, the overlap-derivative Pulay term + the on/off-site CN coupling, using the energy-weighted density W = C·diag(2ε)·Cᵀ built via `k_scale_cols` + rocBLAS `dgemm`), `k_grad_coulomb` (3, γ-derivative); the Obara-Saika overlap derivative `d_cgto_overlap_grad` (s/p) is a verbatim port into `xtb_hip_integrals.hiph`. Wired through the existing `XTB::calculateGradientGpu` → `GpuScfBackend::{supportsGradient,gradient}` seam (reuses the resident dP/dC, no upload). GFN2 keeps its gradient on the host (no multipole gradient ported). Validated on a Radeon 890M (gfx1150): the *entire* gfn1 `-opt` trajectory matches the CPU step-by-step — identical energies AND gradient norms at every step (H2O 8 steps to -5.768775, benzoic acid to -27.417443) — which (a divergence-sensitive check) confirms the device gradient is correct; release/ 11/11. See [docs/SQM_ROCM.md](docs/SQM_ROCM.md). 🤖 AI-generated, ⚙️ machine-tested.
- **ROCm on-device integral build, Stage 3 (June 14)**: `-gpu rocm` now builds CN, overlap S, bare Hamiltonian H0, Cholesky L and Coulomb gamma **on the device** via HIP `__global__` kernels (`k_cn`/`k_self_energy`/`k_overlap_h0`/`k_gamma` + `rocsolver_dpotrf`), so the host skips its integral build (no nao^2 upload). The integral math (s/p contracted Gaussian overlap, the GFN1/GFN2 H0 shell-pair scaling, the Coulomb hardness average) is a verbatim port of the proven CUDA device helpers into `rocm/xtb_hip_integrals.hiph` (the `__device__` functions compile unchanged under hipcc). `beginBasis` uploads the flattened basis + element tables (covrad/pauling/atomic_rad) once; `beginComputed` runs the kernels per geometry, writing S/H0 into the resident buffers the SCF consumes; `downloadOverlap`/`H0`/`Cholesky`/`Gamma` feed the host bookkeeping. Wired through the existing `XTB::exportGpuBasis` → `beginBasis`/`beginComputed` seam (`XtbHipBasisData` POD bridge + the `GpuScfBackend` hooks). Used by both GFN1 (resident SCF) and GFN2 (then Stage-1 eigensolver). Validated on a Radeon 890M (gfx1150): SCF log "CN/S/H0/L built on GPU; no nao^2 upload", gfn1/gfn2 single-point + opt bit-identical to CPU (H2O, benzoic acid) — the bit-identical energy confirms the device integrals are elementwise-correct; release/ 11/11. Only the nuclear gradient stays on the CPU. See [docs/SQM_ROCM.md](docs/SQM_ROCM.md). 🤖 AI-generated, ⚙️ machine-tested.
- **ROCm device-resident GFN1 SCF, Stage 2 (June 14)**: `-gpu rocm -method gfn1` now runs the whole SCF on the AMD GPU via the `GpuScfBackend` seam (`begin`/`solve`/`density`/`finalize`) with **real hipcc-compiled `__global__` kernels**: `k_fock` (F = H0 − ½·S·(v⊕v)), `k_scale_cols`, `k_popband` (Mulliken populations + band), the density via rocBLAS `dgemm`, and the generalized eigensolve via rocSOLVER `dsygvd` (ascending → no host sort). H0/S stay resident; only `v_ao`/`occ` up and `eps`/`pop`/`band` down per iteration. Build pipeline (the crux): the `.hip` is compiled by **`hipcc -c` into a plain relocatable object** (CMake `add_custom_command`) and linked into curcuma_core with g++ as an `EXTERNAL_OBJECT`, so the device kernels build while the HIP-only `--offload-arch`/`--hip-link` flags stay off the GNU link (which would flip it to `ld.lld` and drop libgomp). `xtb_hip_context.cpp → .hip`; `HipScfBackend` adapter + `setGpuScfBackend` in the wrapper. Validated on a Radeon 890M (gfx1150): gfn1 single-point + opt bit-identical to CPU (H2O, benzoic acid); gfn2 unchanged (Stage-1 rocSOLVER); release/ 11/11. Integrals/gradient still CPU. See [docs/SQM_ROCM.md](docs/SQM_ROCM.md). 🤖 AI-generated, ⚙️ machine-tested.
- **ROCm GPU eigensolver, Stage 0+1 (June 14)**: `-gpu rocm` now runs the native GFN1/GFN2 per-iteration eigensolve on an AMD GPU via **rocSOLVER `dsygvd`** (the generalized `F C = S C ε` solved directly on the device through `XTB::setExternalEigensolver`; host builds `S = L·Lᵀ`). Works for GFN1 and GFN2 (the host hands rocSOLVER the complete Fock); integrals/Fock/gradient stay on the CPU (Stage 2-4 pending). The whole path is **host-callable** — device memory via `hipMalloc`/`hipMemcpy`, rocSOLVER's own kernels — so `xtb_hip_context` compiles as plain C++ against `<hip/hip_runtime_api.h>`/`<rocsolver/rocsolver.h>` (no `amdclang`, no `--offload-arch`, no HIP-language link flip that would drop GNU OpenMP). Verified on a Radeon 890M (gfx1150): gfn1/gfn2 single-point + opt bit-identical to CPU (H2O, benzoic acid). CMake: `find_library(amdhip64)` always; rocBLAS/rocSOLVER optional (`HAVE_ROCSOLVER` per-source so enabling it doesn't recompile all of curcuma_core). `xtb_hip_context.hip → .cpp`. GPU build dependencies documented in docs/SQM_ROCM.md + docs/SQM_VULKAN.md + README. See [docs/SQM_ROCM.md](docs/SQM_ROCM.md). 🤖 AI-generated, ⚙️ machine-tested.
- **Vulkan device-resident GFN1 SCF, Stage 2 (June 13)**: `-gpu vulkan -method gfn1` now runs the whole SCF on the GPU via the existing `GpuScfBackend` seam (`begin`/`solve`/`density`/`finalize`) — Fock build, density, Mulliken populations and band energy join the eigensolve on-device; H0/S stay resident, only `v_ao`/`occ` up and `eps`/`pop`/`band` down per iteration. The generalized problem is reduced with a device-built **Löwdin X=S⁻¹ᐟ²** (Jacobi(S)+scale+gemm) instead of the host Cholesky, so `XᵀFX` is GEMMs (no GPU triangular solve). New FP64 shaders `gemm`/`scale_cols`/`fock`/`popband` (embedded SPIR-V); resident buffers + pipelines cached by basis size in `XtbVulkanContext`; `VulkanScfBackend` adapter + `setGpuScfBackend` in the wrapper (supportsMultipole=false → GFN2 stays on the Stage-1 eigensolver). Validated on AMD 890M/RADV: standalone generalized solve vs Eigen ~1e-14; gfn1 single-point + opt bit-identical to CPU (H2O -5.768775, benzoic acid -27.41691877); gfn2 unchanged; `release/` cli tests 11/11. Integrals/gradient still CPU (Stage 3-4 pending). See [docs/SQM_VULKAN.md](docs/SQM_VULKAN.md). 🤖 AI-generated, ⚙️ machine-tested.
- **Vulkan GPU eigensolver, Stage 1 (June 13)**: `-gpu vulkan` now runs the native GFN1/GFN2 SCF with the per-iteration dense symmetric eigensolve on the GPU — a hand-written FP64 two-sided cyclic Jacobi in SPIR-V compute shaders (`vulkan/shaders/{angles,col,row,vec}.comp`, embedded via `spirv_kernels.h`), with round-robin parallel rotation sets. The generalized problem (F, S=L·Lᵀ) reduces to standard form on the host (Cholesky) and back-transforms; only the eigensolve (the SCF hot path) is on-device. Wired through the existing `XTB::setExternalEigensolver` seam in `XtbVulkanComputationalMethod`; `XtbVulkanContext::solveSymmetric` owns the pipelines + buffer cache. Validated on an AMD Radeon 890M (RADV): standalone vs Eigen n=4..128 (~1e-13 eigenvalues, ~1e-14 reconstruction); gfn1/gfn2 single point H2O + benzoic acid bit-identical to CPU; gfn2 -opt H2O converges to the same minimum (-5.070544 Eh). Reusable validation harness in `vulkan/prototype/`. Honest: correctness milestone on an iGPU (FP64 slow, large systems untested); integrals/Fock/gradient still CPU. See [docs/SQM_VULKAN.md](docs/SQM_VULKAN.md). 🤖 AI-generated, ⚙️ machine-tested.
- **Vulkan + ROCm GPU backends, Stage 0 (June 13)**: added `-gpu rocm` (AMD/HIP) and `-gpu vulkan` (hand-written SPIR-V compute) alongside the existing `-gpu cuda`, for gfn1/gfn2/gfnff. This commit is the build + dispatch scaffold: CMake `USE_ROCM`/`USE_ROCM_XTB` + `USE_VULKAN`/`USE_VULKAN_XTB` option blocks (mirroring `USE_CUDA`), generalised `method_factory.cpp` `resolveNativeXtbGpuMode()` (cuda|rocm|vulkan|auto, fallback warnings) + per-backend GFN1/GFN2 dispatch, and Stage-0 device-handshake engines that fall back to the validated CPU pipeline (`rocm/xtb_hip_context.{h,hip}` + `xtb_hip_method`, `vulkan/{vk_context,xtb_vulkan_context}` + `xtb_vulkan_method`). The xTB SCF stays backend-neutral via the existing `GpuScfBackend` seam (any hook false -> CPU), so later stages port the kernels stage-by-stage. Two FP64 GLSL shaders (`gemm_f64`, `jacobi_apply_f64`) compile to SPIR-V. Verified: default `release/` build green + 11/11 ctest subset, `-gpu rocm|vulkan|foobar` warn+CPU-fallback at runtime, all host TUs compile against the project flags (vk_context against the real vulkan.h). Not yet: on-device numerics; the `.hip` TU is compile-pending `hipcc`. See [docs/SQM_ROCM.md](docs/SQM_ROCM.md) / [docs/SQM_VULKAN.md](docs/SQM_VULKAN.md). 🤖 AI-generated, Stage 0 only.
- **ConfScan verbosity scheme + output ordering (June 12)**: redesigned what shows at each level. v1: configuration text block (threshold steps, permutation method, "Current Configuration") - printed in the constructor, i.e. BEFORE the descriptor pass, fixing the "table only appears after the long ripser computation" complaint; a "Reading ensemble and computing descriptors (...)" line before that pass and "Read N structures (descriptors: Xs)" after; the per-pass adjusted thresholds; the per-pass summaries; and the conformer-statistics SUMMARY (Energy span / Total states / Free Energy / Entropy). v2: + per-structure Accept/Reject + detailed status + the per-conformer ConfStat table. v3: + which reference structure each rejection matched and the deciding RMSD. The confscan param_comparison_table was dropped (user chose the text block). Switched the relevant prints from info/param (>=2) to result/result_fmt (>=1) with global-verbosity sync where the RMSD machinery lowers it mid-scan. Also de-UTF'd the ConfStat header/table (=== and dE). 🤖 AI-generated, ⚙️ machine-tested.
- **Clean startup / config output globally (June 12)**: removed the noise that leaked on every run: (1) an ungated `[ConfigManager] Defaults for 'X': {full json}` stderr dump (config_manager.cpp:164 - now behind `#ifdef DEBUG_CONFIG_MANAGER` like its siblings); (2) the block of `Warning: Parameter 'X' has different types in modules ...` developer diagnostics (parameter_registry.cpp validateRegistry - gated behind `#ifdef DEBUG_PARAMETER_REGISTRY`; never affected validity); (3) `Opening file ...` (fileiterator.cpp - routed through `CurcumaLogger::info_fmt`, verbosity >=2). Also reworked `CurcumaLogger::param_comparison_table` (used by all commands): it now skips nested object/array entries (the module/`global`/`rmsd` sub-configs that rendered as ugly `{N keys}`) and left-aligns the key column. 🤖 AI-generated, ⚙️ machine-tested.
- **ConfScan progress bar + clean per-pass summary (June 12)**: at verbosity 1 each pass now shows a single live ASCII progress bar instead of the per-structure `Accept #N`/`Reject #N` + status-block spam; between passes a clean aligned summary (Accepted/Rejected/Reordered+successful+reused/Reorder skipped/Processed). Per-structure detail moved to verbosity >=2. New reusable `CurcumaLogger::progress_bar()`/`progress_done()` + global `m_progress_enabled`; on by default, disablable per run (`-confscan.progress false`) and globally (`-noprogress`), and auto-off when stdout is not a TTY (`isatty`, set in main.cpp) so redirected logs/tests carry no carriage-return noise. Also fixed a pre-existing bug where the reuse pass wrote a `PrintStatus` call between `result_file.open/close` instead of the `Results of Reuse Pass` statistic-file header. 7/7 CLI tests still pass. 🤖 AI-generated, ⚙️ machine-tested.
- **ConfScan CLI no-output regression FIXED (June 12)**: `curcuma -confscan file.xyz` read 0 molecules and produced nothing ("0 structures ... of 0 total"). Root cause: commit 4529895 (BMT) replaced `scan->setFileName(argv[2])` with `initializeBMT(...)` in `executeConfScan`; `initializeBMT` calls the base `setFile()` but never `openFile()`, which is what loads the ensemble (only `setFileName()` calls it). Fix: restore `scan->setFileName(argv[2])` after `initializeBMT`, plus a defensive `if (m_molecules.empty()) openFile();` backstop in `ConfScan::start()`. Also: `SetUp()` now derives `m_result_basename` from `Basename()` (was `Filename()` minus 4 chars, which kept a `./` prefix); confscan announces its output directory at verbosity 1 and auto-`-bak`s `accepted/rejected.xyz` to the CWD so a plain run is never silent. The bug was invisible because the C++ `confscan_test` exercises the `setFileName()` path, which the CLI does not use. 🤖 AI-generated, ⚙️ machine-tested.
- **ConfScan CLI tests hardened — were false positives (June 12)**: all 7 `cli_confscan` scenarios ran the identical command and asserted the identical copy-pasted "14 accepted / 30 rejected", validated via `find_output_file` that matched committed stale outputs; the comprehensive C++ test path differs from the CLI path so neither caught the no-output bug. Now: removed 128 committed generated output files from `test_cases/cli/confscan/0*` (kept only `conformers.xyz` + `run_test.sh`); each scenario exercises real functionality (default/get_rmsd/unknown-method-fallback/sLX/element-template/heavy-only/restart) with its OWN measured goldens incl. the full `<acc> <reorder> <reuse> <skipped>` stdout fingerprint; added a `of 44 total` regression guard, strict exit-code + non-empty checks; `clean_dir_keep` + `extract_confscan_summary` helpers in `test_utils.sh`; removed the stale duplicate `cli/confscan/test_utils.sh`. 🤖 AI-generated, ⚙️ machine-tested.
- **RMSD unknown-method fallback → subspace (June 12)**: `RMSDDriver::LoadAlignmentMethodParameters` defaulted an unrecognized `-rmsd.method` to legacy `incr`, which is so slow on larger ensembles it looked like a hang. Now defaults to the recommended `subspace`. 🤖 AI-generated.
- **Known concurrency issue (June 12)**: the ConfScan threaded reorder path (`threads>1`) can intermittently stall/deadlock - observed with heavy-atom-only (`-rmsd.protons false`), the restart-resume double-run, and (rarely, under load) a plain reorder pass; at both high and low CPU load. Not fixed; all 7 CLI confscan tests pin `threads=1` for determinism (the C++ `confscan_test` scenarios still exercise the multi-threaded path). ⚠️ open. 🤖 AI-found.
- **Known geometry-truncation bug (June 12)**: `ConfScan::Reorder` applies `mol1->ApplyReorderRule(t->ReorderRule())` (`confscan.cpp:1439`) with a rule whose length is the *compared* atom set, not the full molecule. In heavy-only mode the rule covers only heavy atoms, so reordered structures are written with the heavy-atom count (e.g. 55 instead of 114); in `get_rmsd` mode the rule is empty, so reordered/rejected structures are written with 0 atoms. The filtering counts (accepted/rejected) are correct, but the written geometry of the reordered structures is reduced. Surfaced by the hardened CLI tests (06 heavy, 02 get_rmsd). Not fixed (needs the reorder rule to map all atoms or `ApplyReorderRule` to preserve unmapped atoms). ⚠️ open. The test counter (`count_xyz_structures`) was made robust to variable/zero atom counts so it still reports the correct logical structure count. 🤖 AI-found.
- **RMSD JSON output file (June 10)**: `executeRMSD()` in `main.cpp` now writes `<target>.rmsd.json` containing `rmsd`, `rmsd_raw`, `permutation` (identity when no reordering), `reference_xyz`, `reorder_xyz`, `reference_file`, `target_file`. Always generated (even in no-reorder mode). When `TargetReorderd()` is empty (atoms matched, reordering skipped), falls back to `TargetAligned()`. Cleanup in `test_utils.sh` includes `*.rmsd.json`. Tests 01 and 02 verify JSON file existence. 🤖 AI-generated.
- **ConfScan pass output visible at verbosity 1 (June 9)**: Initial/Reorder/Reuse pass start/finish messages and the final "structures were kept" summary were swallowed at verbosity 1 because `CurcumaLogger::success()` checks the global logger verbosity, which the RMSD machinery lowers to 0 mid-scan. Added `ConfScan::logPass()` helper that temporarily syncs the global verbosity to `m_verbosity` (same workaround as `AcceptMolecule`/`RejectMolecule`); routed all pass messages through it and wrapped the end-of-scan summary in `Finalise()`. 13/13 confscan+rmsd CLI tests pass. 🤖 AI-generated.
- **RMSD dtemplate crash fix (June 9)**: `DistanceTemplateStrategy::align` decremented the distance-map iterators without a lower bound; with the new default `limit=10`, molecules offering fewer than 10 matchable template atoms underflowed past `cbegin()` (UB/crash). Capped the effective limit to the available atom count, guarded the iterators against reaching `cbegin()`, and fail cleanly on an empty template. Mirrored in the unused legacy `RMSDDriver::PrepareDistanceTemplate`. Verified: 6-atom methanol now runs (RMSD 0), 90-atom case unaffected (RMSD 2.87). 🤖 AI-generated.
- **BMT output directory system**: All commands default to creating a `Basename.Keyword.YYYYMMDD_HHMMSS/` directory for output. `-bak` flag copies specified files back to CWD. `-no_bmt` disables BMT (legacy CWD output). `BMTUtils` namespace provides `createBMTDir`, `writeMetadata`, `processBakFiles`, `outputPath`, `stripExtension`, `collectBakFiles`. `CurcumaMethod` integration via `createBMTDir()`, `addBakFile()`, `processBakFiles()`. 🤖 AI-generated.
- **SQM: GFN-FF `-gfnff.solvent_model gbsa` now maps to ALPB (no separate GFN-FF GBSA) (2026-06-08, Branch `feature/sqm-implementation`)**: The reference is explicit — `gfnff_alpb.F90:23-24,66`: "The GBSA for GFN-FF technically does not exist. We are using the improved ALPB model instead." (`gfnff_gbsa_init` hardcodes `alpb=.true., kernel=p16` regardless of the request). curcuma's GBSA path was a Still-kernel + ALPB-param mix that is *neither* model and sat ~1-3 mEh off xtb. Changed `GFNFF::InitialiseMolecule` to always build the ALPB model (`setUseAlpb(true)`) and map a `gbsa` request to ALPB with a one-line warning. Now `-gfnff.solvent_model gbsa` == `alpb` == xtb 6.7.1 `--gfnff --alpb` to ≤1e-8 Eh (H2O/caffeine verified). Docs (CLAUDE.md, ff_methods/CLAUDE.md, SOLVATION.md, SQM_SOLVATION_WP.md) corrected — the prior "GBSA approximate" claim is gone. 🤖 AI-generated, ⚙️ machine-tested only.
- **SQM: GFN-FF self-consistent ALPB solvation — WP5 "the riddle" (2026-06-07, Branch `feature/sqm-implementation`)**: Made native GFN-FF implicit solvation self-consistent and validated it against xtb. The previous WP5a code was a **frozen-charge post-hoc add-on** — it evaluated `ALPBSolvation::getEnergyParts` at the *gas* EEQ charges (`m_charges`), so the solvent never polarized the charges (total = gas + Gsolv exactly) and the energy was unvalidated. Decoding the reference (`/home/conrad/src/curcuma/external/gfnff/src/gfnff_engrad.F90`) showed the coupling is a one-line addition to the *linear* EEQ solve: `goed_gfnff` does `A(:n,:n) += gbsa%bornMat` (line 1346-1350) before solving, the EEQ energy `es` excludes the Born term, and the gradient uses the solved charges frozen ("without dq/dr", :308). Implemented exactly that: `EEQSolver::setReactionField(const Eigen::MatrixXd*)` + member, injected as `A.topLeftCorner(n,n) += *m_reaction_field` right before `dispatchSolve` in `calculateFinalCharges`; `ALPBSolvation::bornMatrix()` accessor (the curcuma Born matrix = reference `addBornMatrix`: P16/Still + self + `2·hbw` HB diagonal + `keps·alpbet/aDet` shape); `GFNFF::prepareCNAndEEQ` builds the Born matrix at the current geometry (`m_solvation->update`) and hands it to the solver, and forces the Phase-2 re-solve when solvated (`eeq_charges_current && !m_solvation`) because the gas topology/init solve doesn't include the field. Result: the EEQ charges polarize, the GFN-FF Coulomb term uses them, and gsolv is added separately (no double count). **Validated: native gfnff+ALPB total = xtb 6.7.1 (`--gfnff --alpb SOLVENT`) to ≤1e-8 Eh** for 7 mol × {water,dmso,acetone,chloroform} (`ctest -L gfnff_solvation`, 28/28; refs committed in `reference_data/gfnff_alpb_xtb.ref.json`, no xtb at test time); analytic gradient FD-validated (`gfnff_numgrad_alpb_water`, frozen-charge at solvated q like the reference, ≤5e-3 Eh/Bohr). The exact gas AND water match confirms the gfnff ALPB params are correct. Gas-phase GFN-FF byte-identical; native gfn1/gfn2 (WP1) untouched (108/108 combined solvation+validation ctests green). **GBSA** (`-gfnff.solvent_model gbsa`) stays **approximate** (~1-3 mEh vs xtb): it reuses the ALPB param set with the Still kernel; the dedicated gfnff GBSA params (`gfn1_/gfn2_<solvent>` in the reference) are not yet extracted — now warns at runtime. Source clone failed (no network); read the reference in place. Docs: [docs/SOLVATION.md](docs/SOLVATION.md), [docs/SQM_SOLVATION_WP.md](docs/SQM_SOLVATION_WP.md). 🤖 AI-generated, ⚙️ machine-tested only — human production testing pending (other solvents/ions/metals/large systems/MD, GBSA).
- **SQM: GFN-FF implicit-solvation routing fix + model select — WP5a (2026-06-07, Branch `feature/sqm-implementation`)**: `-method gfnff -gfnff.solvent water` now actually applies solvation; it was **silently ignored** because the native `gfnff` PARAM module declared no `solvent` parameter, so the CLI value never reached `GFNFF::InitialiseMolecule` (`m_parameters["solvent"]` stayed the built-in `"none"`). Registered `solvent` + `solvent_model` PARAMs in `gfnff.h` (`solvent_model`: `alpb` default / `gbsa`, numeric 2/3 accepted) and wired `solvent_model` into the ALPB-model creation (`ALPBSolvation::setUseAlpb` → Still kernel + `alpbet=0` for gbsa); fixed the hard-coded "ALPB" log label (now reflects the model). Energy changes sensibly (H2O gfnff: gas −0.32726266 → water ALPB −0.33981779, GBSA −0.33920019). `test_gfnff_numgrad` gained `--solvent NAME [--solvent-model gbsa]`. **Honest status (no green solvent ctest added — would mislead):** (1) **energy unvalidated** — tblite has no GFN-FF ALPB reference and native gfnff is not bit-exact to Fortran xtb even in gas phase, so there is no 1e-8 target; (2) the **gradient inherits GFN-FF's documented frozen-charge approximation** (no dq/dr): tight for non-polar at full-EEQ (CH4 7.1e-5, C6H6 1.1e-4 Eh/Bohr) but the solvent amplifies the polar charge-response error (H2O gas 2.4e-3 → water 2.4e-2 Eh/Bohr), so solvated GFN-FF opt/MD on polar systems is approximate — the solvation gradient *formula* itself is consistent (non-polar matches FD). The proper fix (WP5b, deferred, no reference) is to couple the Born potential into the EEQ solve so the charges feel the solvent. Gas-phase GFN-FF unchanged (`gfnff_numgrad_builtin` 1/1; the pre-existing `gfnff_numgrad_fixed_charges` / `cli_gfnff_01/02` failures are unrelated). Docs: [docs/SQM_SOLVATION_WP.md](docs/SQM_SOLVATION_WP.md), [docs/GFNFF_STATUS.md](docs/GFNFF_STATUS.md). 🤖 AI-generated, ⚙️ machine-tested only.
- **SQM: device-resident GFN2+solvent on GPU — WP4b (2026-06-07, Branch `feature/sqm-implementation`)**: Builds the implicit-solvation reaction field `v_at += B·q_at` on the device inside the GFN2 device-potential / Stage-6 resident loop, so GFN2+solvent uses the fully device-resident SCF instead of the WP4a host fallback. The whole-potential builder `XtbGpuContext::buildDevicePotentialAndSolve` (shared by `residentScfStep` and `solveDevicePotential`) gains a cuBLAS dgemv `dVat += dSolvB · dQat` after the D4 add and before the AO expand, using the already-resident input-derived `dQat`. New backend seam `GpuScfBackend::{supportsDeviceSolvation(),beginSolvation(nat,born_mat)}` (default false), implemented by `XtbGpuScfBackend`→`XtbGpuContext::beginSolvation` which uploads the symmetric, keps-scaled nat×nat Born matrix `m_born_mat` once per geometry into `dSolvB` (`solv_active` reset in `beginPotential`, set here). `ImplicitSolvationModel::deviceBornMatrix()` (new, default nullptr) returns `m_born_mat.data()` only for the GFN2 Mulliken path and nullptr for GFN1 CM5 (`q_solute=q_at+cm5` can't be rebuilt from the device's plain `q_at`, so GFN1 keeps the host-driven loop). **No device energy change** — the host already recomputes the total from the downloaded converged charges (`energySolvation()` + post-SCF `addSolvationPotential` rebuild); only the in-SCF charges needed the field. The WP4a gate now blocks the device path only when the device can't take solvation (CM5 or no `supportsDeviceSolvation`). Verbosity-2 logs `…+solvation) built on the device`. **Validation**: GFN2+solvent+GPU bit-matches CPU to 0.0 µEh (H2O/C6H6/acetic_acid_dimer/caffeine, ALPB+GBSA); `ctest -L gpu_*_solvation|gpu_*_gbsa` 28/28, gpu component (`gpu_scf|gpu_integrals|gpu_gradient`) 121/121, gas-phase `gpu_gfn{1,2}_validation` 24/24, CPU suite (`_solvation|_gbsa` 112, numgrad 1, gfn{1,2}_validation 24) unchanged, both CUDA + non-CUDA `make` green. Honest: residency/correctness, **not** a measured `-sp` speedup (eigensolve-bound, per the Stage-6 note). Docs: [docs/SQM_GPU.md](docs/SQM_GPU.md), [docs/SQM_SOLVATION_WP.md](docs/SQM_SOLVATION_WP.md). 🤖 AI-generated, ⚙️ machine-tested only.
- **SQM: fix GFN2+solvent on GPU (in-SCF reaction field dropped) — WP4a (2026-06-07, Branch `feature/sqm-implementation`)**: GFN1+solvent on the CUDA path was already correct (the host-driven loop folds `addSolvationPotential` into the per-iteration `v_ao` upload), but **GFN2+solvent+GPU was wrong by up to 3.8 mEh** (water ALPB) / 1.8 mEh (GBSA). The GFN2 device-potential / fully-resident loop builds the per-iteration potential entirely on-device and silently omitted the implicit-solvation reaction field `v_at += B·q_at`, so the SCF converged to the gas-phase fixpoint and the post-SCF `energySolvation()` was then evaluated at unpolarized charges (missing the self-consistent polarization). Measured CPU-vs-GPU on the same `release_cuda` binary: gfn1 0.0 mEh (both models), gfn2 ALPB 3.81 / GBSA 1.84 mEh. Fix: gate the device-potential path off when `m_solvation` is active (`xtb_native.cpp:~722`, added `&& !m_solvation`), so GFN2+solvent falls back to the host-driven device loop — the host builds `m_pot` incl. the reaction field and uploads `v_ao`, keeping it in-SCF, with the eigensolve still device-resident. Now bit-identical to CPU (0.000 µEh, H2O/CH4/NH3/HCN/C6H6/acetic_acid_dimer/caffeine). New `ctest -L gpu_*_solvation` / `gpu_*_gbsa` (28: water × 7 mol × {gfn1,gfn2} × {alpb,gbsa}); gpu component suite (`gpu_scf|gpu_integrals|gpu_gradient`) 121/121, gas-phase `gpu_gfn{1,2}_validation` 24/24 unchanged, full CUDA `make all` green. WP4b (add the Born potential to the device build so GFN2+solvent runs fully device-resident — residency, not a measured speedup) deferred. Docs: [docs/SQM_SOLVATION_WP.md](docs/SQM_SOLVATION_WP.md). 🤖 AI-generated, ⚙️ machine-tested only.
- **SQM: self-consistent GBSA implicit solvation for native GFN1/GFN2 — WP2 (2026-06-07, Branch `feature/sqm-implementation`)**: Adds `-xtb.solvent_model 2` (GBSA) alongside the existing ALPB (model 3). GBSA = ALPB with the analytical-linearized-PB shape term off (`alpbet=0` ⟹ `keps=1/ε−1`, and the constant `keps·alpbet/adet` term drops) **and** the classical **Still** Born kernel instead of P16 — exactly tblite's distinction (`api/solvation.f90:191` selects `born_kernel%still` for the GBSA versions 21/22 vs P16 for ALPB 11/12). `ALPBSolvation::setUseAlpb(bool)` branches: the param load (`getTbliteSolvationParam(method, alpb=false, solvent)` → the gbsa entries already in `tblite_solvation_params.h`), `buildBornMatrix` (new Still off-diagonal `fgb²=r²+aa·exp(−r²/4aa)`), and a new `addGradientStill` (tblite `add_born_deriv_still`); CDS surface-tension/H-bond, SASA, state shift and CM5-for-gfn1 are shared with ALPB unchanged. `xtb_native` maps `solvent_model==2`→`setUseAlpb(false)`. **Bug fixed en route**: `applyXtbScfConfig` parsed `solvent_model` only via `is_number_integer()`, but the CLI routes the flat flag as a **float** (`2.0`) — so the value was silently dropped and the model defaulted to ALPB regardless (the WP1 ALPB tests passed only because the default is 3, i.e. `-xtb.solvent_model 3` had always been a no-op); now accepts any number or numeric string (solvent_epsilon too). Reference harness: `dump_tblite_reference --model gbsa` (tblite version 21/22, same `tblite_new_alpb_solvation_solvent` constructor, no patched tblite needed), `validate_sqm.py` reads `solvent_model` from the ref and passes `-xtb.solvent_model 2/3`. **Validation**: 56 committed gbsa refs ≤1e-8 Eh vs tblite (`ctest -L _gbsa` = 28 gfn1 + 28 gfn2); analytic gradient FD-validated 16/16 (alpb+gbsa, gfn1 ~1e-8 / gfn2 ~1e-4 D4-response floor, `ctest -R xtb_solvation_numgrad`); no regression (ALPB `ctest -L _solvation` 56/56, gas-phase gfn{1,2}_validation 24/24, native_xtb/xtb_gradient/xtb_cpscf/d4_dedq/scf_extrapolation 11/11, full `make all` green). Docs: [docs/SOLVATION.md](docs/SOLVATION.md), [docs/SQM_SOLVATION_WP.md](docs/SQM_SOLVATION_WP.md). 🤖 AI-generated, ⚙️ machine-tested only — human production testing pending. Remaining: CPCM (WP3), GPU residency (WP4), GFN-FF (WP5).
- **SQM: self-consistent ALPB implicit solvation for native GFN1/GFN2 (2026-06-07, Branch `feature/sqm-implementation`)**: Adds a solvent reaction field to the native xTB SCF, matching tblite's full ALPB ΔG (Born electrostatics + CDS surface-tension/H-bond + state shift). New `ImplicitSolvationModel` interface (`src/core/solvation/implicit_solvation.h`); `ALPBSolvation` (`ff_methods/alpb_solvation.{h,cpp}`) gained a method-gated tblite path reusing the verified P16 Born kernel + Lebedev SASA, fed tblite params extracted by `scripts/extract_tblite_solvation_params.py` → `tblite_solvation_params.h` (76 sets) and CM5 tables by `scripts/extract_tblite_cm5.py` → `cm5_data.h`. GFN1 uses CM5 charges (`q_solute=q+cm5`, geometry-only, + `dcm5dr` gradient), GFN2 Mulliken. CDS H-bond folds into the Born diagonal via the existing HB machinery; the **key fix** was that curcuma's `m_sasa` omits the 4π that tblite's surface carries, so the CDS tension needs `gamscale·4π·1e-5` and the H-bond `-kcaltoau·sqrtGhbond²` (the 4π cancels) — Born/keps/radii were already exact (tblite `brad` matched to 7 digits). Wired into `xtb_native.{h,cpp}`: model built in `InitialiseMolecule`, geometry `update()` before the SCF, in-SCF `addSolvationPotential` (v_at=B·q_solute, enters the Fock via `expand_potential` + the GPU upload), `energySolvation()` added to `m_E_total` (separately, not into Tr(P·H0)), analytic gradient added in Eh/Bohr before `/=au` (with the solvation v_at also in the gradient `m_pot` for the band-energy Pulay term). PARAMs `solvent`/`solvent_model`/`solvent_epsilon` in `xtbinterface.h` + `applyXtbScfConfig`; usage `-method gfn2 -xtb.solvent water -xtb.solvent_model 3`. **Validation**: native total = tblite ALPB to ≤1e-8 Eh on 7 mol × 4 solvents × {gfn1,gfn2} (56 committed refs, `ctest -L _solvation` = 28 gfn1 + 28 gfn2, worst 6.4e-9); analytic gradient FD-validated (`ctest -R xtb_solvation_numgrad`, gfn1 ~1e-8 / gfn2 ~1e-4 = base D4-response floor); gas-phase byte-identical (gfn{1,2}_validation 24/24). Default `solvent=none` unchanged; GFN-FF path byte-unchanged. Out of scope/remaining: GBSA (params extracted), CPCM, GPU residency, GFN-FF self-consistent. Docs: [docs/SOLVATION.md](docs/SOLVATION.md), [docs/SQM_SOLVATION_WP.md](docs/SQM_SOLVATION_WP.md). 🤖 AI-generated, ⚙️ machine-tested only — human production testing pending (other solvents/ions/metals/large systems/MD untested).
- **ConfSearch: merge master (BMT) + integrate BMT output + final energy stats + snapshot-pruning default OFF + close two threads>1 init races (2026-06-10, Branch `confsearch`)**: Merged `origin/master` (brings the BMT `Basename.Keyword.YYYYMMDD_HHMMSS/` output-dir system) into the ConfSearch branch and wired ConfSearch into it. **(1) BMT integration (fixes an immediate crash):** ConfSearch created a BMT dir but still wrote its own files with bare basenames while `PerformOptimisation`/`PerformFilter` read them via `outputPath()` — so `triose.confsearch.../triose.input.xyz` was read from a dir nothing had written to (`File not found` → abort on the very first cycle). Routed all 12 ConfSearch I/O sites (`.input.xyz`, `.bias.xyz`/`.mtd.xyz`, `.cumulative.opt.xyz`, `.bias.opt[.accepted].xyz` reads, `_md_params.json`, `CalibrateBias` loads, the MD-thread basename) through `outputPath()`; trajectory/snapshot artefacts now land under the BMT dir (incl. per-run `*.snapshots/`), CWD stays clean (residual GFN-FF `*.topo.json` cache files are FF-owned, out of scope). **(2) Final Energy Statistics block** after the closing dedup pass: unique-conformer count, global minimum, energy span, improvement vs. the initial structure, and a top-10 relative-energy ladder (the run previously ended with no closing energy summary). **(3) `opt_feedback_prune_snapshots` default flipped true→false:** pruning the non-persistent MD snapshots after feeding back the optimised minima left the next cycle's bias pool with only the sparse persistent hills, weakening the metadynamics push and yielding shallower minima; keeping the raw snapshots preserves search depth (triose global min −9.94→−9.95 Eh, 45–80 conformers; pruning stays available opt-in). **(4) Two pre-existing `threads>1` init races, both unguarded lazy-populated process-globals, surfaced and closed with `std::call_once`:** `D4ParameterGenerator` re-ran `initialize_d4_alphaiw()`/`initialize_d4_corrections()` (which `.resize()`+assign the mutable global C6/α tables) on EVERY construction → concurrent OptThreads raced resize-vs-read in `computeC6Reference` → SIGSEGV; `CurcumaLogger::initCitationRegistry()` reassigned the static citation map on every `CurcumaMethod` ctor → concurrent `SimpleMD` construction in the MD pool rehashed it under other readers → `std::bad_alloc`/SIGSEGV (also added a mutex around `addCitation`/`printCitations`). The exact previously-crashing config (`triose -startT 600 -threads 4 -opt_feedback_prune_snapshots true`) now runs to completion across repeated trials (0 crash markers, 63–80 conformers). `cli_confscan_|cli_rmsd_|cli_simplemd_|cli_curcumaopt_` 30/30. 🤖 AI-generated, ⚙️ machine-tested only — human production testing pending.
- **ConfSearch: verbosity scoping (RAII) + cite/threads hardening + GOOD DEFAULTS for the bias-heating (2026-06-10, Branch `confsearch`)**: Three pieces. **(1) Verbosity-ownership (known issue #3) largely resolved:** RAII save/restore of the global `CurcumaLogger` level in the `CurcumaMethod` base (ctor captures the parent level, dtor restores), removing the 7 per-phase re-asserts in `ConfSearch::start()`; ConfSearch per-cycle logs survive across temperature cycles and multi-worker MD again. The verbosity is a non-thread-safe shared static, so the two pool-owning helpers (`PerformMolecularDynamics`/`PerformOptimisation`) re-assert on the main thread after the `CxxThreadPool` joins (verified: 1 worker → level intact, 2 workers → static left at 0), and `SimpleMD::Initialise` re-asserts after the energy-method setup (GFN-FF param-gen leaves it clamped to 0); the `InitConstrainedBonds` RATTLE report moves back to `CurcumaLogger` (summary ≥1, per-bond ≥3, silent at 0 — the old `std::cout` leaked even in silent mode). A `thread_local` verbosity (the only full fix) is out of scope. **(2) CitationRegistry (known issue #4):** `cite()` was already `std::mutex`-safe + `Citations::database()` a thread-safe static; added a `thread_local` fast path (skip the lock off the hot path) and per-instance crash dumps (`<basename>.unstable.json`/`.final.json`/`_step_*.json`); the residual `threads=4 startT 600` crash was the bias-heating runaway, now bounded. Verified gfnff `threads=4` runs to completion (4 concurrent instabilities, no SIGSEGV). **(3) Good defaults (the user's "weird temperature"):** the bias-heating mitigations were opt-in, so the bare `-confsearch triose -startT 500` still ran away (⟨T⟩ 550→577→1035 K start-of-run, then NaN ~1e63 K, repeated blow-ups). Measured on triose (`startT 500→400, repeat 2`): none → 4 blow-ups/20 conformers; **`freeze_inherited`+`temp_abort` → 0 blow-ups/42 conformers (best)**; `cap20`+`temp_abort` → 0 blow-ups/25; `temp_abort` only → 2 blow-ups/5 (insufficient: sudden single-step NaNs + aborts starve exploration). So **`rmsd_mtd_freeze_inherited` and `temp_abort` are now ON by default for ConfSearch** (`rmsd_mtd_max_height` stays opt-in for tighter T); standalone `-md` defaults unchanged. The exact `-startT 500` command now runs with 0 blow-ups. `cli_simplemd_|cli_confscan_|cli_curcumaopt_|cli_rmsd_` pass (full-suite timeouts are load-induced flakes, all pass isolated). 🤖 AI-generated, ⚙️ machine-tested only.
- **ConfSearch: bound the shared-bias-pool heating + temperature-runaway abort + fix the silently-dropped robustness flags (2026-06-09, Branch `confsearch`)**: Diagnosed the reported "MD temperature climbs with every MD run" (triose, `-startT 500`): the `SharedBiasPool` persists across all MD runs/temperature cycles, the per-structure hill height is `W_i = k·counter_i` with `counter_i` bumped every visit and never capped/reset (`pruneByCounter(1)` removes nothing since min counter is 1), and `wtmtd` damps only the COLVAR energy, not the force — so each run inherits a taller, denser, time-dependent (non-conservative) bias that pumps energy faster than the thermostat removes it. Reproduced: baseline `<T>` 552→596→654 across the first three runs, run 4 blows up ("Simulation got unstable"). **Two opt-in, default-off bias bounds (`simplemd`):** `rmsd_mtd_max_height` (Int, 0=unbounded) caps the counter used in the force `W_i = k·min(counter_i, cap)`; `rmsd_mtd_freeze_inherited` (Bool) snapshots the pool counters at each run's start in `prepareRun()` and uses the frozen height for inherited structures (and skips bumping them), so only this run's own deposits grow. Measured (triose, 5 sequential runs at 500 K): cap=3 holds `<T>`~495–525 K, freeze holds ~550–600 K with no blow-up, vs. baseline explosion. **New temperature-runaway abort gate** `temp_abort`/`temp_abort_factor`(1.5)/`temp_abort_delta`(300 K) in `step()`, mirroring `epot_abort` (running-mean `<T>` vs target `m_T0`, warmup `m_step>100`, either threshold trips, `<=0` disables one): factor 1.15 cleanly aborts the runaway runs at step 101 with no NaN, ConfSearch continues. **Routing bug fixed (pre-existing, affected `topo_check`/`epot_abort` too):** these are SimpleMD-owned PARAMs, so a flat CLI flag (`-temp_abort`, `-topo_check`, …) is auto-routed to `controller["simplemd"]`, which `ConfSearch` never consumed — it only forwarded its own `controller["confsearch"]` members (all left at `false`), so the flags were silently ignored. `ConfSearch::start()` now reads each gate value from `controller["simplemd"]` (falling back to the ConfSearch default), verified via `-export_run`/`*_md_params.json`. **Abort messages made visible:** all three gate aborts (epot/topo/temp) now print via `fmt::print` (orange/bold) instead of `CurcumaLogger::warn_fmt`, which is swallowed even at `-verbosity 3` by the verbosity-ownership clamp (roadmap issue #3) — matching the existing "got unstable" message. `make GenerateParams` clean (5 new params registered), `cli_simplemd_|cli_confscan_|cli_curcumaopt_|cli_rmsd_` 30/30 (defaults-off behaviour unchanged). 🤖 AI-generated, ⚙️ machine-tested only — human production testing pending; addresses roadmap TODO #4.
- **ConfSearch Phase C (cluster + RMSF-weighted) + RATTLE mode-2 gate fix + nicer RATTLE report (2026-06-09, Branch `confsearch`)**: **C10 `bias_calibration=cluster`** and **C11 `bias_scale_mode=weighted`**: after each cycle's opt+filter, `CalibrateBias()` assigns the optimised structures to the accepted distinct minima (permutation-aware best-fit RMSD via `PermRMSD` + an energy tolerance `bias_energy_tol`), then learns the basin capture radius (median intra-cluster pre-opt RMSD) -> sets the MTD hill width (`alpha=ln2/r_basin^2`), and the per-atom RMSF -> flexibility weights `1/sigma^2` fed to the shared pool for the weighted-Kabsch bias (Phase-B foundation). **Robustness (added after a smoke test produced absurd values):** requires >=2 distinct minima (else graceful skip -> keeps bootstrap alpha/uniform weights), gates same-basin pairs by `rmsd < d_inter` (drops cross-basin outliers that gave r_basin=39 A), uses the median, clamps `r_eff` to `[0.05, 0.8*d_inter]`, and clamps the weight dynamic range to 10x. Pool weights/permutations are mutex-guarded (`SharedBiasPool::setWeights/weights`); empty -> uniform/identity -> bit-identical default. **RATTLE mode-2 fix (pre-existing bug):** the activation gate was `if (rattle == 1)`, so `-rattle 2` (X-H only) silently ran plain Verlet with 0 constraints although `InitConstrainedBonds` handled mode 2 — Phase A1 defaults `rattle_hot_mode=2`, so its auto-RATTLE did nothing. Now `rattle == 1 || rattle == 2`; verified triose `-rattle 2` -> 32 X-H constraints, DOF 198->166. **Cleaned the RATTLE printout:** one-line summary `RATTLE: N constraints | X of Y 1-2 bonds (X-H only) + Z 1-3 angles | DOF 198 -> 166 (-32)` plus an element-labelled per-bond list (e.g. `H53-O5(0.990)`), wrapped. Kept on `std::cout` with a `FIXME`: the global CurcumaLogger verbosity is clamped to 0 by the energy-method setup before `InitConstrainedBonds`, so logger calls there are dropped even at `-verbosity 3` (see the verbosity-ownership rework in [docs/CONFSEARCH_ROADMAP.md](docs/CONFSEARCH_ROADMAP.md) and main CLAUDE.md Known Issues). **Threads caveat:** gfnff `threads>1` ConfSearch can crash via a `CitationRegistry::cite` data race on instability cleanup (separate FIXME); workaround threads=1. `cli_simplemd_|cli_confscan_` 19/19. 🤖 AI-generated, ⚙️ machine-tested only; cluster/weighted are experimental and unvalidated.
- **ConfSearch Phase C (couple) + measured bit-identity of the Phase-B MTD refactor (2026-06-08, Branch `confsearch`)**: **C9 adaptive MTD width** (`bias_calibration=off|couple`, opt-in, default off → behaviour-neutral): the default Gaussian width `alpha=10` puts the hill half-maximum at `sqrt(ln2/alpha)=0.26 A`, far inside a basin, while the dedup/"distinct conformer" scale is `rmsd~1.25 A` — so filling a basin needs dozens of hills and exploration is slow. `couple` sets `alpha = ln2/(bias_couple_factor*rmsd)^2` so the hill half-max sits at `bias_couple_factor*rmsd` (factor default 1.0 → half-max exactly at the dedup threshold, one hill ~ one basin; triose: alpha 10 → 0.4436 A^-2). Units verified: `m_eigen_geometry`/RMSD and `m_rmsd` are both Angstrom (m_eigen_geometry is set without conversion at simplemd.cpp:608), so alpha is A^-2 — also fixed the stale "Bohr^-2" log label. `bias_couple_factor<1` keeps the hill inside the basin (less neighbour-basin bleed) at the cost of fill speed. `cluster` (learn the basin radius from opt+filter clustering) and the RMSF-weighted scale (uses the Phase-B weighted Kabsch) warn "not yet implemented" and are pending. **Measured bit-identity (correcting an earlier asserted claim — there are NO ConfSearch regression tests):** swapped only `simplemd.cpp` to the pre-B8 commit, built, ran a deterministic ConfSearch (`-seed 42 -threads 1`), then restored B8 and ran the identical case with `-mtd_permutation false`: `triose.mtd.xyz` and `triose.bias.xyz` are byte-identical (md5 0e81d04c…, b07cf8cd…; `diff -q` clean) → the B8 `ApplyRMSDMTD` refactor (lambda + sum-over-images) is numerically a no-op with the permutation set empty. 🤖 AI-generated, ⚙️ machine-tested only.
- **ConfSearch Phase B: smooth symmetry-permutation-aware RMSD-MTD bias (2026-06-08, Branch `confsearch`)**: ConfScan's duplicate detection is best-fit-Kabsch RMSD PLUS the Hungarian/Kuhn-Munkres atom permutation (after inertia/subspace prealignment) — verified it reports `RMSDDriver::BestFitRMSD()`/`RMSD()` (confscan.cpp:86,129). So the **RMSD-unification** step (planned B7) is a no-op: the MTD already uses the same best-fit-Kabsch metric in the same units (Angstrom); the only difference is the MTD lacked the permutational information. **B8 (permutation cache):** ConfSearch now harvests the distinct, non-identity reorder rules ConfScan discovers (`ConfScan::getReorderRules()`), accumulates them across temperature cycles (`m_permutation_cache`, opt-out via `mtd_permutation`, default on), and feeds them to the shared bias pool (`SharedBiasPool::setPermutations`/`permutations`, mutex-guarded). In `SimpleMD::ApplyRMSDMTD` the bias becomes `V = Σ_i Σ_p W_i·exp(-α·RMSD_{i,p}²)` — a **SUM of Gaussians over the symmetry orbit, NOT a hard min over images**. Rationale (operator): a hard min makes the bias FORCE discontinuous at the symmetry seam where the argmin permutation switches (an MD integration artefact); summing one Gaussian per image is C∞-smooth (as the walker crosses the seam, one image's Gaussian rises while the other's falls continuously) and for well-separated images only the nearest contributes appreciably, so it behaves like the min but without the discontinuity (the erf/range-separated smoothing idea, realised as a Gaussian sum). The analytic force is just the sum of the existing per-image gradients. Only applied when the RMSD subset is the full molecule (rules are full-atom); empty permutation set -> identity only -> bit-identical to before. **B6 (weighted Kabsch, in the Phase-A commit):** `RMSDFunctions::{BestFitRotationW,getRMSDW,WeightedCentroid}` + optional per-atom weights in `RMSDDriver::BestFitRMSD()/Gradient()` (empty = unweighted, bit-identical) — the foundation for Phase C's flexibility-weighted (RMSF) scale, still inert. **Verified:** `cli_simplemd_|cli_confscan_` 19/19 (MTD refactor bit-identical, ConfScan unaffected); triose ConfSearch harvests a symmetry permutation in cycle 1 and applies it in cycle 2 ("1 symmetry permutation(s) active … smooth sum-over-images"). 🤖 AI-generated, ⚙️ machine-tested only. (Phase C: adaptive α↔dedup-scale coupling + cluster basin-radius learning + RMSF weights = pending.)
- **ConfSearch efficiency/robustness — Phase A + a global-verbosity leak fix (2026-06-08, Branch `confsearch`)**: Five opt-in/low-risk ConfSearch improvements, all defaults-off or behaviour-neutral. **(A1)** Temperature-triggered RATTLE: new `rattle_threshold_temp`(400)/`rattle_hot_mode`(2, H-only) — hot cycles auto-constrain X-H so 1 fs high-T MD doesn't drift/break bonds; cooler cycles keep the baseline. **(A2)** Opt-in topology abort during MD (`topo_check`, default off so reactive paths still sample): aborts a run when the connected-component count grows (`Molecule::GetFragments()`), and gates bias deposition; verified it does NOT false-abort an intact molecule (triose 400K, 42==42 frames). **(A3)** Opt-in early Epot abort (`epot_abort`/`epot_abort_window` kJ/mol): stops a run when the running-mean BARE potential climbs far above the run's start energy (window must exceed the thermal baseline ~N_dof·kT/2); verified `epot_abort_window=0.001` cuts a triose MD 42→13 frames while baseline is full-length. **(A4)** Seed energy window decoupled from output: new `seed_energy_window`(50) selects next-cycle MD seeds relative to the running GLOBAL minimum (not the per-cycle min), with `seed_window_schedule=static|exp` funnel (`seed_window_decay`); wide `energy_window` still governs the cumulative output. Verified the funnel shrinks 50→25 kJ/mol over two cycles and rejects high-energy seeds. **(A5)** Optimised minima fed back into the shared bias pool (`opt_feedback_bias`, default on; `opt_feedback_height` counter): `BiasStructure` gains a `persistent` flag (exempt from `pruneByCounter`); pool grows with discovered basins (triose: 2 MD + 2 fed-back → 4, then →6). Pool geometries confirmed to be in ANGSTROM (m_eigen_geometry set without conversion), so feedback uses `mol->getGeometry()` directly. **Verbosity leak fix (pre-existing):** every `CurcumaMethod` sub-instance and `OptimizerDriver::Optimize` drive the global `CurcumaLogger` verbosity down to their own level and don't restore it, so ConfSearch's entire per-cycle progress (the existing "independent MD runs" line too, not just the new logs) was invisible at default verbosity — confirmed by instrumentation (global=0 while m_verbosity=3 at cycle start). Fixed with a RAII guard in `OptimizerDriver::Optimize` plus ConfSearch re-asserting its `m_verbosity` after each sub-phase (initial Opt, MD, per-cycle Opt/ConfScan, final ConfScan). All Phase-A cycle logs now show at default verbosity. **Tests:** `cli_simplemd_` 19/19, `cli_curcumaopt_|cli_confscan_|cli_rmsd_` 18/18 green; 2-cycle ConfSearch on triose runs end-to-end. 🤖 AI-generated, ⚙️ machine-tested only — human production testing pending. (RMSD-unification + flexibility-weighted Kabsch + permutation cache + adaptive bias calibration = Phases B/C, pending.)
- **ConfSearch: fix RMSD-MTD shared-bias-pool path so MD actually deposits new conformers (2026-06-07, Branch `confsearch`)**: ConfSearch found no new structures for `triose` (and generally) while a standalone `-md -rmsd_mtd` on the same molecule did. Root cause in `SimpleMD::ApplyRMSDMTD()`: the shared-pool path (used by ConfSearch via `setSharedBiasPool`, always — even single-threaded) set the `m_shared_pool_driver` **reference once at init** to the start geometry and updated only the target in the per-step loop, while the local path (`BiasThread`) re-points its reference to the current walker geometry every step (`setCurrentGeometry`→`setReference`). Since bias structure 0 IS the start geometry, the frozen reference made `RMSD(start, struct0)≈0` → `current_bias≈dT·k≈1e5` huge → the deposition criterion `current_bias·econv < pool_count` never fired (pool stuck at 1) and the applied bias force was bogus. Fix: set `m_shared_pool_driver.setReference(current walker)` once per step before evaluating the snapshot. Verified the standalone MD (local path) was untouched and kept depositing. 🤖 AI-generated, ⚙️ machine-tested only.
- **RMSD-MTD: make energy/force/dt consistent and well-temper opt-in (2026-06-08, Branch `confsearch`)**: Audit of the RMSD-metadynamics found three inconsistencies (the RMSD value + Kabsch fit and `RMSDDriver::Gradient()=dRMSD/dx` were correct). **(P1) Force was not the gradient of the bias energy:** energy used `k·counter·exp(-α·RMSD²)` but the force factor was `-2α·k·factor/N_atoms·exp` — missing the `RMSD` factor and carrying a spurious `1/N`, so the applied force was ~`1/(RMSD·N)` too weak vs the reported energy (force direction right, magnitude/shape wrong, MD not bias-energy-consistent). Fixed to `dEdR = -2α·(k·counter)·RMSD·exp` = exact `-dV/dx` (hill height W_i = k·counter_i; reference = the walker, so `Gradient()=dRMSD/dx_walker`). **(P2) `dt` constant was used differently in the two paths:** local path took hill height from `m_dT`(=1, since `setdT` was never called) while `rmsd_mtd_dt`(=1e6) only fed WT; the shared path multiplied the height by `m_rmsd_DT`(=1e6) → `current_bias` differed by 1e6 between paths → different deposition rate (the real cause of the earlier 12-vs-23 gap, not stochastics). Removed `dt` from the height in both paths (height is purely `k·counter`); `dt` now only means ΔT for WT. Dropped the unused `BiasThread::m_dT`/`setdT`. **(P3) Well-tempered was effectively dead and inconsistent** (`factor=1+exp(...)`, ΔT=1e6 K → no damping, different denominators per path). Reimplemented as **opt-in (`wtmtd`, default off), energy-only, never touching exploration**: counter (→ force + deposition) is always undamped; WT accumulates a separate output weight `factor += exp(-V/(k_B·ΔT))` and reports `current_bias_wt = Σ k·factor·exp` in COLVAR. Both paths identical. `SharedBiasPool::incrementCounters` → `registerVisits((index, wt_weight))` (counter++ always, factor += weight). Defaults retuned for the now-correct force: `rmsd_mtd_k` 0.1→**0.01**, `rmsd_mtd_dt` 1e6→**2000 K** (help texts updated; the counter scheme is documented in-code as intentional non-textbook). **Verified (triose, gfnff):** MD stable, no blow-up (traj E ≈ −9.78 Eh); k-sweep shows healthy rate (k≤0.02 → 6–9 deposits/1000 fs, RMSD stays ~3 Å; k=0.1 over-drives → 139 deposits, RMSD→8). WT seed-42 run: deposits and RMSD trajectory **identical** to non-WT (exploration unaffected) while the WT energy damps correctly (ΔT=200: 0.0084→0.00027 Eh near a reference; ΔT=2000: ~undamped since V≪k_B·ΔT). ConfSearch 2-thread: pool 28 → 17 conformers, several below input (−9.928…−9.908 vs −9.9145). `ctest -R cli_simplemd_` 12/12. 🤖 AI-generated, ⚙️ machine-tested only.
- **SQM/EEQ: honor an explicit PCG request + fix the explicit-LU segfault in the EEQ solver dispatch (2026-06-06, Branch `feature/sqm-implementation`)**: Two `dispatchSolve` fixes in `eeq_solver.cpp`. **(1)** The April-2026 Gershgorin SPD pre-check (`min_gersh<=0 || κ_est>1e12 → bypass PCG to SchurCholesky`) fires on essentially every real molecular system because the EEQ matrix's off-diagonal `erf(γ·r)/r` terms sum past the diagonal hardness, so the Gershgorin lower bound `diag-Σ|A_ij|` is intrinsically negative even though `A_nn` is SPD. Left unscoped it silently overrode an explicit `-eeq_solver.solve_method pcg` (and `solve_method` defaults to `pcg`), so the PCG iterative loop — and with it the 1-step warm-start AND the new multi-step EEQ warm-start extrapolation — **never ran on dense systems**. Scoped the bypass to `m_solve_method==Auto` only; explicit PCG now runs PCG (NaN→LU fallback still guards genuine failures), Auto keeps the heuristic chain. **(2)** Pre-existing crash (present in HEAD, found during verification): the outer dispatch guard listed `SchurCholesky|PCG|Auto` but not `LU`, so explicit `-eeq_solver.solve_method lu` fell straight through to `goto end_dispatch` with `charges` never assigned → `signal 11`, and the `else if (method_to_use==LU){ goto lu_solve; }` branch was dead code. Added `LU` to the guard. **Verified (complex/231, gfnff):** `test_eeq_extrapolation` now ALL PASS — ASPC/Gauss fire 12× over a 13-frame trajectory and stay bit-identical to `none` (max|dE|=5.8e-15/8.0e-15); pcg/auto/cholesky/lu all give −37.24064873 Eh (no crash). The 3 still-red ctests (`gfnff_numgrad_fixed_charges`, `cli_gfnff_01/02`) are pre-existing — they fail identically with both edits reverted (proven by rebuild); 59/60 pass their energy checks and fail only on a missing `*.opt.xyz` harness expectation, 41 is a fixed-charges numerical-gradient issue. 🤖 AI-generated, ⚙️ machine-tested only.
- **SQM: XL-BOMD (extended-Lagrangian) corrector mode for the native GFN SCF — Phase 2 (2026-06-05, Branch `feature/sqm-implementation`)**: Implements `-scf_extrapolation_apply xlbomd` (was Phase-1 placeholder). The SCC density becomes a **time-reversibly propagated auxiliary variable** (Verlet + Niklasson dissipation, J. Chem. Phys. 130, 214109 (2009); coefficients K=3..7 in `XTB::xlbomdCoefficients`, `P_{n+1}=2P_n-P_{n-1}+κ(D[P_n]-P_n)+α Σ c_k P_{n-k}`) that seeds the SCF each geometry step; the SCF then **converges normally** so the energy is exact, and the time-reversibility lowers long-MD energy drift. **Key finding:** the naive "few bare SCF maps, no convergence loop" corrector is NOT contractive for tight-binding SCF (Picard map is expansive without mixing) and diverges to +80..150 Eh — verified — so the corrector converges; the iteration savings come from the good guess (like guess mode), while XL-BOMD's distinct value is the time-reversible auxiliary. New `m_xlbomd_aux` deque (bootstrap seeds K+1 converged states, then propagate from the converged D[P_n] in `Calculation`; cleared in `InitialiseMolecule`); `scf_xlbomd_correctors` repurposed as a min-corrector-iteration floor (default 1 = no effect). Cites `xlbomd`→Niklasson 2008 on use. **Measured (caffeine smooth 15-frame trajectory, thr 1e-8):** energy bit-identical to full SCF (gfn1 dE=3.5e-14, gfn2 dE=7.1e-15), SCF iters gfn1 170→141, gfn2 215→180; a short NVE MD stays bounded (CH4 gfn2 ~−4.17 Eh). `ctest -L scf_extrapolation` extended with a gfn1+gfn2 xlbomd check (energy==none + iters<none); default `none`/`guess` paths byte-unchanged, full `sqm_val`/`xtb_gradient`/`native_xtb`/`xtb_cpscf`/`d4_dedq` green. Honest: MD energy conservation / drift is NOT yet validated (only boundedness smoke-tested); experimental. Doku: [docs/SQM_SCF_EXTRAPOLATION.md](docs/SQM_SCF_EXTRAPOLATION.md). 🤖 AI-generated, ⚙️ machine-tested only.
- **SQM: opt-in multi-step SCC extrapolation for the native GFN1/GFN2 SCF (2026-06-05, Branch `feature/sqm-implementation`)**: Generalises the 1-step warm-start (which reuses only the previous converged step) to predict the new-geometry SCC vector — GFN1 `q_sh`, GFN2 `[q_sh; dp_at; qp_at]` — from a history of the last few converged steps, so the SCF starts closer to the new fixpoint (fewer iterations, esp. in MD). Two **independent opt-in** axes: predictor `-scf_extrapolation none|aspc|gauss` (ASPC = Kolafa 2004 fixed binomial coefficients `w_j=(-1)^j C(k+2,j+1)`, ideal for fixed-timestep MD; gauss = least-squares polynomial fit, robust for irregular LBFGS opt steps; both charge-conserving, `Σw_j=1`), order `-scf_extrapolation_order`, and coupling `-scf_extrapolation_apply guess|xlbomd` (guess = predictor seeds the guess + **full SCF still converges** → safe; xlbomd corrector-only is **Phase 2/not yet implemented**, warns + falls back to guess). New `XTB::{packSccState,unpackSccState,extrapolationWeights}` + `m_scf_history` deque (push in `UpdateMolecule`, predict in `Calculation`; default `none` path byte-unchanged, history cleared in `InitialiseMolecule`). **Measured (caffeine, smooth 15-frame interpolated trajectory, `guess` mode, scf_threshold 1e-8):** cumulative SCF iters GFN1 170→79 (aspc) / 72 (gauss), GFN2 215→90 (aspc) / 92 (gauss) — **~55% fewer**, final energy bit-identical (GFN1 dE=0, GFN2 dE=2e-14); caffeine gfn2 `-opt` reaches the same minimum −42.1538368 Eh with none/aspc/gauss. (Honest: the synthetic uniform-step trajectory is the ideal case — real LBFGS opt steps are irregular so gains are smaller; real fixed-dt MD is closer to ideal.) **Citations** auto-registered when a scheme is used: `aspc`→Kolafa, J. Comput. Chem. 25, 335 (2004); `gauss`→Pulay & Fogarasi, Chem. Phys. Lett. 386, 272 (2004); `xlbomd`→Niklasson, PRL 100, 123004 (2008) (DB entries added). Default `none` byte-unchanged: `sqm_val_*` gfn1+gfn2 24/24, `xtb_gradient`/`native_xtb`/`xtb_cpscf`/`d4_dedq` green (29/29); new `ctest -L scf_extrapolation` (`test_xtb_scf_extrapolation`, asserts same fixpoint + iters ≤ none for both methods). GPU resident loop benefits transparently (consumes the same warm `m_wfn`). Same loose-SCF gradient caveat as the warm-start (tighten `-scf_threshold` for tight forces). Doku: [docs/SQM_SCF_EXTRAPOLATION.md](docs/SQM_SCF_EXTRAPOLATION.md). 🤖 AI-generated, ⚙️ machine-tested only.
- **SQM: GFN2 opt/md SCF warm-start now restores the atomic multipoles (2026-06-05, Branch `feature/sqm-implementation`)**: The cross-geometry-step warm-start (`m_warmstart`, on by default) restored only the converged shell charges `q_sh`, while `UpdateMolecule` zeroed the GFN2 atomic dipoles/quadrupoles each step — so the multipole half of the SCC vector re-converged from zero every geometry, and the GFN2 `-opt`/`-md` SCF **plateaued at ~12 iterations/step** even as the geometry stopped moving (GFN1, whose whole SCC vector IS `q_sh`, already dropped 11→3 near convergence). Fix: save `m_wfn.dp_at`/`qp_at` alongside `q_sh` in `UpdateMolecule` and restore the FULL state `[q_sh; dp_at; qp_at]` in `Calculation` (both GFN2-gated; GFN1 byte-unchanged). **Result (caffeine gfn2 -opt):** per-step SCF now declines 15→4-5 like GFN1; total SCF iterations 343 vs ~600 over 50 steps (**−43 %**). Each saved iteration is a full eigensolve, so the absolute win grows with system size; benefits CPU and the GPU resident loop alike (`beginResidentLoop` uploads the warm `m_wfn` multipoles), and `-md` more than `-opt` (tiny per-step geometry change → near-perfect guess). Correctness: warm path converges to the SAME SCF fixpoint as cold — opt minimum −42.15383682 Eh == cold `-sp` on the optimized geometry; `gfn1+gfn2_validation` 24/24, `xtb_gradient`/`native_xtb`/`d4_dedq`/`xtb_cpscf` green. 🤖 AI-generated, ⚙️ machine-tested only.
- **SQM: thread the GFN2 D4 ATM 3-body term (2026-06-05, Branch `feature/sqm-implementation`)**: `D4Evaluator::computeATM` (the O(N³) Axilrod-Teller-Muto triple loop) was the only un-threaded phase left in the native GFN2 single point — flat across `-threads N` while everything else scales. Factored the per-`iat` work into a lambda and fanned the outer atom loop over the gated intra-thread budget (`effectiveIntraThreads`, `nat >= 4·m_threads`) with the same private-partial reduction as the 2-body loop; the serial path (`nthr=1`) writes the outputs directly so it stays byte-for-byte unchanged. **Measured (complex/231, RTX-host 32-core):** GFN2 post-SCF E 68→24 ms, TOTAL `-threads 8` 488→443 ms (~9%). Energy bit-stable (complex −329.52707823, triose −119.90699419, C6H6/CH4 unchanged t1↔t8); gradient norm identical. `computeATM` has a single caller (`xtb_native.cpp`, GFN2) — GFN-FF uses the per-pair path and is unaffected. Regression: CPU `gfn1+gfn2_validation` 24/24, `xtb_gradient`/`d4_dedq`/`native_xtb`/`xtb_cpscf` green. **Benchmark finding (no code change):** the per-iteration generalized eigensolve already threads via MKL (`MklThreadScope` + `mkl_gnu_thread` link) — complex/231 solve-eigen 607→206 ms (gfn2) / 731→212 ms (gfn1) at 8 threads, total 2.58×/3.33×; it saturates at ~8 threads (16/32 regress) and is at MKL's practical ceiling. The earlier "eigensolve doesn't thread / sequential MKL" note was stale. 🤖 AI-generated, ⚙️ machine-tested only.
- **SQM: GPU Stage 6 — restored the 5 device-resident SCF component tests (2026-06-05, Branch `feature/sqm-implementation`)**: The 5 component-test files lost in the WIP restore (`test_xtb_cuda_{occ,qsh,d4refw,scc_energy,broyden}.cpp`) re-authored against the now-frozen Stage-6 API and re-registered under the `gpu_scf` ctest label (31 tests). Each isolates one device kernel against its host reference: `occ` (`k_occupations` vs `XTB::computeOccupations`, gfn1+gfn2), `qsh` (resident shell charges vs `getShellCharges`), `d4refw` (`k_d4_build_refw` vs `buildD4RefWFlat` at a frozen q, compared over j<nref), `scc_energy` (device `GpuScfBackend::sccEnergy` vs `evaluateComponentsAtFixedDensity` at the identical converged resident state — a true frozen-state device-vs-host check, not a CPU/GPU fixpoint diff), `broyden` (device mixer vs host `BroydenMixer` over a synthetic contractive sequence, sweeping N/max_hist past the history ring). **Validated (RTX 5080):** `ctest -L gpu_scf` 31/31, residuals ~0..1e-14 (occ 4.7e-15, qsh 0, d4refw 1.1e-16, scc_energy 5.2e-16, broyden 1e-14); `gpu_integrals|gpu_gradient` 90/90 + `gpu_gfn2_validation` 12/12 unaffected. Purely additive (no source touched), gated by `USE_CUDA_XTB` (runtime-skip without a device). 🤖 AI-generated, ⚙️ machine-tested only.
- **SQM: GPU Stage 6 — fully device-resident GFN2 SCF iteration (2026-06-05, Branch `feature/sqm-implementation`)**: The whole per-SCF-iteration loop body now runs on the device; the host polls only `max|Δq_sh|` + the 4 SCC energy scalars (O(1)) per step — `eps`/`occ`/`pop_ao`/moments/`q_sh` never cross the bus. New device kernels: `k_occupations` (single-block Fermi-bisection / integer fill, port of `occupationsFromEps`), `k_qsh_scatter` (resident shell charges), `k_d4_build_refw` (port of `D4ParameterGenerator::buildAtomRefW` — rebuilds the D4 reference weights W/dWq from the resident SCF charges, fed by a new `exportRefWDeviceData`), `k_energy_third_order_shell` + `k_energy_multipole` + a γ·q_sh gemv/dot for the device SCC energy, `k_broyden_*` (the modified-Broyden mixer — cuBLAS Gram + a single-block M×M regularised solve, the delicate history port), and `k_maxabsdiff` (convergence). New seam `GpuScfBackend::{supportsResidentLoop,beginResidentLoop,residentScfStep,residentLoopCharges,beginDispersionWeights,shellCharges,sccEnergy}` + `XtbGpuContext::{occupations,residentShellCharges,beginDispersionWeights,dispersionBuildRefW,sccEnergy,broyden{Begin,Update},beginResidentLoop,residentScfStep,residentLoopCharges}`; `eigensolveResidentFock` gained a `download_eps=false` resident mode and `residentSolvePotential`/`residentDensity` were factored into resident (no-upload) cores. New host branch in `XTB::Calculation` (gated on the GFN2 device-potential + Broyden path; host-driven loop is the byte-unchanged fallback, GFN1 unaffected); the converged charges/P/C/eps download once so the post-SCF energy + gradient path is unchanged. **Validated:** energy bit-stable vs the CPU host loop (H2O/CH4/NH3/C6H6/HCN/triose to all printed decimals; complex/231 −329.52707822, 1e-8 to the bar); `gpu_gfn2_validation` 12/12 @1e-8 vs tblite (+ `gpu_gfn1_validation` 12/12 unaffected); gradient through the resident loop matches the CPU to ~1e-10 Eh/Å at `-scf_threshold 1e-8` (the device Broyden lands at a different point in the loose 1e-5 ball — energy stationary so 1e-8, gradient needs tight SCF; `test_xtb_cuda_gradient` now sets it); CH4 `-opt` −4.17521844; new `ctest -L gpu_scf` (31: occupation/q_sh/d4refw/scc_energy/broyden, frozen-state ~1e-10..1e-16; the 5 test files were lost in the WIP restore and restored in the follow-up above); compute-sanitizer 0 errors (H2O+triose); non-CUDA `release/` rebuilds + CPU `gfn{1,2}_validation` 12/12 (seam `#ifdef`-free). **Honest perf:** a residency/correctness milestone, NOT a measured `-sp` speed-up — complex/231 GFN2 `-sp -threads 8` SCF 341 ms (GPU) vs 417 ms (CPU) but ≈ the Stage-5 host-driven path (~515 ms TOTAL); the eigensolve + the residual O(1) host-pointer cuBLAS syncs (~5/iter) dominate, not the removed transfers. Remaining lever (deferred): device-pointer reduction accumulators with one download per step + a k-iteration convergence poll. Doku: [docs/SQM_GPU.md](docs/SQM_GPU.md), [docs/SQM_GPU_STAGE6_WP.md](docs/SQM_GPU_STAGE6_WP.md). 🤖 AI-generated, ⚙️ machine-tested only.

- **SQM perf WP — AP2 (D4 ref-block hoist) + AP1-GPU (partial diagonalization, opt-in) (2026-06-04, Branch `feature/sqm-implementation`)**: **AP2:** split `D4ParameterGenerator::weightedC6Gfn2` into `buildAtomRefW` (per-atom CN-Gaussian × charge-zeta weights) + `contractC6Gfn2` (the 7×7 block contraction); `d4_evaluator.cpp` builds each atom's `RefW` once (O(N)) before the O(N²) pair loop and the ATM `c6` fill instead of ~N times each — mirror of the D3 `refC6Block` hoist (commit 4b41562). Numbers bit-identical (`gfn2_validation` 24/24 @1e-8, `d4_dedq`/`d4_diag` green, `gfnff` untouched — it never enters these whole-molecule paths). Measured GFN2 post-SCF E 73.2→66.2 ms on complex/231 (~10%; the removed `buildRefW` redundancy). **AP1-GPU:** `eigensolveResidentFock(…, n_eig)` + `residentSolve`/`residentSolveMultipole` solve only the lowest `nocc+~5%` eigenpairs via `cusolverDnDsyevdx`/`Ssyevdx` (range il=1..n_eig), back-transform those columns, sentinel-pad `eps` (host occupation bit-faithful), and auto-widen to the full solve on a tiny-gap occupied tail; gated **opt-in default OFF** (`-scf_gpu_partial_diag`; auto-off for d4_charge_source=mulliken / verbosity≥3). Energies identical to the full solve (caffeine/triose/complex × gfn1/gfn2), `gpu_gfn{1,2}_validation` 24/24, `gpu_gradient`/`gpu_integrals` 66/66, compute-sanitizer 0 errors, non-CUDA `release/` gfn{1,2}_validation 24/24 unchanged. **Measured net-neutral on an RTX 5080** (the tridiagonalization, not the eigenvector count, dominates the dense eigensolve — the same reason the CPU `dsyevr` partial-solve was a dead-end); kept as a research path, default GPU path unchanged (full `cusolverDnDsyevd`). Doku: [docs/SQM_PERF_OPT_WP.md](docs/SQM_PERF_OPT_WP.md). 🤖 AI-generated, ⚙️ machine-tested only.

- **SQM: GPU native xTB integrals + nuclear gradient on the device (Stages 3+4) — device-resident -opt/-md (2026-06-03, Branch `feature/sqm-implementation`)**: The one-time-per-geometry integral build (CN, overlap S, bare Hamiltonian H0, Cholesky L, Coulomb γ, GFN2 multipole dp_int/qp_int) and the analytical nuclear gradient now run on the GPU, completing the device-resident path: an `-opt`/`-md` step uploads only xyz and downloads only the gradient+energy. Stage 3: new `cuda/xtb_gpu_integrals_device.cuh` (`__device__` ports of cn_exp/cn_gfn, cgto_overlap+primitives, kshell/kpair, Klopman-Ohno, cgto_multipole+moment1d), `XTB::exportGpuBasis` flattens the basis/H0 into CUDA-free bundles, `XtbGpuContext::beginBasis`+`computeIntegrals` build S/H0/L (device `cusolverDnDpotrf`) + γ + GFN2 multipole; the resident SCF computes instead of receiving them (seam falls back to the Stage-2 upload). Stage 4: `k_grad_repulsion`/`k_grad_cn_onsite`/`k_grad_h0_pulay`/`k_grad_coulomb` (GFN1) + the GFN2 multipole-integral Pulay (`d_cgto_multipole_grad_transformed`); energy-weighted density W built on the device; dispersion gradient + CN chain-rule + the GFN2 direct multipole interaction (section 5) stay on the host; `XTB::calculateGradientGpu` drives it via a new `GpuScfBackend::gradient` seam, wired into `Calculation` for both methods. γ/CN host consumers use option (a) (download γ once per geometry; the host gemv is unchanged). **Validated:** every device integral/gradient matches the CPU elementwise — CN/SE bit-identical, S/H0/L ~1e-15, γ ~5e-17, multipole ~1e-13, dS/dR ~1e-14, full GFN1+GFN2 gradient ~1e-15 Eh/Å; GPU `-opt` converges to the same geometry/energy as CPU (CH4 −4.17521844 Eh); `ctest -L gpu_integrals|gpu_gradient` 54 component tests + gpu_gfn{1,2}_validation 12/12 each (90/90 total); compute-sanitizer 0 errors on every kernel; non-CUDA `release/` gfn{1,2}_validation 24/24 unchanged. Commits 9e84e79 (Stage 3 + gradient primitive), 7df2210 (Stage 4a GFN1), 9bd55df (Stage 4b GFN2). Doku: [docs/SQM_GPU.md](docs/SQM_GPU.md), [docs/SQM_GPU_STAGE3_WP.md](docs/SQM_GPU_STAGE3_WP.md). 🤖 AI-generated, ⚙️ machine-tested only.

- **SQM: GPU device-resident GFN2 SCF — multipole Fock + moments on the device (Stage 2b) (2026-06-03, Branch `feature/sqm-implementation`)**: Extends the Stage-2a device-resident SCF to GFN2 (default Broyden mixing). On top of the resident H0/S/L + density/MO, the 3 dipole + 6 quadrupole AO integrals (`m_dp_int`/`m_qp_int`) and the AO→atom map are uploaded once (`residentBeginMultipole`); per iteration the device adds the anisotropic Fock term `F −= ½·Σ(dp_int·v_dp + qp_int·v_qp)` before the cached-L eigensolve (`residentSolveMultipole`, new `k_add_fock_multipole` kernel) and reads off the atomic moments `dp_at`/`qp_at` from the resident density via an atomicAdd scatter (`residentMultipoleMoments`, `k_multipole_moments`). Extra per-iter traffic is only v_dp (3·nat) / v_qp (6·nat) up and dp_at/qp_at (9·nat) down — the n×n multipole integrals never move. The isotropic potential (shell third-order, multipole scalar shift, **in-SCF D4** charge coupling folded into v_at from host-resident charges — no special callback), Fermi occupation, Mulliken aggregation, all energies, the Broyden mix of the larger SCC vector (q_sh+dp_at+qp_at) and convergence stay on the validated CPU path. Seam: 4 new `GpuScfBackend` virtuals (supportsMultipole/beginMultipole/solveMultipole/multipoleMoments; default false → CPU fallback); the shared cuSOLVER eigensolve factored into `eigensolveResidentFock` reused by GFN1 and GFN2; GFN1 (Stage 2a) and the CPU path byte-unchanged. **Validated:** gpu_gfn2_validation 12/12 (resident, 1e-8 vs tblite; complex xfail reproduced), gpu_gfn1_validation 12/12, CPU gfn{1,2}_validation 12/12, native_xtb 9/9 (release_cuda + no-CUDA release/); GFN2 GPU-vs-CPU energy ≤1e-8 on H2O/caffeine/triose/acetic_acid_dimer/complex(231); compute-sanitizer 0 errors on the atomicAdd kernels. Stages 0–2 complete; next is Stage 3 (integrals on the GPU) toward device-resident -opt/-md. Doku: [docs/SQM_GPU.md](docs/SQM_GPU.md). 🤖 AI-generated, ⚙️ machine-tested only.

- **SQM: native GFN1/GFN2 on the GPU — Stage 2a device-resident GFN1 SCF (`-method gfn1|gfn2 -gpu cuda`) (2026-06-03, Branch `feature/sqm-implementation`)**: Staged CUDA port of the native xTB solver (cuSOLVER/cuBLAS), sibling to the GFN-FF GPU stack; the CPU `XTB` class stays `#ifdef`-free. Built in `release_cuda/` with `-DUSE_CUDA=ON -DUSE_CUDA_XTB=ON -DCMAKE_CUDA_ARCHITECTURES=120`. **Stage 0** (scaffolding) + **Stage 1** (per-iteration generalized eigensolve on cuSOLVER) landed in commit `6956f16`. **Stage 2a (this change): GFN1 device-resident SCF.** For GFN1 with the default Broyden mixing, `H0`/overlap `S`/Cholesky `L` and the per-iteration density `P`/MO coefficients `C` stay RESIDENT on the device for the whole SCF loop — only length-nao vectors cross the bus per iteration (`v_ao` up; `eps`, `pop_ao`, band scalar down), never the nao² matrices that Stage 1 still re-uploaded. **On the device** (`XtbGpuContext::resident{Begin,Solve,Density,Finalize}`, `qm_methods/cuda/xtb_gpu_context.cu`): Fock `F=H0−½·S·(v_ao⊕v_ao)` kernel, eigensolve via cached `L` (2× `cublasDtrsm`+`cusolverDnDsyevd`), density `C·diag(occ)·Cᵀ` GEMM, Mulliken-AO `pop_ao(μ)=Σ_ν P_μν S_μν` reduction, band `Σ P⊙H0` (`cublasDdot`). **On the host (unchanged, validated CPU path):** potential build, Fermi occupation, Mulliken shell/atom aggregation, Coulomb+third-order energies, Broyden mixing, convergence; `finalize()` downloads `P`/`C` once for post-SCF + the (still CPU) gradient. Seam = new abstract `GpuScfBackend` interface (`xtb_native.h`, project types only → core stays CUDA-free; `nullptr` default is inert). GFN2 and non-Broyden GFN1 keep the Stage-1 eigensolver hook. **Validated:** `gpu_gfn1_validation` 12/12 (resident path, 1e-8 vs tblite; He2/complex xfails reproduced exactly), `gpu_gfn2_validation` 12/12, CPU `gfn{1,2}_validation` 12/12; GFN1 GPU-vs-CPU energy ≤1e-8 on H2O/caffeine/triose/complex(231); `compute-sanitizer` 0 errors; canonical no-CUDA `release/` rebuilds + passes. Stage 2b (GFN2 resident: multipole Fock/Mulliken + in-SCF D4) is next. Doku: [docs/SQM_GPU.md](docs/SQM_GPU.md). 🤖 AI-generated, ⚙️ machine-tested.

- **SQM: large-system modes `-large_system_mode fragments|dc|sparse` (opt-in, approximate >1000-atom scaling) + `-eigensolver` propagation (2026-06-02, Branch `feature/sqm-implementation`)**: Three opt-in locality approximations to the native GFN1/GFN2 SCF, default `none` (dense) byte-unchanged; all reuse the validated dense `XTB` engine + tblite-validated `evaluateComponentsAtFixedDensity`. **`fragments`**: bond-connectivity partition, per-fragment dense SCF, summed energy + block-diagonal gradient (the only mode with a gradient — `-opt` works); exact for non-interacting clusters, single connected fragment → exact dense fallback; 8-water sweep error 0.67→0.03 mEh as 5→20 Å (= the neglected inter-fragment interaction). **`dc`**: Yang divide-and-conquer — spatial core cells (`-large_system_cell_bohr`) + buffer (`-large_system_buffer_bohr`), GLOBAL Fock each outer iter, sub-block diagonalisation, shared-µ electron-count bisection, core-projected density assembly, Broyden charge mixing; energy-only; complex(231) error 291→0.1 mEh and polymer(1410) 90.8→0.10 mEh monotonic over buffer 6/8→16 Bohr, polymer @buffer-8 **2× faster than the 70 s dense SCF** at 0.064 mEh/atom. **`sparse`**: non-orthogonal canonical Palser-Manolopoulos density purification of M=P·S (0 K, gapped) replacing the eigensolve, power-iteration spectral bounds for M0, one-shot threshold prune (`-large_system_sparse_threshold`) → measured nnz; complex nnz 1.0→0.14 over threshold 0→1e-3 at ≤0.5 mEh, gapless → eigensolver fallback; dense-storage first cut (true-sparse O(N) deferred). **NEW: `-eigensolver` propagation** — `fragments` and `dc` propagate the chosen `-eigensolver` into every fragment / sub-block (`mkl` / `native` / `lobpcg` / `purify`), so e.g. `-eigensolver=native + -large_system_mode=dc` gives a fully GPU-portable pipeline; `sparse` ignores `-eigensolver` (it IS the eigensolver replacement). **NEW: T=0 hard rule** — `fragments`/`dc` with `-eigensolver=purify` requires `-electronic_temperature 0`; the wrapper rejects in `setMolecule` with a clear error (purification is 0-K by construction, integer occupation, no Fermi smearing). New PARAMs in `xtbinterface.h` (LargeSystem); `large_system_modes` ctest (label `large_system`) green — 13 checks including eigensolver propagation and the T>0 hard-error. Doku: [docs/SQM_LARGE_SYSTEMS.md](docs/SQM_LARGE_SYSTEMS.md). 🤖 AI-generated, ⚙️ machine-tested.

- **SQM: seeded Block-LOBPCG `-eigensolver lobpcg` (experimentell, subspace-recycelt) (2026-06-02, Branch `feature/sqm-implementation`)**: Dritter GPU-Readiness-Hebel, opt-in + ehrlich als **Net-Loss auf dichtem GFN** etikettiert. `curcuma::eigsolver::lobpcgLowest(A,k,X,evals,…,nConverge)`: Block-LOBPCG (Knyazev) für die niedrigsten k Eigenpaare mit Diagonal-Preconditioner und **robuster generalisierter Rayleigh-Ritz** (Gram-Matrix-Eigenfilterung des [X|W|P]-Unterraums → near-null-Richtungen werden verworfen statt den Solve zu korrumpieren). **Guard-Vektoren:** nur die niedrigsten `nConverge` Zustände müssen `tol` erreichen, die übrigen k−nConverge beschleunigen den randständigen (lückenlosen) Eigenvektor. **Seeding:** der SCF reicht die Eigenvektoren der Vor-Iteration (`m_eig_seed`, n×kb) als Startblock weiter → Warmstart konvergiert in ≤30 Iter. SCF-Verdrahtung (`xtb_scf.cpp` solveEigen): k=nocc+guard, nconv=nocc+2, die kb Vektoren füllen die untersten A-Spalten, Virtuelle darüber Sentinels (occ≈0); **Fallback auf dsyevd** bei Nicht-Konvergenz; default-eeq-D4-Gradient (nur besetztes W) unberührt. **Validiert:** ctest `lobpcg` (Eigenwerte ~1e-13, Unterraum ~1e-10 vs Eigen, inkl. Warmstart); SCF bit-identisch zu MKL — caffeine −42.14723025, complex gfn2 −329.52707823 @300K; H2O-`-opt` Energien+Gradientennormen identisch; `native_xtb` 9/9. **Net-Loss bestätigt:** complex single-core mkl 1.32s vs lobpcg 3.75s (~2.8×) — LOBPCG zahlt sich nur bei k≪n oder Sparsity (C1) aus; der ~3·nocc-RR-Unterraum bei nocc≈n/2 kostet mehr als ein dsyevd. Damit alle drei GPU-Readiness-Hebel (WY-Block-Q, Purifikation, LOBPCG) gelandet; C1 (Sparsity, >1000 Atome) bewusst zurückgestellt. Doku: [docs/SQM_EIGENSOLVE_GPU.md](docs/SQM_EIGENSOLVE_GPU.md). 🤖 AI-generated.

- **SQM: 0-K-Dichtematrix-Purifikation `-eigensolver purify` — GEMM-only, ohne Diagonalisierung (2026-06-02, Branch `feature/sqm-implementation`)**: Zweiter GPU-Readiness-Hebel. Neuer opt-in SCF-Dichtepfad, der die idempotente 0-K-Dichte direkt aus F per **trace-correcting TC2** (Niklasson 2002) baut — nur Matrixprodukte + Spuren, keine Eigenzerlegung. `curcuma::eigsolver::purifyDensity(Ã, nocc, P̃, W̃)` im orthonormalen Basis (`Ã=L⁻¹FL⁻ᵀ` ist im native-Pfad ohnehin geformt): Gershgorin-Init auf [0,1], dann P←P² (Tr zu hoch) bzw. 2P−P² (zu niedrig), Idempotenz-Monitor `Tr(P)−Tr(P²)` aus dem ohnehin gebildeten P² → **ein GEMM/Iteration**. Rücktransformation `P_AO=2·L⁻ᵀP̃L⁻¹`. **Gradient ohne Eigenpaare:** die energiegewichtete Dichte wird als `W=2·L⁻ᵀ(P̃ÃP̃)L⁻¹` geliefert (`m_wfn.W`/`W_valid`), der Gradient nutzt sie statt `C/eps`. **Gating:** nur bei `-electronic_temperature 0` + vorhandener Cholesky; bei T>0 / Konvergenzfehler **Warnung + Fallback** auf den Eigensolver (keine stille Physikänderung). **Validiert:** Energie bit-identisch zu MKL@0K — caffeine −42.14723025, complex gfn2 −329.52707823, gfn1 −343.17980352 Eh; H2O-`-opt` Schritt-für-Schritt identische Energien UND Gradientennormen (W̃-Route korrekt); neuer ctest `purification` (idempotenz/Tr/P/W vs Eigen-Projektor ~1e-12, bis n=558); `native_xtb`-Suite 8/8. 0-K-Erstausbau (operator-Entscheid); finite-T (grand-canonical) zurückgestellt. 🤖 AI-generated.

- **SQM: WY-Block-Q-Akkumulation für `blockedTridiag` — Q jetzt GEMM-only, on-par mit Eigen (2026-06-02, Branch `feature/sqm-implementation`)**: Der eigene blockierte Tridiagonalisierungs-Kernel (`CURCUMA_EIG_TRED2=blocked`, der MKL-freie GPU-Blueprint in `native_eigensolver.cpp`) akkumulierte die orthogonale Q bisher per Spalte mit Rang-1-Updates (BLAS-2, O(n³)) — der Grund, warum `blocked` gegen Eigen verlor. Ersetzt durch eine **Compact-WY-Block-Anwendung pro Panel**: `ΠH_i = I − V·T·Vᵀ` (T spaltenweise wie LAPACK `dlarft` aufgebaut), dann `Q_panel −= ((Q_panel·V)·T)·Vᵀ` in zwei GEMMs. Der Kernel ist damit **vollständig GEMM-only** (außer dem inhärenten Panel-symv, wie in LAPACK `dsytrd`) → GPU-portabel. **Gemessen (complex/231, nao=558):** blocked-`tred2` t8 **22.2 → 10.20 ms** (Eigen 9.99 — jetzt on-par, vorher 1.8× Lücke), t1 16.4 ms (Eigen 12.4); scalar-Baseline 56.9 ms. **Bit-identisch:** SCF-Energie caffeine −42.14723025 / complex −329.52707823 Eh über MKL, native(eigen), native(blocked). ctest `native_eigensolver{,_blocked,_scalar}` grün (random/degenerate/cluster bis n=558, recon ~2e-14), volle `native_xtb`-Suite 7/7. Opt-in, Eigen bleibt nativer Default, MKL globaler Default. Erster von drei GPU-Readiness-Hebeln (danach B2 Purification, B1 seeded solve). 🤖 AI-generated.

- **SQM-WP4: D&C-Rekursion parallelisiert — nativer Eigensolver skaliert 3.07×, on-par mit MKL (2026-06-01, Branch `feature/sqm-implementation`)**: Die unabhängigen Teilprobleme der Cuppen-D&C werden jetzt parallel gelöst. **Deadlock-frei** (kein nested fork-join auf dem bounded Pool): Abstieg im Split-Baum bis Tiefe ~log2(threads) mit Anwendung der Rang-1-Tearings, dann die balancierten Subtrees FLACH parallel (jeder per seriellem D&C, `parFor` über thread-lokalen `CxxThreadPool`), danach Merge bottom-up seriell (die Rücktransformations-gemms threaden über BLAS). Thread-Budget vom xTB über `effectiveIntraThreads` (`solveSymmetric(..., nThreads)`). **Gemessen (complex/231, gfn2, min-of-3 solve-eigen): t1 598 → t2 368 → t4 236 → t8 195 ms (3.07×)** — der eigene D&C ist bei t8 jetzt **on-par mit MKLs threaded dsyevd (~190 ms)**. Energie+Gradient bit-identisch (−329.52707823), ctest `native_eigensolver` (random/degenerate/cluster) unverändert grün, Default-MKL-Pfad unberührt (56/57, nur pre-existing #40). Der gleiche flache Subtree-Split ist der GPU-Offload-Punkt. **Korrektur:** `ParallelEigenSolver.hpp` ist NICHT entfernbar (frühere Changelog/Docs falsch) — die Klasse wird von EHT/PM3/AM1/MNDO/PM6/NDDO als Eigensolver-Wrapper genutzt (nur ihr interner Block-D&C war verworfen). Siehe [docs/SQM_THREADING_WP.md](docs/SQM_THREADING_WP.md). 🤖 AI-generated.

- **SQM-WP4 (komplett): eigener Cuppen-Divide-and-Conquer-Eigensolver, on-par mit MKL (2026-06-01, Branch `feature/sqm-implementation`)**: Der `-eigensolver native`-Pfad nutzt jetzt eine echte **Cuppen-Divide-and-Conquer** auf dem Tridiagonal (statt QL): rekursives Tearing in zwei Hälften + Rang-1-Kopplung, Säkulargleichung pro Merge. **Numerik-kritische Teile:** (1) **shifted bisection** (dlaed4-Trick) — Wurzel am näheren Pol verankert, nach dem Shift τ gelöst, sodass `D_i−λ=(D_i−Anker)−τ` ohne Auslöschung exakt bleibt (direkte λ-Bisektion verlor relative Präzision der kleinen Nenner → Eigenvektoren bei großen Zufallsmatrizen kaputt, recon ~3e-8); (2) **Deflation** vernachlässigbarer Gewichte + entarteter Diagonalen via Givens; (3) **Löwner/Gu–Eisenstat**-rekonstruierte Gewichte → orthogonale Eigenvektoren auch bei engen Clustern. QL (`tql2`) als Basisfall für n≤32. **Validiert** (ctest `native_eigensolver`, erweitert um random/degenerate/identity/tight-cluster bis n=558): Eigenwerte ~1e-12, Rekonstruktion/Orthonormalität ~1e-14 vs Eigen. SCF-Energie UND Gradient **bit-identisch** zu MKL (gfn1/gfn2, complex, t1 und t8). **Geschwindigkeit: on-par mit MKL `dsyevd`** — gfn2 complex min-of-5 TOTAL t1 1390 vs 1377 ms (~1%), t8 519 vs 521 ms (der serielle D&C matcht MKL, weil die umgebende BLAS — Reduktion, Rücktransformation — threadet). Default-MKL-Pfad unverändert (67/68, nur pre-existing #40). Damit zwei wählbare SCF-Eigensolver (MKL-Default + eigener D&C, dependency-frei, GPU-fähiger Split: rekursive Teilprobleme sind unabhängig). Der alte `ParallelEigenSolver.hpp` bleibt bestätigtes Dead-End. Siehe [docs/SQM_THREADING_WP.md](docs/SQM_THREADING_WP.md). 🤖 AI-generated.

- **SQM-WP4 (Foundation): eigener self-contained Eigensolver, `-eigensolver mkl|native` (2026-06-01, Branch `feature/sqm-implementation`)**: Zweiter, MKL-unabhängiger Eigensolver für das SCF-Eigenproblem, wählbar per `-eigensolver native` (Default `mkl`). `native_eigensolver.{h,cpp}`: Householder-Tridiagonalisierung (`tred2`) + Implicit-Shift-QL (`tql2`), dependency-frei (kein LAPACK-Eigensolve). Der native-Pfad in `solveEigen` reduziert `A=L⁻¹·F·L⁻ᵀ` über Eigen-Dreieckslöser (BLAS dtrsm, kein dsygst) und diagonalisiert mit dem eigenen Solver; Rücktransformation wie gehabt. **Validiert:** standalone vs Eigen bis n=558 — Eigenwerte ~1.4e-12, Rekonstruktion/Orthonormalität ~5e-14 (neuer ctest `native_eigensolver`); SCF-Energie UND Gradient **bit-identisch** zu MKL für gfn1/gfn2 (complex −329.52707823 / −343.17980352, t1 und t8); Default-MKL-Pfad unverändert (native-xTB ctests grün außer pre-existing #40). **Kosten:** native QL ~20-30 % langsamer als MKL `dsyevd` bei t1 (784 vs 639 ms complex) — erwartet für from-scratch-QL vs optimiertes D&C. Der alte `ParallelEigenSolver.hpp` ist bestätigtes Dead-End (sein Block-D&C war mathematisch falsch, verworfen) — nicht wiederverwendet. **Nächster Schritt:** `tql2` durch ein paralleles Cuppen-Divide-and-Conquer (rekursiver Split + Säkulargleichung + Deflation) ersetzen — gleiches Interface, GPU-fähig. WP4-Foundation von 5, siehe [docs/SQM_THREADING_WP.md](docs/SQM_THREADING_WP.md). 🤖 AI-generated.

- **SQM-WP2b: D4-Evaluator-Paarschleife parallelisiert — D4-In-SCF t8 154→46 ms (3.8×), GFN-FF intakt (2026-06-01, Branch `feature/sqm-implementation`)**: `D4Evaluator::computeEnergyAndGradient` (GFN2-only Whole-Molecule-Pfad) parallelisiert über die Paare via eigenem lazy `CxxThreadPool`; thread-lokale E/gradient/dEdCN/dEdq-Partials, danach reduziert (bit-identisch bis FP-Reassoziation). Per-Pair-Generator-Lesezugriffe (`weightedC6Gfn2`, `getZetaCharges`, `getDC6DCN`) sind `const`/thread-safe. Thread-Budget vom xTB-Treiber über `effectiveIntraThreads` gesetzt (`setThreads`), also seriell unter Molekül-Level-Parallelität. **GFN-FF unberührt** — nutzt `pairEnergyAndGradient` (per-Pair, über ForceFieldThread), nicht diese Methode; **17/17 `gfnff_val` grün** (Integration verifiziert). **Gemessen (complex/231):** D4-In-SCF t8 154→46 ms (3.8×), gfn2 TOTAL t8 ~640→593 ms; Energie bit-identisch (−329.52707823). Eigensolver (~180 ms) ist wieder die dominante SCF-Komponente. d4_diag/gfn2/gradient/cpscf grün (nur pre-existing #40). WP2-Erweiterung, siehe [docs/SQM_THREADING_WP.md](docs/SQM_THREADING_WP.md). 🤖 AI-generated.

- **SQM-WP2: D4-In-SCF-Potential Pair-Cache — D4/SCF-Iter 336→152 ms bei t1 (2026-06-01, Branch `feature/sqm-implementation`)**: Der D4-Evaluator (`computeEnergyAndGradient`) baute die **geometrie-feste** Paarliste (`GenerateDispersionPairsNative`) in JEDER SCF-Iteration neu auf — inkl. einer per-Referenz-C6-Gewichtung, deren Ergebnis der GFN2-per-ref-Loop ohnehin verwirft (er überschreibt `pair.C6` via `weightedC6Gfn2(q)`). Neuer **geometrie-gekeyter Pair-Cache** in `D4Evaluator` (`m_pairs_cache`, nur per_reference_charge/GFN2-Pfad), invalidiert vom xTB-Treiber bei neuer Geometrie (`invalidatePairCache()`, gekoppelt an den bestehenden `m_d4_prepared`-Reset in `Calculation()`). **Gemessen (complex/231):** D4-In-SCF 336→152 ms/SCF bei t1 (2.2×), gfn2 TOTAL t1 ~1567→1362 ms. Energie bit-identisch (SP −329.52707823; -opt CH4 konvergiert t1/t4 identisch → Cache-Invalidierung über Geometrieschritte korrekt). d4_diag/gfn2_validation/gradient/cpscf ctests grün (nur pre-existing `gfnff_numgrad_fixed_charges`). Rest-D4 (~152 ms) ist die inhärente ladungsabhängige `weightedC6Gfn2`-Gewichtung pro Iteration (nicht gethreaded). Neuer `-verbosity 3` „of which D4"-Sub-Timer. WP2 von 5, siehe [docs/SQM_THREADING_WP.md](docs/SQM_THREADING_WP.md). 🤖 AI-generated.

- **SQM-WP1: threaded MKL freigeschaltet — Eigensolver threadet jetzt, complex(231) t8 gfn1 3.07× / gfn2 2.26× (2026-06-01, Branch `feature/sqm-implementation`)**: Die „MKL-Threading kein Effekt"-Wand war ein **Build-Artefakt**, kein `dsyevd`-Limit. `curcuma_core` linkte den threaded Layer (`mkl_gnu_thread`, CMakeLists L976) UND — über das unbedingte `find_package(BLAS/LAPACK)` (L933–942/L962–972) — den **sequentiellen** `libmkl_sequential`, der zuerst lädt und gewinnt → jede BLAS/LAPACK-Routine seriell. **Fix:** die find_package-Link-Blöcke mit `if(NOT USE_MKL)` gaten, sodass nur der explizite threaded MKL linkt (`ldd`/`readelf` zeigen kein `mkl_sequential` mehr). Global bleibt MKL seriell per Default — `MKL_Set_Num_Threads(1)` einmal in `main.cpp` (die KAPITALISIERTE C-API by-value; die lowercase-`mkl_set_num_threads` ist der Fortran-Alias by-reference → Segfault) — sodass GFN-FF/UFF/Optimizer und molekül-level Batches unverändert bleiben; nur `MklThreadScope(eig_threads)` um `solveEigen` hebt es lokal (gated über `effectiveIntraThreads`/`SuppressIntraParallel`, also seriell in Batches → keine N²-Oversubscription). Bonus: energiegewichtete Dichte `W` im Gradient von Rang-1-Schleife auf ein `gemm` (`xtb_gradient.cpp`). **Gemessen (complex/231, min-of-5):** dsyevd 425→133 ms (3.2×), solve-eigen 614→215 (2.85×); TOTAL t8 gfn1 375 ms (3.07×, 1152 t1), gfn2 650 ms (2.26×, 1469 t1; D4-In-SCF-Potential ~180 ms bleibt seriell → WP2). **Single-Core ohne Regression** (taskset -c 0, kein -threads: gfn1 1160, gfn2 1336, leicht schneller durch W-gemm). Energie+Gradient bit-identisch t1/t8; native-xTB + GFN-FF ctests grün (nur pre-existing `gfnff_numgrad_fixed_charges`). Erster von 5 Single-Molekül-WPs, siehe [docs/SQM_THREADING_WP.md](docs/SQM_THREADING_WP.md). 🤖 AI-generated.

- **SQM-Multithreading: native GFN1/GFN2 intra-Molekül-Parallelisierung via CxxThreadPool — complex(231) 1.3–1.4×, Setup 4×, Gradient 3.6× (2026-06-01, Branch `feature/sqm-implementation`)**: Eine einzelne große Rechnung (`-sp`/`-opt`/MD eines Moleküls) nutzt jetzt mehrere Kerne; Default (ohne `-threads`) bleibt seriell und bit-identisch. Kein mindless-OpenMP — der Projekt-`CxxThreadPool` (GFN-FF-Idiom `enqueue`+`future`, Haupt-Thread macht Stripe 0). **Auto-Gate (`src/core/intra_parallel_context.h`, tracked — NICHT im vendored CxxThreadPool):** molekül-level Batch-Worker (`CurcumaOpt::SPThread`/`OptThread`, `HessianThread`) setzen `curcuma::intraParallelSuppressed()` per RAII → die SCF darin bleibt seriell (kein N×N-Oversubscribing); Single-`-sp`/`-opt`/MD laufen auf dem Haupt-Thread → volles Budget; Conformer-Search optimiert sequentiell → jede SCF darf threaden. Größen-Guard (`effectiveIntraThreads`, `kMinWorkPerThread`) hält kleine Systeme seriell. Budget vom globalen `-threads` über `setThreadCount`→`XTB::setIntraThreads`. **Parallelisiert** (disjunkte Schreibzugriffe bzw. thread-lokale Reduktion, bit-identisch): Overlap+H0 (`xtb_h0.cpp`), GFN2-Multipol-Integrale (`xtb_multipole.cpp`), Gradient (`xtb_gradient.cpp`, thread-lokales grad/dEdcn + Reduktion), Fock-Build (`xtb_scf.cpp`). In-Loop nur `buildFock` (Multipol-Potential/Populations wieder seriell — Dispatch-Overhead > Nutzen). **Gemessen (complex/231, nao=558, gfn2):** setup 206→49 ms (4.2×), gradient 218→59 (3.7×), buildFock 92→28 (3.3×), TOTAL 1593→1072 (1.5×); t8 gfn1 922, gfn2 1062 ms. **Single-Core ohne Regression** (taskset -c 0: gfn1 1200 vs 1221, gfn2 1362 vs 1364). **Eigensolver-Wand re-validiert + Ursache gefunden:** MKL-`dsyevd` threadet weder bei nao=558 (417→419) noch nao=4028/1410-Atome (9442→9378), auch mit `MKL_NUM_THREADS=8` nicht — **weil der Build `libmkl_sequential` linkt** (CMake zieht `mkl_sequential` UND `mkl_gnu_thread`; sequential lädt zuerst und gewinnt). Die alte „MKL-Threading kein Effekt“-Notiz war also ein Build-Artefakt, keine `dsyevd`-Grenze; `MklThreadScope` ist aktuell ein harmloser No-Op. Folgearbeiten: threaded-MKL linken ODER der aufgeschobene GPU-ready Divide-and-Conquer-Eigensolver. CxxThreadPool-Gewinne (Setup 4.1× auch bei nao=4028) sind MKL-unabhängig. Energie+Gradient bit-identisch t1/t8; native-xTB-ctests grün (nur pre-existing `gfnff_numgrad_fixed_charges`, `cli_sqm_11`). Siehe [docs/SQM_THREADING.md](docs/SQM_THREADING.md). 🤖 AI-generated.

- **SQM-Performance II: native GFN1/GFN2 schlägt jetzt tblite (gfn1 auch xtb) — complex −54…−59% (2026-06-01, Branch `feature/sqm-implementation`)**: Tiefe Single-Core-Retiming-Analyse + 5 energie-neutrale Fixes; 45/45 native-GFN-ctests grün, Energien bit-stabil. **complex(231), Energie+Gradient, 1 Kern:** gfn1 2982→**1221 ms** (schlägt tblite 2562 *und* xtb-cold 1367), gfn2 2944→**1364 ms** (schlägt tblite 1567; 1.39× xtb 979). Die alte „eigensolver-bound, near-optimal“-Story war unvollständig — per-Iteration war der Eigensolver schon auf tblite-Niveau; die echten Kosten lagen woanders. Fixes: (1) **EEQ-Startguess als Default** (`scf_guess=eeq`, war `h0`): der dftd4-EEQ-Single-Shot startet im richtigen Becken statt ~6 Iterationen aus bare-H0 zu erholen — complex gfn1 35→16 it, gfn2 34→22 it, Energie bit-identisch (Fixpunkt guess-unabhängig). (2) **Cholesky-Reduktion** statt dichtem `S^{-1/2}` (`xtb_scf.cpp`): `solveEigen` bildete `X·F·X` + dichte Rücktransformation (~6·nao³/it); jetzt einmal `L=chol(S)` cachen (`buildOrthonormalizer`, Eigen-LLT), pro Iteration LAPACK-`dsygst`-Reduktion + ein Dreiecks-Solve (`dtrsm`) zurück (~2·nao³) — Transform+Back complex gfn1 393→190, gfn2 304→160 ms. **Falle:** der gecachte Faktor muss column-major `Eigen::MatrixXd` sein (Projekt-`Matrix` ist row-major → Fortran-`dsygst` bekäme sonst Lᵀ und zerstört den SCF). (3) **Dichte nur über besetzte Spalten** (`leftCols(ncol)`, occ>1e-12) — complex gfn1 129→58, gfn2 101→56 ms, energie-neutral. (4) **GFN2-D4-Ladungsantwort auf analytisches EEQ geroutet** (`d4_charge_source=eeq` war Default, aber nie verdrahtet — Code rief immer die teure Mulliken-CPSCF/Z-Vektor `computeMullikenChargeResponse`, 574 ms = 88% der D4-Kosten): jetzt `D4ChargeModel::addChargeResponseGradient` (Adjoint-Solve auf dem kleinen EEQ-System, ~ms); Energie-Gewichtung bleibt auf SCF-Mulliken-Ladungen → Energie unverändert, nur Gradient-Pfad — post-SCF gfn2 653→83 ms, Gradient weiter < 5e-5 FD (`xtb_gradient_*` grün). (5) **scf_threshold 1e-6→1e-5 Default**: das Bindekriterium `max|dq_shell|<1e-6` überkonvergiert (Energie <1e-8 Eh mehrere Iterationen früher); 1e-5 ist energie-bit-identisch über den ganzen Molekülsatz, MD/opt können via `-scf_threshold` verschärfen. **Verworfen/nicht verfolgt:** `MKL_ENABLE_INSTRUCTIONS` (kein Effekt — xtb/tblite/curcuma linken dasselbe MKL, Eigensolver-Boden geteilt); partielle Diagonalisierung (`dsyevr`, nur besetzte Vektoren — bei 44–54% Besetzung gering + bricht den `mulliken`-CPSCF/Orbital-Pfad). Neu: `-verbosity 3` Intra-Iterations-Breakdown + `scripts/sqm_bench.sh` (Single-Core, min-of-N, xtb `--norestart` gegen das warm-restart-Artefakt). Siehe [docs/SQM_PERFORMANCE.md](docs/SQM_PERFORMANCE.md). 🤖 AI-generated.
- **SQM-Performance: native GFN1/GFN2 Eigensolver-Caching + MKL-Serial + D4-Pair-Skip — complex 1.5–1.75× schneller (2026-05-31, Branch `feature/sqm-implementation`)**: Drei Bottlenecks im nativen xTB-Pfad entfernt, ohne die 1e-8-Validierung zu brechen. (1) **Eigensolver** (`xtb_scf.cpp`): `XTB::solveEigen` löste pro SCF-Iteration das *verallgemeinerte* `FC=SCε` (Eigen `GeneralizedSelfAdjointEigenSolver`) und faktorisierte das geometriekonstante S jedes Mal neu. Jetzt einmal pro Geometrie `X=S^{-1/2}` (Löwdin, neue `buildOrthonormalizer`), pro Iteration nur das *Standard*-Problem `(X·F·X)C̃=C̃ε` via MKL-`dsyevd` (Divide-and-Conquer, `extern "C"`, Eigen-`SelfAdjointEigenSolver`-Fallback ohne MKL), Rücktransform `C=X·C̃` erhält `CᵀSC=I` → Gradient/CPSCF unberührt. Dichteaufbau von Rang-1-Akkumulation auf gewichtetes GEMM (`C·diag(occ)·Cᵀ`/`noalias`). (2) **MKL-Threading**: ganze `Calculation()` thread-lokal auf 1 MKL-Thread gepinnt (`MklSerialScope` in `xtb_native.h`, `MKL_Set_Num_Threads_Local` — die C-API, NICHT die lowercase-Fortran-Alias mit By-Reference-Arg → sonst Segfault). MKLs Per-Call-Thread-Team ist für die serielle SCF über kleine/mittlere Matrizen bis ≥231 Atome reiner Overhead (gemessen serial schneller); grobe Parallelität bleibt CxxThreadPool auf Molekül-Ebene, keine Oversubscription bei parallelen Rechnungen. (3) **D4-Setup** (`d4param_generator.{h,cpp}`): `GenerateParameters` baute pro Geometrie eine `nlohmann::json`-Pair-Liste (~12 String-Key-Inserts/Paar + OpenMP-Spawn), die nativer GFN2 (nutzt `D4Evaluator` direkt) **nie konsumiert**; neues `setBuildPairLists(false)` überspringt sie (GFN-FF baut sie weiter, ForceFieldThread konsumiert `d4_dispersion_pairs`). **Gemessen (min/3, release/, MKL+AVX2):** complex(231) gfn1 4250→2435 ms (1.75×), gfn2 3157→2078 ms (1.52×), triose gfn1 42→34, caffeine gfn1 5.9→4.1; SCF steady-state ~0.6 ms/Iter (caffeine gfn2 iter1+), die iter-0-Setup-Tax amortisiert in opt/MD. **Keine Regression**: 12/12 native ctests (`xtb_gradient`/`xtb_cpscf`/`d4_dedq`/`d4_diag`/ngfn) + 17 `gfnff_val` grün; Energien bit-stabil (gfn2 H2O −5.07036982, =Referenz). Die 3 GFN-FF-Failures (#40 numgrad, #52/#53 cli erwartet `*.opt.xyz`) **per Stash-Baseline-Rebuild als pre-existing bestätigt** (0/3 auch ohne meine Änderung), `cli_sqm_11` ist ipea1/no-tblite. 🤖 AI-generated.
- **SQM-WP2: GFN1 D3-C8-Tail behoben — exakte s-dftd3 C8/C6-Form; 10/12 bei 1e-8 (2026-05-31, Branch `feature/sqm-implementation`)**: Der nach dem Elektronik-Fix verbleibende, größenextensive **Dispersions-Rest** war ein systematischer **+0.06%-Fehler im D3-`C8/C6`-Verhältnis**. `D3ParameterGenerator::getR6` (`d3param_generator.cpp`) nutzte eine empirische `C8/C6 = 10.72·r4r2_i·r4r2_j` mit nicht-standardisierter `r4r2`-Tabelle; die autoritative s-dftd3-Form ist `C8/C6 = 3·r4r2_i·r4r2_j` mit `r4r2(z)=√(½·⟨r⁴⟩/⟨r²⟩·√Z)` (s-dftd3 `data/r4r2.f90:69`, `damping/rational.f90:173`). Der Fit war +0.055…+0.069% pro Paar zu hoch. Fix: exakte Form + verbatim s-dftd3-Rohtabelle ⟨r⁴⟩/⟨r²⟩ (118 Elemente; identisch zu `D4ParameterGenerator::m_r4_over_r2`); korrigiert auch den BJ-Radius `R0=a1·√(C8/C6)+a2`. **Ergebnis:** triose-Dispersion −0.04199628→**−0.04201074** (=tblite, exakt); **10/12 SQM-Moleküle bei 1e-8** (H2, LiH, H2O, CH4, NH3, C6H6, HCN, acetic_acid_dimer, caffeine, triose); xfail-Liste auf He2 (~1.5e-8 Floor) + complex (~2.6e-7) reduziert. **Validierung nur vs tblite/s-dftd3 über den GFN1-Pfad** (fixiert den geteilten D3-Kernel: C6, CN, C8/C6, BJ-R0, Damping). **UFF-D3, `gfnff-d3` und standalone-D3 bleiben UNVALIDIERT** — andere Damping-Parameter/Pfade, keine autoritative s-dftd3-Referenz; der `getR6`-Fix macht deren C8/C6 exakt, prüft aber ihre Gesamtenergien nicht. Keine Test-Regression (3 GFN-FF-Failures pre-existing; `cli_sqm_11` ipea1/no-tblite pre-existing). 🤖 AI-generated.
- **SQM-WP2: GFN1 elektronisches Residuum behoben — doppelt gezähltes Third-Order-Potential (2026-05-30, Branch `feature/sqm-implementation`)**: Das nach dem D3-Fix verbleibende, größenextensive **elektronische** Residuum (triose +8.96e-4, complex +3.36e-3) war ein **doppelt gezähltes GFN1-Third-Order-Potential**. `XTB::addThirdOrderPotential` (`xtb_thirdorder.cpp`) addierte das atom-aufgelöste `v_at(i)=q_i²·Γ_i` in **beide** `pot.v_at` *und* (broadcast) `pot.v_sh`; da `expand_potential` `v_ao = v_sh + v_at` bildet, ging der Term mit 2× in den Fock → überstrafte Ladung → weniger polarisierter SCF-Fixpunkt (triose Coulomb-ES2 0.660 vs tblite 0.676), Elektronik +8.8e-4. Dieselbe Doppel-Broadcast war im CPSCF-Response (`xtb_response.cpp`) gespiegelt. Fix: GFN1-Third-Order nur in `v_at` (vorgesehener Platz) in beiden Stellen. **Lokalisierung:** Phase-0-Diffs zeigten S und H0 bit-genau (max|Δ|~1e-13), γ strukturell exakt, n0-Referenzbesetzungen exakt (~1e-16); der auf GFN1 erweiterte **ref-P-One-Shot-Fock** (`test_xtb_scf_snapshot.cpp`) reproduzierte tblite-Orbitalenergien auf ~5e-6; alle drei SCF-Modi (plain/diis/broyden) konvergierten zum *selben* falschen Fixpunkt → nicht der Mixer. **Ergebnis** (curcuma−tblite total): triose 8.96e-4→**1.45e-5**, complex 3.36e-3→**5.09e-5**, caffeine 1.8e-3→5.3e-6, acetic_acid_dimer 1.4e-3→2.1e-6, kleine Moleküle ~6e-8..2e-7; triose-Elektronik stimmt auf 8 Stellen (−131.33260976). Gradient FD-validiert (`test_xtb_gradient` ngfn1) + CPSCF grün (der Fix korrigiert auch den GFN1-Gradienten/Response). Verbleibendes Residuum ist jetzt **dispersionsdominiert** (D3-C8-~0.06%-Rest); xfails bleiben (Operator-Entscheidung). 🤖 AI-generated.
- **SQM-WP2: native GFN1-D3 Referenz-CN-Tabelle korrigiert (2026-05-30, Branch `feature/sqm-implementation`)**: Die GFN1-Dispersionsabweichung vs tblite (triose 8.4 mEh Überbindung, complex 35 mEh) war **vollständig im Zwei-Körper-D3 C6** lokalisiert (CN, C8/C6-Verhältnis, BJ-R0, Energieformel und He2 alle bit-identisch zu s-dftd3; per-Container-Audit `gfn1_align` + neue Probes `dump_dftd3_atomic_c6.f90` / `diag_d3_c6.cpp`). Ursache: `d3_reference_cn.cpp` (`reference_cn_data_complete`, 721 Werte) war **verwürfelt** — z.B. Kohlenstoff `[-1,-1,0.96,-1,-1,9.81,0]` statt s-dftd3 `[0,0.987,1.999,2.999,3.984,-1,-1]` → falsche Gauss-Gewichte → C6 für Kohlenstoff +61% (C-C), +26% (C-H); C6-Referenztabelle (262444 Werte) und `m_number_of_references` waren korrekt. Fix: Tabelle aus s-dftd3 `reference.f90 reference_cn(7,103)` (spaltenmajor = `[elem*7+ref]`) **neu generiert**. Ergebnis: interpolierte C6 jetzt 0.00000% vs s-dftd3 (alle 2145 triose-Paare); injizierte-Dichte `|disp+elec|` 100–640× kleiner (triose 9.28e-3→1.44e-5); GFN1-Gesamtresiduen ~10× kleiner (triose 8.4e-3→8.96e-4, complex 3.5e-2→3.36e-3). **Geteilt mit UFF-D3/GFN-FF-D3 — keine Regression** (GFN-FF 21/22 wie Baseline, gfn2/gradient/cpscf/d4 grün). `gfn1_align`-Toleranzen verschärft; `d3_s9=0.0` in `createForGFN1` explizit gepinnt (tblite gfn1.f90:53; war effektiv bereits 0). **GFN1 noch nicht bei 1e-8**: verbleibendes Residuum ist ein separater, kleinerer **elektronischer** Self-Consistent-Unterschied (nicht SCF-Konvergenz — energieidentisch bei 1e-12) + C8/r4r2-~0.06%-Rest; xfails bleiben. 🤖 AI-generated.
- **SQM-WP3: Komponenten-Validierung (per-Container) + Referenzen mit `energy_components` (2026-05-30, Branch `feature/sqm-implementation`)**: `dump_tblite_reference.cpp` erfasst Energie-Container jetzt **einzeln** (vorher droppte `ok &=` den ganzen Block, sobald ein Container fehlte — `halogen` für H/C/N/O, `interactions` ohne Solvens); alle 24 committeten `*.ref.json` tragen nun `energy_components` (textuell eingespleißt, übrige Bytes byte-identisch — 1e-8-Baseline ungestört). `validate_sqm.py` läuft mit `-verbosity 2`, parst die native Container-Zerlegung und meldet **advisorisch** native-vs-Referenz `disp+elec` (apples-to-apples) und `repulsion` separat; Exit-Code bleibt am 1e-8-Gesamtenergie-Gate (opt-in `--tol-components`). GFN1-Forensik-Dumps (`dump_tblite_multipole gfn1`) + `ecomp_*_gfn1`-ctests (Label `gfn1_align`, H2/H2O/NH3/CH4/triose) ergänzen den GFN2-Audit. Resultat: GFN1-Residuum als **Dispersion** lokalisiert (Repulsion bit-clean), Eingang für WP2. 🤖 AI-generated.
- **SQM-Validierungssuite: native GFN1/GFN2 vs committete tblite-Referenzen, ehrliches 1e-8-Gate (2026-05-30, Branch `feature/sqm-implementation`)**: End-to-end-Suite analog GFN-FF. Neuer kompakter Dumper `test_cases/sqm_reference/dump_tblite_reference.cpp` (USE_TBLITE) schreibt kleine `*.ref.json` (Gesamtenergie, Ladungen, Dipol, HOMO/LUMO, Gradient-Norm+Werte) → `reference_data/` (24 Dateien, **committet**); Python-Treiber `validate_sqm.py` ruft das echte `curcuma`-Binary (ngfn1/ngfn2) und gated die Gesamtenergie gegen die Referenz — **kein tblite-Build nötig** (wie GFN-FF). **Gate = 1e-8 Eh für ALLE** `sqm_val_*`; Moleküle, die es noch nicht erreichen, sind `WILL_FAIL`-xfails mit gemessenem Residuum (Toleranz wird nie aufgeweicht; erreicht ein Molekül 1e-8, meldet ctest den xfail als Fehler → erzwingt Entfernung). ctest-Labels `gfn2_validation`/`gfn1_validation` (je 12). Molekülset (12): H2 He2 LiH H2O CH4 NH3 C6H6 HCN acetic_acid_dimer caffeine triose complex. **Gemessen (release/, USE_TBLITE=OFF, dE=native−tblite):** GFN2 erreicht 1e-8 auf **11/12** (nur `complex` offen, 6.95e-5 Eh); GFN1 auf **0/12** (He2 1.5e-8, H2 1.3e-6 … triose 8.4e-3, complex 3.5e-2). **Ursache GFN1 offen** — NICHT „fehlende D3" (native GFN1-D3 existiert; große Systeme überbinden, falsches Vorzeichen für fehlende Dispersion), per-Container zu lokalisieren (WP3→WP2). Beide Labels grün (11 Pass+1 xfail / 12 xfail). Doku: [docs/SQM_VALIDATION.md](docs/SQM_VALIDATION.md), Roadmap [docs/SQM_VALIDATION_ROADMAP.md](docs/SQM_VALIDATION_ROADMAP.md) + WP1/WP2/WP3. 🤖 AI-generated.

- **GFN2 D4: doppelte Parameter-Regenerierung pro SCF-Iteration entfernt (2026-05-30, Branch `feature/sqm-implementation`)**: `XTB::addDispersionPotential` (im SCF-Loop, `xtb_native.cpp`) rief pro Iteration explizit `D4ParameterGenerator::GenerateParameters()` auf (~85 ms auf `complex`: CN+EEQ+C6-Ref+Gauss-Gewichte inkl. verworfener JSON-Pair-Gen C6Interp=52 ms) UND danach `computeEnergyAndGradient()`, das intern via `GenerateDispersionPairsNative()` CN+Gewichte+dc6dcn **nochmal** berechnet. Die explizite `GenerateParameters` war reines Duplikat — nur ihr `dEdq` (per-Atom D4-SCF-Potential `vat`) wurde gebraucht. **Fix**: `mutable bool m_d4_prepared` Guard (oben in `Calculation()` zurückgesetzt — Pflicht für opt/MD-Korrektheit); erste `addDispersionPotential` einer Geometrie macht den vollen Aufbau einmal, spätere SCF-Iter setzen nur `setTopologyCharges(q_at)` und werten `dEdq` neu aus. `with_gradient` bleibt TRUE (D4Evaluator füllt `dEdq` nur bei gesetztem Gradient-Flag — false zu setzen nullt die D4-SCF-Kopplung und driftet triose um 5.7e-5 Eh). Diagnose: `d4_genparams_calls_this_geometry` (verbosity≥3). **Energien bit-identisch** (H₂O −5.07036982, CH₄ −4.17507458, NH₃ −4.42618246, C₆H₆ −15.87853118, triose −119.90699419, complex −329.52707823); explizite genparams-Calls 34→1 auf complex; kontrollierte A/B-Best-of-3-Wall complex-SP **7.07 s → 4.38 s (~38 %)**; alle 15 D4/Komponenten/Gradient/CPSCF/SQM-ctests grün. GFN1-D3-Pfad unberührt. 🤖 AI-generated.

- **Legacy-GFN1/GFN2-Standalone-Klassen entfernt (2026-05-30, Branch `feature/sqm-implementation`)**: `gfn1.cpp` (1542 Z.) + `gfn2.cpp` (1232 Z.) + `gfn1_params_loader.{cpp,h}` + `gfn2_params_loader.{cpp,h}` + `gfn1.h`/`gfn2.h` (8 Dateien) waren toter Code — der aktive `-method gfn1/gfn2/ngfn1/ngfn2`-Pfad läuft über `gfn{1,2}_method.cpp` → `curcuma::xtb::XTB` (`xtb_native.cpp`), das die separaten Header-Tabellen `parameters/gfn{1,2}_params.hpp` nutzt (NICHT die gelöschten `*_params_loader`). MethodFactory instanziierte die Altklassen nie. Nachweis vor Löschung: Altheader+Loader nur von den Alt-`.cpp` referenziert (quoted-glob-grep; unquoted `--include` matcht in fish still nichts — Falle); einzige Außen-Erwähnung `class GFN2;`-Forward-Decl in `interface/ulysses.h` (USE_ULYSSES=OFF, harmlos). 2 Zeilen aus `CMakeLists.txt` (curcuma_core_SRC) entfernt; `release/` baut grün, Energien bit-identisch, 5 vorbestehende ctest-Fehler (auf pristine baseline verifiziert) unverändert. 🤖 AI-generated.

- Native GFN SCF: Broyden-Charge-Mixing ist neuer Default — `complex` (231 Atome) Divergenz behoben (2026-05-29): **Default-`scf_mode` ist jetzt `broyden`** (`broyden_mixer.h`, modified Broyden / Johnson 1988), das den SCC-**Ladungsvektor** (q_sh + GFN2-Multipole) quasi-Newton mischt — wie tblite/xtb, der eigentliche Architektur-Unterschied zum alten linearen-Dichte+Fock-DIIS. `-method gfn2` konvergiert `complex` jetzt ohne Flags aus bare-H0 in **34 Iter** → −329.52707823 Eh. Energiegleich zu `-scf_mode diis` auf kleinen Molekülen; volle native-GFN-ctest-Suite grün (xtb_gradient_{H2O,CH4,NH3}, d4_dedq, xtb_cpscf, ngfn1+ngfn2-Baseline, 7/7). Weitere Modi wählbar: `-scf_mode diis|plain|level-shift` (`applyLevelShift`, Saunders-Hillier), Initial-Guess `-scf_guess h0|eeq` (single-shot dftd4 EEQ via `seedEEQGuess`/`D4ChargeModel`). Außerdem SCF-Parameter endlich durchgereicht (`updateGFN2Parameters()` war ein leerer Stub); DIIS extrapoliert nicht mehr aus pathologischem Kommutator-Fehler. Neu: `xtb`-PARAMs `scf_mode/scf_guess/scf_damping/scf_threshold/diis_start/diis_subspace/level_shift`. Docs: [docs/SCF_MODES.md](docs/SCF_MODES.md). 🤖 AI-generated.

- AP6b GFN2 D4 exact port (2026-05-27): native GFN2 D4 now uses the exact dftd4 per-reference charge weighting (new d4_charge_scaling.h shared zeta + D4ParameterGenerator::weightedC6Gfn2 with wf=6/ngw/gc=2) and a self-consistent SCF dispersion potential (XTB::addDispersionPotential); H2 matches tblite to 3e-9 (HOMO 1.9e-6, was 1.5e-4), GFN-FF numerically unchanged, regression 6/6 green.
- **RMSD JSON output file (June 10)**: `executeRMSD()` in `main.cpp` now writes `<target>.rmsd.json` containing `rmsd`, `rmsd_raw`, `permutation` (identity when no reordering), `reference_xyz`, `reorder_xyz`, `reference_file`, `target_file`. Always generated (even in no-reorder mode). When `TargetReorderd()` is empty (atoms matched, reordering skipped), falls back to `TargetAligned()`. Cleanup in `test_utils.sh` includes `*.rmsd.json`. Tests 01 and 02 verify JSON file existence. 🤖 AI-generated.
- **ConfScan pass output visible at verbosity 1 (June 9)**: Initial/Reorder/Reuse pass start/finish messages and the final "structures were kept" summary were swallowed at verbosity 1 because `CurcumaLogger::success()` checks the global logger verbosity, which the RMSD machinery lowers to 0 mid-scan. Added `ConfScan::logPass()` helper that temporarily syncs the global verbosity to `m_verbosity` (same workaround as `AcceptMolecule`/`RejectMolecule`); routed all pass messages through it and wrapped the end-of-scan summary in `Finalise()`. 13/13 confscan+rmsd CLI tests pass. 🤖 AI-generated.
- **RMSD dtemplate crash fix (June 9)**: `DistanceTemplateStrategy::align` decremented the distance-map iterators without a lower bound; with the new default `limit=10`, molecules offering fewer than 10 matchable template atoms underflowed past `cbegin()` (UB/crash). Capped the effective limit to the available atom count, guarded the iterators against reaching `cbegin()`, and fail cleanly on an empty template. Mirrored in the unused legacy `RMSDDriver::PrepareDistanceTemplate`. Verified: 6-atom methanol now runs (RMSD 0), 90-atom case unaffected (RMSD 2.87). 🤖 AI-generated.
- **BMT output directory system**: All commands default to creating a `Basename.Keyword.YYYYMMDD_HHMMSS/` directory for output. `-bak` flag copies specified files back to CWD. `-no_bmt` disables BMT (legacy CWD output). `BMTUtils` namespace provides `createBMTDir`, `writeMetadata`, `processBakFiles`, `outputPath`, `stripExtension`, `collectBakFiles`. `CurcumaMethod` integration via `createBMTDir()`, `addBakFile()`, `processBakFiles()`. 🤖 AI-generated.

## May 2026

- **Native GFN1 SCF charge-sloshing behoben — HCN 263→0.33 mEh (Branch `feature/sqm-implementation`)**: `ngfn1` (Route → `curcuma::xtb::XTB(GFN1)`) konvergierte für polare/Mehrfachbindungs-Systeme (HCN, Koffein) in einen **falschen Ladungstransfer-Zustand** (HCN: C≈−1.48, N≈+0.85 — N positiv trotz höchster EN!), weil die SCF ohne Dämpfung allein über Fock-DIIS lief und vom blanken H0-Startwert in die falsche Mulde extrapolierte. **Fix** (`xtb_native.cpp`): Dichte-Dämpfung (`m_scf_damping`, default 0.4) während einer Warmup-Phase + DIIS erst ab `diis_start=5`. HCN −5.51942→**−5.78229** (Ref −5.78263, 0.33 mEh); Ladungen jetzt physikalisch (N=−0.247, C=+0.182, H=+0.065). 6/7 ngfn1-Tests grün (vorher effektiv 0/7); nur Koffein offen (~14 mEh, dominiert von fehlendem GFN1-D3, separater AP). Keine GFN2-Regression (ngfn2/Gradient/CPSCF/D4 alle grün, `gfn2==ngfn2` bit-identisch). 🤖 AI-generated.

- **Native xTB Fermi-Dirac-Smearing im SCF-Eigensolver (Branch `feature/sqm-implementation`)**: `XTB::solveEigen` (`xtb_scf.cpp`) nutzte das deklarierte `m_electronic_temp` (300 K, `xtb_native.h`) nicht — nur Integer-Besetzung. Jetzt: `temp>0` → Fermi-Dirac mit Bisektions-Suche nach µ und fraktioneller Besetzungs-Dichte (Muster aus `gfn2.cpp`), `temp==0` → bisheriger Integer-Fill. Default 300 K (wie TBLite); hilft bei kleinen HOMO-LUMO-Gaps. 🤖 AI-generated.

- **Single-Point-Energie-Ausgabe vereinheitlicht (`main.cpp`)**: `-sp` gab `Single Point Energy: … Eh` (Logger, Doppelpunkt) aus, aber alle SQM-CLI-Testskripte grepten nach `Single Point Energy = …` (wie `curcumaopt.cpp`). Folge: jeder `-sp`-Test meldete "no energy output". Fix: `fmt::print("Single Point Energy = {:.8f} Eh")`. PM3/AM1/MNDO + ngfn1/ngfn2 Tests laufen seither korrekt. 🤖 AI-generated.

- **GFN2 Mulliken-Ladungsantwort — Vorzeichen-Bug gefunden+behoben (Branch `feature/sqm-implementation`)**: Der Lade-Response `∂q_A/∂x` in `computeMullikenChargeResponse` (`xtb_response.cpp`) war **vorzeichen-invertiert** (`anal ≈ −num`). Ursache: `RHS_SIGN=−1` wurde mit einer **vorzeichen-blinden** max-abs-Ratio `|Δanal|/|Δnum|` "validiert", die einen reinen Vorzeichenflip als "Ratio≈1.0, korrekt" las. Neuer entkoppelter Test (`test_xtb_cpscf` **Test D**: `computeMullikenChargeResponse(e_A)` = `∂q_A/∂x`, FD gegen Mulliken-Ladungen bei straffer SCF) deckte es für H₂O/HCN/CH₄ auf. **Fix `RHS_SIGN=+1`**: Roh-`∂q/∂x`-Fehler 5–8× kleiner; D4-Mulliken-Gradient erreicht jetzt das `<5e-5`-Ziel für H₂O (4.19e-5→**2.46e-5**) **und** HCN (5.66e-5→**7.75e-6**, vorher Fehlschlag). Multipol-Response-Terme (3b-4a Integral-Pulay + 7b Interaktions-Linearisierung) **vorhanden aber per Default aus** (`MP_RESPONSE_ENABLED=false`): korrekt für schwach-multipolare Systeme (CH₄ → ~1%), aber fehl-kontrahiert für stark polare Moleküle (H₂O/HCN); für das D4-Ziel irrelevant. Expliziter Overlap-Term verworfen (verschlechtert beide Vorzeichen). Test C (D4-Isolation) jetzt **gegated** `<5e-5` (H₂O/HCN), Test D gated `<3e-2` (sichert RHS_SIGN). `XTB::setScfThreshold` ergänzt (Test-Support). 6/6 Tests grün. Roh-`∂q/∂x` `<1%` für polare Moleküle → [docs/PHASE3B5_MULTIPOLE_RESPONSE_WP.md](docs/PHASE3B5_MULTIPOLE_RESPONSE_WP.md). 🤖 AI-generated.

- **Native xTB SCF-Output mit Timing + Verbosity-Stufen (Branch `feature/sqm-implementation`)**: `XTB::Calculation` (`xtb_native.cpp`) gibt jetzt gestaffelt aus (std::chrono-Timing, ASCII). **V0**: still. **V1**: Konvergenz-Status + SCF-Zeit + Final Energy. **V2**: Header (atoms/shells/basis/electrons), Iterations-Tabelle (iter | E(SCC)/Eh | dE | max|dq| | t/ms), Energie-Decomposition, HOMO/LUMO/Gap, Timing-Breakdown (setup/SCF/post-SCF/gradient/total). **V3**: + Setup-Phasen-Timing (CN/H0/gamma/multipole) + DIIS-Diagnostik (n, err) pro Iteration. Doppelte Nicht-Konvergenz-Warnung entfernt; GFN1/GFN2-spezifische Terme (Halogen/Multipol) nur beim jeweiligen Verfahren. Reine UX, Numerik unverändert (Energien bit-identisch, 6/6 Tests grün). 🤖 AI-generated.

- **GFN2 Mulliken-Ladungsantwort — Phase 3b-4 TEILWEISE (Multipol-Pulay, Branch `feature/sqm-implementation`)**: Multipol-Integral-Pulay-Term (AP5b-Analog) zur `computeMullikenChargeResponse`-Gradient-Assemblierung hinzugefügt — D_z (statt P) kontrahiert mit `cgto_multipole_grad_transformed` × konvergierte v_dp/v_qp. **Wirkung**: HCN-Isolationsverhältnis |Δanal|/|Δnum| 1.105→**1.044** (isotrop+Multipol-Pulay erfasst HCNs Response auf ~4%); H₂O bleibt 2.65× (braucht die noch fehlende Multipol-Interaktions-Linearisierung). Alle 6 Regressionstests grün, H₂ Response weiter exakt 0. **OFFEN (Rest 3b-4)**: (a) Multipol-Interaktions-Linearisierung — Section-5-Analog (SD/DD/SQ + mrad/CN-Kette) mit δ-Momenten (δq_at/δdp/δqp aus `mullikenFromDensity(D_z)` bereits verfügbar), je ein Momentfaktor durch δ ersetzt; (b) expliziter Overlap-Term −Tr(Λw·P·Sˣ) (EXPL_FAC=0). Beide brauchen gemeinsame FD-Kalibrierung → Gate <5e-5 auf H₂O/CH₄/NH₃/C₆H₆ erst danach. 🤖 AI-generated.

- **GFN2 Mulliken-Ladungsantwort — Phase 3b-3 (isotrop, Branch `feature/sqm-implementation`)**: `computeMullikenChargeResponse(dEdq, grad_out)` in `xtb_response.cpp` — faltet Σ_A ∂E_D4/∂q_A·∂q_A/∂x für `d4_charge_source="mulliken"` in den D4-Gradienten. Erkenntnis: der Response ist die **Linearisierung des Energie-Gradienten** — P→D_z (Pulay sval), W→W_z (ε-gewichtete relaxierte Dichte), Coulomb-Ladungsprodukt linearisiert (q_is·q_js → δq_is·q_js + q_is·δq_js mit δq aus D_z-Mulliken), plus CN-Kettenregel mit D_z. Z-Vektor via `solveZVector` (Forcing −C_occ^T·G_P·C_virt, G_P=sym(S·Λw)). Verdrahtet im mulliken-Zweig von `calcDispersionEnergy` (eeq-Pfad + Energien unberührt). **Validierung**: H₂ Response = exakt 0 (Symmetrie → keine Baseline-Korruption); HCN z-Response ratio |Δanal|/|Δnum|=1.1 (Vorzeichen+Skala korrekt). **Deferred zu 3b-4**: expliziter Overlap-Term −Tr(Λw·P·Sˣ) (EXPL_FAC=0) und Multipol-Pulay — beide Sˣ-Familie, gemeinsame FD-Kalibrierung nötig; isotroper Rest H₂O/HCN ~4–6e-5 Eh/Å. Gate-Test `test_xtb_cpscf` Test C. Wichtig: ein **vorbestehender** GFN2-Gradient-Baseline-Fehler ~4e-5 Eh/Å (h-unabhängig, in eeq wie mulliken) wurde sichtbar (bei xtb_gradient-Toleranz 5e-4 unsichtbar) — separates Issue. 🤖 AI-generated.

- **GFN2 CPSCF-Infrastruktur — Phase 3b-1 + 3b-2 (Branch `feature/sqm-implementation`)**: Grundlage für die analytische Mulliken-Ladungsantwort `∂q_Mulliken/∂x` (deferred Phase 3b-3/3b-4). **3b-1**: SCC-Potential-Routinen parametrisiert — `addCoulombShellPotential`, `addThirdOrderPotential`, `addMultipolePotential` bekommen Overloads, die Ladungen/Multipole explizit nehmen (bestehende Signaturen sind dünne `m_wfn`-Wrapper → SCF bit-identisch); neuer `thirdOrderKernelDiag` (linearisierter Jacobian `2·Γ·q`); neue SCF-Helfer `buildFockFromPotential` (H0-freies δF) und `mullikenFromDensity` (δq/δMultipole aus δP). **3b-2**: neue `xtb_response.cpp` mit `applyOrbitalHessian` (Orbital-Hessian `A·z = (ε_a−ε_i)z + K·z`, K = SCC-Kopplungskernel via δP→δq→δv→δF→occ-virt-Block) und `solveZVector` (PCG, Preconditioner `1/(ε_a−ε_i)`, relatives Konvergenzkriterium). Gate-Test `test_xtb_cpscf` (H2O/CH4): (A) 1st-order-PT vs Single-Diag-δq_sh = 1.9e-12; (B) Z-Vektor-CG-Residuum |A·z−rhs|/|rhs| = 1.6e-16 (H2O), 3.3e-12 (CH4). Regression: ngfn2-Energien H2O/CH4/NH3/C6H6 bit-identisch, xtb_gradient 3/3, d4_dedq grün. 🤖 AI-generated.

- **GFN2 D4 q-Response — ∂E_D4/∂q · ∂q/∂x (Branch `feature/sqm-implementation`)**: Vollständiger Ladungs-Kettenregel-Term für den GFN2-D4-Gradienten (vorher zetac6 als statischer Prefaktor). Phase 1: `GFNFFParameters::zetaChargeScaleDerivative` (Closed-form dζ/dq) + `D4Evaluator::computeEnergyAndGradient` liefert per-Atom `dE_D4/dq` (FD-validiert 1e-14 Eh/e). Phase 2: neues `D4ChargeModel` (`dispersion/d4_charge_model.{h,cpp}`) — kanonisches Einzelschritt-dftd4-EEQ (ein glattes Linearsystem A·q=b, geometrische Distanzen, GFN-FF-erf-CN) mit analytischer ∂q/∂x via Adjoint/Z-Vektor (FD-validiert ~1e-11 Eh/Bohr). GFN2 (`d4_charge_source="eeq"`, default) nutzt jetzt diese Einzelschritt-Ladungen statt des GFN-FF-Zweiphasen-Solvers (D4-Energie für H₂O/CH₄/C₆H₆ praktisch unverändert), q-Response in `m_disp_gradient` gefaltet. Voller GFN2-FD-Gradient weiter < 5e-5 Eh/Å. GFN-FF unberührt (Topologie-Ladungen, statischer Prefaktor). Test `test_d4_dedq` (Phase 1+2). Phase 3a: `d4_charge_source="mulliken"` (CLI-Flag, via `xtb`-Scope geroutet) speist die GFN2-SCF-Mulliken-Ladungen in zetac6 (Energie + ∂E/∂q); die ∂q_Mulliken/∂x-Response (CPSCF/Z-Vektor, Phase 3b) ist noch offen — mulliken-Gradient nutzt bis dahin statischen Prefaktor (Default `eeq` für Gradienten empfohlen). 🤖 AI-generated.

- **GFN2 D4-Dispersion + analytischer Gradient (May 23, Branch `feature/sqm-implementation`)**: D4-Infrastruktur aus GFN-FF nach `src/core/energy_calculators/dispersion/` extrahiert (`D4ParameterGenerator` verschoben, neuer `D4Evaluator` mit Damping-Enum). Native GFN2 (`xtb_native.cpp`) bekommt erstmals D4-Energie + analytischen Gradienten mit Caldeweyher-2019-BJ-Parametern (s6=1, s8=2.7, a1=0.52, a2=5.0). CN-Chain-Rule via cached `m_disp_dEdcn` in bestehender CN-Distribution-Loop. ForceFieldThread auf Evaluator-Shim umgestellt — GFN-FF bleibt bit-identisch (Tests unverändert). FD-validiert: H₂O/CH₄/NH₃/C₆H₆ MaxErr < 5e-5 Eh/Å. q-Response (∂E_D4/∂q · ∂q/∂x) deferred. CUDA-Hook im Evaluator vorbereitet, kein neuer GPU-Code. Doku: [src/core/energy_calculators/dispersion/CLAUDE.md](src/core/energy_calculators/dispersion/CLAUDE.md). 🤖 AI-generated.

- **GFN-FF WP-FF-SoA — Pass-A/B/C für Repulsion-Pair-Loops (May 2026, Branch `native-gfnff`)**: `thread_local static`-Buffer (flat N×5 RowMajor, keine Heap-Allokation im Hot-Path) in `CalculateGFNFFBondedRepulsionContribution` und `CalculateGFNFFNonbondedRepulsionContribution`. Nutzt bestehenden `fast_exp_neg_sq_block` direkt (arg = α·r^1.5, Funktion berechnet exp(-arg)). Gated via `#ifdef GFNFF_FAST_EXP` — FAST_EXP=OFF bleibt bit-identisch. **Messung**: Polymer N=1410 T=4: FAST_EXP=ON 57.9 ms/step, FAST_EXP=OFF 57.2 ms/step — kein messbarer Gewinn (±0.7 ms Rauschen). Ursache: Repulsions-Listen zu klein für 3-Pass-Overhead-Amortisation. BATM/Angles wurden analysiert und korrekt übersprungen (kein exp()). Mixture N=6200 noch nicht gemessen. Erkenntnis dokumentiert in WP-Dok. 🤖 AI-generated.

- **GFN-FF WP-D Stage C — dcn Neighbor-List reuse (May 18, Branch `native-gfnff`)**: `CNCalculator::calculateGFNFFCNWithNeighbors()` returns CN values, raw CN, and symmetric neighbor list in one pass. `calculateCoordinationNumberDerivatives()` accepts optional `neighbors` pointer; when non-null, replaces O(N²) triangle scan with O(k) neighbor iteration (no threshold check). Hot path in `prepareCNAndEEQ` uses the new API when `cn_cutoff_bohr>0` (default). Fallback when `cn_cutoff_bohr=0` or `external_cn` provided. All four loop sites patched (MT+sequential × FAST_EXP+plain). Polymer N=1410 T=4: dcn median **17.2 → 0.16 ms** (>100× speedup; WP target was ≤15.5 ms). Cache-locality gain on top of ~28× fewer iterations accounts for the unexpectedly large factor. Doku: [docs/wp4/WP-D-Stage-C-dcn-neighbor-reuse.md](docs/wp4/WP-D-Stage-C-dcn-neighbor-reuse.md). 🤖 AI-generated.

- **GFN-FF WP-D Stage B — SIMD `exp(-x²)` in dcn step 3 (May 18, Branch `native-gfnff`)**: Opt-in (`USE_GFNFF_FAST_EXP=OFF`) hand-rolled AVX2 Remez-Polynom-Kernel `fast_exp.h/.cpp` plus Pass-A/B/C-Restruktur + SoA Eigen-Matrix-Scratch in `calculateCoordinationNumberDerivatives()`. Polymer N=1410 A/B auf gleichem Hash: `dcn` median 19.4→17.2 ms (−11 %), 1000-Step-Wall 105.7→104.2 ms/step (−1.4 %). v1-Iteration (push_back in 7 std::vectors) zeigte Pass-A-Overhead frisst exp-Gewinn (`dcn` 21.2 ms, schlechter); v2-SoA-Refactor fängt das ab. Erkenntnis: `exp()` ist nicht der `dcn`-Bottleneck (~0.2 ms Anteil), Memory-Traffic pro überlebendem Paar ist es. `fast_exp_neg_sq_block` als wiederverwendbarer Helper für künftige WPs (CN-Loop, D4-Gaussian-Weights, Angle-Damping) angelegt. Default OFF bit-identisch zu pre-WP. Test `test_fast_exp` 15/15 (≤ 1 ULP über operationale Domäne). Doku: [docs/wp4/WP-D-Stage-B-dcn-simd-exp.md](docs/wp4/WP-D-Stage-B-dcn-simd-exp.md).

- **GFN-FF Static-Mode Performance Initiative — WP-S1/S2/S3 (May 11, Branch `native-gfnff`)**: Three-WP bundle to close the MD-perf gap to xtb-Fortran by freezing per-step recompute and tightening EEQ.
  - **WP-S1**: `static_charges` / `static_cn` / `static_all` PARAMs freeze CN/dcn/D4-weights/Phase-2-EEQ after the first call. Reuses existing `m_last_cn` / `m_charges` / `m_d4_generator` caches; extends `m_skip_eeq_recalc` with a new `freeze_eeq` branch. GPU mirrors via `FFWorkspaceGPU::setStaticFlags()` skipping `computeCN` / `computeGaussianWeightsOnGPU` / `k_cn_chainrule` / EEQ-charge upload. Defaults false → no regression. Saves ~55 ms/step polymer.
  - **WP-S2**: `md_diagnostics` PARAM in simplemd appends a JSONL record per `dump_frequency` step (`<basename>.diag.jsonl`) with energy decomposition, charges, CN, per-atom gradient norms, HB/XB counts. New `MDDiagnosticsWriter` + 3 virtual hooks on `ComputationalMethod` (`getCN/HBCount/XBCount`) wired into `EnergyCalculator`. Reader script `scripts/analyse_diag.py`. CLI test `cli_simplemd_11_diagnostics_dump` 5/5.
  - **WP-S3**: `eeq_distance_cutoff_auto` activates a 30 Bohr EEQ cutoff after Phase-1 when `nfrag==1` and `max|topology_charges|<0.5 e`. `EEQSolver` gains `setEEQDistanceCutoff()` + override member; reads via new `getEEQDistanceCutoffEffective()`. Heuristic logged via `CurcumaLogger::info` at verbosity ≥2. New `test_cases/molecules/ionic/NaCl_cluster.xyz` + `cli_simplemd_12_eeq_cutoff_auto` (6/6, positive caffeine + negative NaCl). Saves ~12 ms/step polymer when triggered.

- **GFN-FF WP-V Energy-Bias resolved (May 10, Commit `cba0696`)**: Pre-existing dead inline fallback `m_config.get<double>("eeq_distance_cutoff", 30.0)` an `eeq_solver.cpp:3077` überschrieb canonical PARAM-Default 0.0 silent für Default-CLI-Invokationen — Phase-2-EEQ truncated alle Default-Calls für N>200. Polymer-Bias zu Fortran von **+930 mEh → +48 mEh** (Restdiff im Torsion-Term). Fortran-Quelle bestätigt: `goed_gfnff` (`gfnff_engrad.F90:1319-1389`) hat keinen Phase-2-Cutoff. Affects nur N>200; kleine Moleküle unverändert. Doku: `docs/wp4/cutoff-inventory.md` (Inkohärenz #1 ✅), `docs/wp4/WP-V-gradient-validation.md`, `docs/GFNFF_STATUS.md`.

- **GFN-FF WP6: Coulomb Cell-List + G2c Phase C + CLI-Plumbing-Fix (May 10, Commit `f3ffe97`)**: Optional `SpatialCellList` für Coulomb-Pairs gated über `eeq_distance_cutoff`. mixture.xyz (N=6200) Coulomb-Wall **611 → 74 ms (8.3×)** bei cutoff=30; default cutoff=0 bit-identisch zu pre-WP6. G2c Phase C: Site 1a Filter in `buildCorrectedEEQMatrix` Geometric-Mode (`eeq_solver.cpp:1175-1185`); Site 5 CN-Derivate-Stencil `cn_deriv_cutoff_sq = max(40², eeq_cut²)` (`gfnff_method.cpp:1014`). Threshold `nb_cell_list_min_atoms` (Default 800, Alias `hb_cell_list_min_atoms`) — ein Knob für HB/XB/Coulomb-Cell-List. CLI-Plumbing-Fix als Voraussetzung: `EnergyCalculator::reattachMethodScopes` re-attached method-spezifische Sub-Scopes nach ConfigManager-Extraction; `GFNFF`-Konstruktor promotiert `parameters["gfnff"]` ins Top-Level; `CurcumaOpt::LoadControlJson` forwarded Method-Scopes ins opt/sp-Config. War pre-existing pipeline bug (`-cn_cutoff_bohr 0.001` propagierte ebenfalls nicht). Activation: `-gfnff.eeq_distance_cutoff <Bohr>`.

- **CLI flat-flag auto-routing + JSON round-trip (Mai 2026)**: ParameterRegistry gains `findOwnerModules()`; CLI2Json routes flat flags (e.g. `-cn_cutoff_bohr 5.5`) to their owning module via the registry. `-import_config` now performs recursive deep-merge and accepts a JSON file with `_command`/`_input` as the sole input (`curcuma -import_config run.json`). `-export_run` writes those meta fields plus full registry defaults so the file alone replays the run. CLI flags always win on import. New ctest category `cli_routing_*`. See [docs/CLI_ROUND_TRIP.md](docs/CLI_ROUND_TRIP.md).

- **WP7-A/B/C: nfrag>1 GPU EEQ-Solver-Triple (Mai 2026)**: Drei aufeinanderbauende WP komplettieren den nfrag>1 GPU-Pfad und vereinheitlichen die Solver-Naming-Konventionen.
  - **WP7-A**: `solveWithDeviceRHSAndGPUSchurGeneral` ersetzt CPU-Schur für nfrag>1 — neue Kernels `k_eeq_reduce_fragment_sums` + `k_eeq_schur_general`; D2H pro Schritt `(nfrag + nfrag²)·8 Byte` statt `N·(1+nfrag)·8 Byte`; bit-identisch zur WP2+CPU-Schur Baseline.
  - **WP7-B**: Per-fragment Cholesky aktiviert via `solve_method=batched` — wiederverwendet bereits implementierten `solveWithDeviceRHSAndGPUSchurBatched`; `eeq_batched_min_distance` (Default 15 Bohr) Warnschwelle bei engem Fragment-Kontakt; `solve_method`-Wertraum vereinheitlicht (`cholesky | batched | pcg | auto | lu`, Alias `schur_cholesky`); CPU `batched` warnt einmal + fällt zurück auf cholesky.
  - **WP7-C**: GPU-PCG mit Schur-Reuse + Warm-Start — neue cuBLAS-Handle, 4 kleine PCG-Kernels (`k_pcg_extract_diag_inv`, `k_pcg_init_residual`, `k_pcg_apply_precond`, `k_pcg_dir_update`); persistente `d_z1_persistent`/`d_Z2_persistent`-Buffer überleben Calls; `cublasDsymv(FILL_MODE_LOWER)` halbiert Matvec-FLOPs vs dgemv; `solve_method=pcg`/`auto` (für N>=`pcg_large_threshold`); Stall-Fallback auf WP7-A; bit-identisch zur Cholesky-Baseline; `CUDA::cublas` zur Linkage hinzugefügt; bug fix in der Stall-Recovery: `m_last_N` ist jetzt exklusiv für die cuSOLVER-Pfade (PCG schreibt es nicht mehr).

- **WP5-B/C/D: Vollständig GPU-residenter EEQ-Pfad (Mai 2026)**: Drei aufeinanderbauende Phasen eliminieren alle CPU-Sync-Punkte im normalen GPU-Hot-Path (nfrag==1, nicht skip_phase2).
  - **WP5-B**: `setCNDerivatives()` wird No-op — `m_cnf`/`d_coul_cnf` entfernt; `k_coulomb_postprocess` liest `cnf` direkt aus `eeq_topo.d_cnf` (topology-konstant); `~11 KB/Schritt` H2D-Upload gespart.
  - **WP5-C**: D4 dc6dcn Skip-Check auf GPU verlagert — `k_check_dc6dcn_skip` (zwei-Stufen-Max-Reduktion) + `d_cn_d4_ref` (Device-Referenz-CN) ersetzt CPU-`recordD4CNValues` + `canSkipGaussianWeightsUpdate`; `~N doubles` D2H-Download pro Schritt entfällt.
  - **WP5-D**: `finalizeCNForCPU()` + `prepareCNAndEEQ(..., skip_eeq=true)` aus dem normalen Pfad entfernt — beide Calls nur noch in Fallback-Zweigen (`skip_phase2 || nfrag!=1`); Hot-Path hat keinen CPU-Code mehr zwischen Phase 1 und Phase 2.
  - **Validierung**: Energie GPU↔CPU < 1 µEh für H2O/Caffeine/Triose/Complex/Polymer; ctest 3/4 pass (1 pre-existing Failure `cli_gfnff_01`); Polymer-MD (N=1410, 5000 Schritte) stabil.

- **GFN-FF EEQ Coulomb-Cutoff Fix (May 6)**: Polymer (N=1410) Coulomb-Energie GPU vs CPU diff Δ≈0.978 Eh behoben. Ursache war ein 30-Bohr-EEQ-Cutoff in `eeq_solver.cpp` (für N>200), der GPU-`k_eeq_build_matrix` nicht spiegelte; im Fortran-`goed_gfnff` (gfnff_engrad.F90:1274-1391) gibt es keinen Cutoff. Fix: GPU-Solver nimmt jetzt `cutoff_sq`-Argument, EEQ-Default `eeq_distance_cutoff: 30.0 → 0.0` (matcht Fortran und stellt Hellmann-Feynman-Konsistenz mit der un-truncated Coulomb-Energie wieder her — vorher beobachtete MD-Thermostat-Drift verschwunden). Cutoff bleibt als CLI-Option für künftige Performance-Experimente, **muss** dann aber konsistent über EEQ-Matrix + Coulomb-Pair-List + CPU-Coulomb + GPU-Coulomb-Kernel gezogen werden. Doku: `docs/GFNFF_GPU_EEQ_CUTOFF_FIX.md`, Roadmap-Eintrag G2c.

- **GFN-FF EEQ verbosity + optimizer hang fix (May 7)**: Moved verbose EEQ charge output from verbosity>=2 to >=3; added `natoms<=100` guard to O(N³) `SelfAdjointEigenSolver` in `eeq_solver.cpp` (was hanging for >100 atoms at verbosity>=3, e.g. 7640×7640 matrix for mixture.xyz); NaN/Inf in energy/gradient now always prints visible error before returning 0.0 (was completely silent, causing GPU optimization to show 0 steps completed); optimizer verbosity clips raised from >3 to >4 (allows verbosity 4 to reach GFN-FF debug output); SP mode now sets global CurcumaLogger verbosity before molecule creation.

## April 2026

- **Native xTB AP5b — GFN2 multipole integral Pulay gradient**: `xtb_gradient.cpp` lines ~316–390: adds `d(dp_jat)/dRiat·v_dp(jat) + d(dp_iat)/dRiat·v_dp(iat) + d(qp_jat)/dRiat·v_qp(jat) + d(qp_iat)/dRiat·v_qp(iat)`; iat-origin terms via origin-shift correction `dDiat = dD_dA + ΔR·dS - δ·S` and traceless `Δqraw` correction; `cgto_multipole_grad_transformed()` in `xtb_multipole_ints.hpp`. FD test: GFN2 CH₄ max error 7e-8 Eh/Å.
- **Gradient unit bug fix**: `xtb_native.cpp` `m_gradient /= au` (was `*= au`); old code gave au² ≈ 0.28× too-small gradient (~72% error at non-equilibrium, e.g. CH₄ 58 mEh/Å off FD); fix: `1 Eh/Bohr = (1/au) Eh/Å`. All 3 FD tests now pass (H₂O, CH₄, NH₃, GFN1+GFN2 ≤ 1.8e-4 Eh/Å).
- **Native xTB AP5 Schritt 1 — GFN2 multipole direct-interaction gradient**: `xtb_gradient.cpp` section 5 (runs before CN section 4): port of `tblite/coulomb/multipole.f90:get_multipole_gradient_0d` — SD (charge-dipole g3), DD (dipole-dipole g5), SQ (charge-quadrupole g5) pairwise gradients; damping radii chain rule `dmrdcn = -t2·kexp·t1/(1+t1)` feeds into `dEdcn` before section 4; TBLite vec-convention `R_jat-R_iat`; `ddmp3` includes g5, `ddmp5` does NOT include g7. Gradient now formally consistent with GFN2 Hamiltonian. AP5 Schritt 2 (integral Pulay `cgto_multipole_grad`) still pending. FD validation test `test_cases/test_xtb_gradient.cpp` added (H₂O, CH₄, NH₃; tolerance 5e-4 Eh/Å).
- **Native xTB AP4 — Analytical gradients for GFN1/GFN2**: `xtb_gradient.cpp` implements `XTB::calculateGradient()` in Eh/Bohr: repulsion gradient, Hamiltonian/Pulay gradient (Obara-Saika `cgto_overlap_grad`), Coulomb ES2 gradient (Klopman-Ohno kernel), CN chain-rule gradient (GFN1/GFN2 double-exp counting). `-opt` converges H₂O, CH₄, NH₃. GFN2 multipole gradient deferred to AP5. Note: unit conversion bug fixed in AP5b.
- **Native xTB AP3 — gfn2/gfn1 default backend switched to native curcuma::xtb::XTB**: MethodFactory `createGFN2()`/`createGFN1()` simplified from priority chain (TBLite→Ulysses→XTB→Native) to direct native call. TBLite/Ulysses/XTB selectable via `ipea1`/`ugfn2`/`xtb-gfn2`. 6 CTests disabled until AP5 re-baselines TBLite golden references. Test 11 updated to verify gfn2==ngfn2 identity.
- **Native xTB AP2 — Wrapper GFN2Method/GFN1Method auf neue XTB-Klasse umgestellt**: `GFN2Method` und `GFN1Method` halten jetzt `curcuma::xtb::XTB` statt der alten monolithischen `GFN2`/`GFN1`-Klassen. Factory-Anbindung opak — `ngfn1`/`ngfn2` nutzen automatisch die modulare Implementierung. Bugfix: `m_wfn.dp_at`/`qp_at` in `buildReferenceOccupations()` für GFN2 initialisiert (vorher Crash in `addMultipolePotential()`). ngfn1 Energien ~5–70 mEh von TBLite; ngfn2 ~35–60 mEh — numerische Korrektheit ist AP 5.
- **Native xTB AP1 — XTB-Klasse konsolidiert**: `UpdateMolecule()` mit Cache-Invalidierung, `m_h0.rad` in `buildH0Data()`, Energy-Component-Getter (`getEnergyDecomposition`, `getNumElectrons`), veralteter hscale-Kommentar korrigiert. Build sauber, sqm_reference 14/14 pass.
- **Native-GFN-FF Branch-Konsolidierung (Apr 28)**: Merge `origin/native-gfnff` (P-/G-Optimierungs-Reihe + numerischer Gradient-Test + PBC + D4 P1a) in lokal `native-gfnff` (HB-Re-Detection-Fix + Stepwise-MD/Opt-API + EEQ-Cherry-Picks aus GPU-Branch); manuelle Konfliktauflösung in `gfnff_gpu_method.cpp`/`simplemd.cpp` mit Erhalt aller wertvollen Blöcke beider Seiten; Reparatur zweier latenter Defekte aus den Apr-25-Cherry-Picks (`ada6925`/`7389e2e`): `eeq_solver.h` hatte alle 87c1a8e/e37db8b-Member-Deklarationen verloren (Build-Fehler), `eeq_solver.cpp` hatte duplizierten `pcg_viable`-Block + undeklariertes `schur_ok`, `gfnff_method.cpp` hatte HB/XB-Cache-Block außerhalb der `if (hbond)`-Klammer; aggressive `e37db8b`-Defaults (PCG, 100, 100) auf konservative Werte (SchurCholesky, 200, 5000) zurückgesetzt; Build clean, 21/22 CLI-Tests grün; eine 3.5 mEh-Regression in `cli_gfnff_01_complex` mit HBond als Hauptverdächtigem dokumentiert (siehe `docs/NATIVE_GFNFF_CONSOLIDATION_2026-04-28.md`)

- **ANCOpt large-system optimizer (Apr 18)**: 8-phase implementation for scalable geometry optimization: (1) Truncated Lanczos ANC generation (`generateANCLanczos`, O(n3²·m) vs O(n3³), Tier L >=600 atoms); (2) Implicit T/R projection O(N²) via rank-6 update (vs O(N³) dense); (3) `detrotra8` gated behind n3<=1800; (4) L-BFGS in ANC subspace for Tier XL (>2000 atoms, replaces dense nvar×nvar Hessian with m=12 pair history); (5) Strong-Wolfe line search in native LBFGS; (6) Shared RF solver (`rf_solver.h/.cpp`, RFSolver namespace) used by both ANCOpt and native RFO; (7) EIGEN_USE_LAPACKE per-file in rf_solver.cpp + lbfgs.cpp; (8) Structured tier advisory at verbosity 2 with per-phase timing; plus LBFGSpp gradient-norm convergence detection fix

## March 2026

- **GPU Pipeline Phase 10: SoA Coordinate Layout (Mar 27)**: Replaced `d_coords[3*N]` AoS buffer with `CoordSoA{d_x,d_y,d_z}[N]` across all 20 GPU kernels + EEQ solver; enables fully coalesced warp reads (~3× bandwidth gain for pairwise kernels at N>100); `d_ref_coords[3*N]` → `RefCoordSoA`; pinned host staging split into 3×N buffers; CUDA Graph re-capture unaffected (pointers stable); 28/28 CLI tests pass
- **GPU Pipeline Phase 3+4: CPU/GPU Overlap + Sync Removal (Mar 26)**: Split `FFWorkspaceGPU::calculate()` into `prepareAndLaunchChargeIndependent()` (non-blocking, ~12 kernels) and `launchChargeDependentAndFinish()` (Coulomb + download), enabling CPU EEQ O(N³) solver to overlap with charge-independent GPU kernels; removed unnecessary `cudaStreamSynchronize` in `computeDC6DCNOnGPU`; relaxed postprocess stream dependencies (k_coulomb_self no wait, k_subtract_qtmp pairwise+bonded only); energy agreement CPU vs GPU < 1 nEh
- **GFN1 Total Energy Now Negative**: Fixed 5 critical bugs making GFN1 produce spurious large positive energies: (1) refocc-based delta-charges in Coulomb energy (was Z_A-based → +2-4 Eh per atom false positive); (2) valence-only electron count in buildBasisSet (was counting core electrons → wrong n_occ); (3) two-pass MakeH loop (was computing off-diagonals before diagonals were set); (4) CN sign correction in getSelfEnergy (+kcn instead of -kcn); (5) repulsion uses geometric-mean alpha + product Zeff + kexp=1.5. Water energy: +∞ → -4.987 Eh (TBLite reference: -5.07 Eh, difference = missing D3).
- **GFN1 Electronic Energy Accuracy Improvements**: Fixed two critical issues in native GFN1-xTB implementation:
  1. Self-energy sign error: Corrected E = selfenergy - kcn*CN (was +) in getSelfEnergy() function
  2. Hamiltonian construction: Added proper density matrix weighting H_ij = scale*S_ij*(H_ii+H_jj)/2 in MakeH() function
  Combined effect improved H2 electronic energy from -1.82 Eh to -0.73 Eh (reference: -1.06 Eh), reducing error from 0.76 Eh to 0.33 Eh.

## February 2026

- **GFN2-xTB D4 Dispersion & Shell-Hubbard Improvements**: Integrated DFT-D4 dispersion into native GFN2 (calculateDispersionEnergy with USE_D4 flag, GFN2_D4_S6/S8/A1/A2 parameters from TBLite); extracted exact SHELL_HUBBARD_CORR array (86×3 elements) from TBLite gfn2.f90 lines 127-171; getShellHubbard() now computes γ_shell = HUBBARD[Z] × (1.0 + correction); expected accuracy improvement ~2-4 Eh when D4 compiled in, ~10⁻²-10⁻³ Eh for shell-Hubbard corrections.
- **GFN2 Native Implementation Modernization**: Analytical gradients implemented for Electronic (Hellmann-Feynman), Repulsion, Coulomb (ES2/ES3), and Coordination Number components; Overlap derivatives added to STOIntegrals.hpp; ParameterDatabase expanded to support all 86 elements with shell-resolved real TBLite parameters; Speedup for optimizations expected to be 10-20x.
- **Native GFN2 Analytical Gradients**: Full implementation of analytical gradients (Hellmann-Feynman theorem) for GFN2-xTB, including derivatives for overlap, Hamiltonian, core-core repulsion, Coulomb ES2, and coordination number shifts; replaces numerical gradients for 10-20x speedup in geometry optimization.
- **STO Integrals Overlap Derivatives**: Added analytical dS/dR and 3D gradient vector functions for Slater-Type Orbital (STO) overlap integrals (SS, SP, PP) in STOIntegrals.hpp.
- **GFN2 Parameter Database Expansion**: ParameterDatabase now supports all 86 elements (H to Rn) with shell-resolved zeta, shpoly, and repulsion parameters; infrastructure for AES2 (dkernel, qkernel) added.
- **GFN-FF Inversion Damping Fix (Feb 11)**: Fixed incorrect star-topology damping in inversion energy calculation; Fortran uses nb1-hub distances (center-nb1 bond + nb1-nb2 and nb1-nb3 as 1-3 distances), not center-to-all-neighbors; complex molecule torsion+inversion error reduced from +234 mEh to -0.45 mEh (520× improvement); also added pi-sp3 torsion barrier override after ring handling block
- **GFN-FF Test Suite Unification (Feb 7)**: Unified fragmented GFN-FF tests into a single high-precision validation runner (`test_gfnff_validation.cpp`), implemented Python reference generator (`gfnff_ref_generator.py`) to bridge Fortran analyzer and Curcuma, standardized reference data in JSON format, achieved < 1 µEh accuracy for many terms across multiple molecules, removed 8+ redundant legacy test files, integrated with CTest for automated regression testing
- **GFN-FF Bond Energy Systematic Error Fix (Feb 1)**: Removed redundant topology_factor scaling in generateTopologyAwareBonds() that applied ring/pi corrections on top of already-computed corrections in getGFNFFBondParameters(), fixing +3% bond energy error, Complex (231 atoms): -38.16 Eh → -37.03 Eh (ref: -37.025 Eh), error reduced from 3.06% to 0.007% (456× improvement)

## January 2026

- **GFN-FF Fragment-Constrained EEQ Charges (Jan 31)**: Implemented molecular fragment detection (BFS) and per-fragment charge constraints in the EEQ solver, expanding the augmented matrix size to `natoms + nfrag`, correcting charge redistribution in multi-fragment systems and reducing Coulomb energy error for a 231-atom complex from 6.5 mEh to 0.65 mEh (10× improvement)
- **GFN-FF HB/XB Parameter Refinement (Jan 17)**: Implemented context-dependent basicity and acidity overrides for Hydrogen and Halogen bonds, added detector for carbenes, carbonyls, nitro groups, and amides, hardcoded XB acceptor basicity to 1.0 per Fortran reference, added sulfur neighbor filter and group 4 base charge criterion for XB detection, significantly improving non-covalent interaction accuracy
- **GFN-FF EEQ Charge Fine-Tuning (Jan 17)**: Aligned EEQ implementation with GFN-FF Fortran reference, removed non-standard neighbor electronegativity correction, added Group 6 (O, S) and Group 7 (Halogens) charge-dependent polarizability (alpeeq) corrections, implemented amide hydrogen electronegativity shift (-0.02 Eh) for H bonded to pi-system nitrogen, refined nitrogen hardness corrections with pi-system (ff=-0.14) and amide (ff=-0.16) factors, added detectPiSystem and detectAmideNitrogens helpers to EEQSolver, improved Coulomb energy accuracy for CH3OCH3 by 82% (8.3% error -> 1.5% error)
- **D4 Dispersion Weighting Fix (Jan 17)**: Corrected D4 weighting formula to use CN-only weighting instead of CN+charge combined, aligned with GFN-FF hybrid dispersion model and Fortran reference gfnff_gdisp0.f90:405, changed default dispersion method to D4
- **GFN-FF Angle Parameter Refinement (Jan 9-10)**: Implemented complete port of element-specific angle corrections from Fortran GFN-FF, added amide detection via FunctionalGroupDetector, implemented pi-bond order approximation for nitrogen angle force constant scaling, achieving 86% angle energy error reduction (9.4% -> 1.3%)
- **Torsion Gradient and NCI Support (Jan 13)**: Implemented full analytical gradients for all torsion types including NCI-specific damping derivatives, matching Fortran engrad formulas exactly

## December 2025

- **UFF-D3 and GFN-FF Native D3 Integration (Dec 19)**: Implemented UFF-D3 hybrid method combining UFF bonded terms with validated native D3 dispersion (10/11 molecules <1% error), replaced GFN-FF's generateGFNFFDispersionPairs() with direct D3ParameterGenerator integration eliminating ~200 lines of duplicate dispersion code, added GenerateUFFD3Parameters() to ForceFieldGenerator, implemented CalculateD3DispersionContribution() in ForceFieldThread with multi-threaded parallelization, both UFF-D3 and GFN-FF now use identical validated D3 infrastructure (PBE0/BJ parameters for UFF-D3: a1=0.4145, a2=4.8593, s8=1.2177), code consolidation achieved with single D3 implementation shared across all force field methods, method routing configured for "uff-d3" command
- **D3 Test Suite Expansion (Dec 19)**: Added triose (66 atoms) and monosaccharide (27 atoms) to comprehensive D3 test suite, validates accuracy on larger sugar molecules (351-2145 pairs), 10/11 molecules now passing with <1% error, tests complex organic systems to ensure D3 reliability at scale
- **D3 Dispersion Triangular Indexing Fix (Dec 19)**: Fixed critical bug in C6 reference data access - Fortran 1-based formula `ic = j + i*(i-1)/2` requires `ic = j + i*(i+1)/2` for 0-based C++, reduced heteronuclear pair errors from 20-87% to <1% (8/9 test molecules now passing: H₂ 0.026%, HCl 0.036%, CH₃OCH₃ 0.659%), O₃ outlier at 10.5% (geometry/CN-specific), D3 implementation now production-ready
- **EEQ Consolidation and D4 Integration (Dec 14)**: Extracted EEQ solver from embedded GFN-FF (2200+ lines) into standalone `eeq_solver.{h,cpp}` (~800 lines) in ff_methods/, integrated with D4ParameterGenerator for charge-weighted C6 coefficients with Gaussian charge-state weighting (expected +20-30% accuracy), refactored GFN-FF to delegate to EEQSolver eliminating ~340 lines of duplication, all tests passing with zero functional regression

## November 2025

- **Phase 2a+2b: Platform-Independent External Dependency Discovery**: Plumed2 now uses find_library() with fallbacks (local build, system libs), DFT-D4 auto-discovers LAPACK/BLAS via find_package(), both with manual override via env vars, CMake reports discovered libraries, tested and verified working on Linux, removes hardcoded .so path and manual env var requirements
- **Phase 1b External Dependency Verification**: Verified all 14 external dependencies have proper conditional compilation guards (USE_XTB, USE_TBLITE, USE_D3, USE_D4, USE_ULYSSES, USE_Plumed), MethodFactory runtime checks functional, CMake flag propagation correct, identified 2 minor issues (Plumed2 hardcoded .so path, DFT-D4 manual config), Phase 1a+1b now production-ready with clean builds at any feature level

## October 2025

- **SimpleMD Coarse Graining Integration (Oct 30)**: Automatic CG system detection (isCGSystem), periodic boundary condition wrapping (applyPeriodicBoundaryConditions), 10x timestep scaling for pure CG systems, CLI test case (simplemd/08_cg_spheres), complete CG MD support ready for production
- **Coarse Graining Phases 1-4 Complete**: Molecule CG detection (isCGSystem, getCGAtoms), ForceField JSON parameter loading (cg_default, cg_per_atom, pair_interactions), VTF reader/writer for CG structures and trajectories, comprehensive test suite (9+6+1 test cases), spherical CG particles fully functional for Casino MC, ready for SimpleMD integration
- **Help System Migration to ParameterRegistry**: 3-tier migration complete, 300+ lines of duplicate hardcoded help text eliminated, 4 modules refactored with registry-based printHelp(), 3 modules enhanced with new printHelp() methods, 2 modules (casino, modern_optimizer) with complete PARAM definitions (29 new parameters), single source of truth for all help documentation, automatic help generation for new modules
- **CurcumaOpt ParameterRegistry Migration - JSON Null Bug Fixed**: 37 parameters defined, ~30 Json2KeyWord calls eliminated, CurcumaOptJson replaced with ParameterRegistry, LBFGS parameter access migrated, fixes critical JSON null bug affecting 11/26 CLI tests, optimization now functional
- **ForceField Inversion Bug Fixed**: Copy-paste error in ForceFieldGenerator::setBonds() causing vector out-of-bounds crash, changed incorrect m_stored_bonds[i] to m_stored_bonds[j] access
- **ConfScan ConfigManager Migration - Multi-Module Architecture Complete**: ConfigManager extended with Multi-Module support, 59 Json2KeyWord calls eliminated, 41 parameters defined, RMSD/Ripser parameters imported via nested notation (-rmsd.method, -rmsd.threads), sLX parsing simplified from ~90 to ~50 lines, #pragma message hack removed, exportModule() API for submodule configs, DRY principle (RMSD params defined once), 100% backward compatible
- **SimpleMD ConfigManager Migration Complete**: 82 Json2KeyWord calls eliminated (LARGEST migration!), CurcumaMDJson removed, ~350 LOC boilerplate eliminated, 48 parameters defined, 100% backward compatible
- **RMSD ConfigManager Migration Complete**: 33 Json2KeyWord calls eliminated, RMSDJson removed, enum-based method selection, simplified element parsing, 100% backward compatible via aliases, build successful
- **ConfigManager Production-Ready**: Type-safe parameter access layer implemented (analysis.cpp proof-of-concept: 37 calls � elegant get<T>())
- **Universal Parameter Registry System Complete**: Macro-based parameter definitions, auto-generation at build time, 25 parameters in analysis module
- **Complete Trajectory Analysis Output**: CSV implementation, bracket notation support, stream refactoring for flexible output
- **Unified Molecular Analysis with Trajectory Statistics**: Moving averages, cumulative statistics, multi-metric tracking
- **EnergyCalculator Architecture Refactoring**: Polymorphic ComputationalMethod interface, MethodFactory priority-based resolution, seamless API migration
- **Universal Verbosity System**: Consistent 0-3 levels across all QM/MM methods, silent optimization support, structured scientific output
- **Physical Architecture Restructuring**: All computational methods consolidated under src/core/energy_calculators/
- **Enhanced Topological Data Analysis**: dMatrix integration via TDAEngine, publication-grade analysis, complete migration path
- **MNDO Integrals Library Extraction (Nov 15)**: Standalone Dewar-Thiel multipole expansion implementation extracted from Ulysses (LGPL 2.1+), comprehensive pedagogical documentation (~700 lines), all integral types (ss, sp, pp, sd, pd, dd) with first/second derivatives, header-only library in src/core/energy_calculators/qm_methods/integrals/MNDOIntegrals.hpp, docs/MNDO_INTEGRALS.md with usage examples and theory, enables native semi-empirical method development
